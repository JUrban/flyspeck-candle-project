schema_version = 1
inventory_id = "candle-flyspeck-ocaml-syntax-v1"
roadmap_version = "1.3"
roadmap_sha256 = "13fc3a6209787e9fa9c1879cb482fef6837252ec96b3ae506d312646c898863f"

# This is a repository-snapshot inventory, not yet the production dependency
# closure.  Exact load-closure membership is deliberately a later Phase 0
# manifest result and must not be inferred from a hit in this inventory.
scope_kind = "tracked_ocaml_family_repository_snapshot"
extensions = [".ml", ".mli", ".hl", ".vhl", ".cml", ".mll", ".mly"]

[repositories.candle]
path = "../worktrees/candle-loader-v13"
pinned_commit = "87f1fd965313c090fd4d87bcfcc493a3ad7bc79f"

[repositories.flyspeck]
# Direct S3 compatibility evidence must come from the clean upstream worktree.
# The PFT development head is tracked separately in manifest.lock.toml and is
# intentionally not part of this inventory.
path = "../worktrees/flyspeck-v13-source"
pinned_commit = "1ce0353008eba83d3c76ae9a25c3c242e4802d53"

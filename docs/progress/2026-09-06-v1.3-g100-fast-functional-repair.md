# Great-100 fast functional repair

Date: 2026-09-06 UTC

## Scope and status

This report covers only G100-F, the explicitly nonpromotable fast functional
Great-100 gate.  It does not claim exact post-state agreement, S1, or release
qualification.

The first complete fast census is retained at:

```text
/project/flyspeck-candle-runs/
  great100-compact-localizer-eabe9a6-all65-001/report.json
```

It completed all 65 targets with no timeout and reported 36 functional
matches and 29 failures.  Focused checks at the repaired head then passed all
29 failed targets.  The coherent-head 65-target rerun is retained at:

```text
/project/flyspeck-candle-runs/
  great100-compact-localizer-9a7b250-3ef67e3-all65-001/
```

That run completed all 65 targets with 65 theorem-compatibility passes, no
failure, and no timeout.  G100-F is therefore closed at 65/65.  This is still
explicitly nonpromotable diagnostic evidence: its compact identity comparison
is MD5 plus exact wire length under the unapproved repaired serializer, and it
does not claim exact post-state agreement.

## Repaired compatibility classes

The repair series on Candle branch
`codex/flyspeck-v13-great100-csdp-bridge` now ends at `3ef67e3`.
Its functional changes are intentionally split into small commits:

- `8668a7e`: repair the common CakeML kernel startup typing failure;
- `7b192f6`, `a4a859e`, and `a2c0df2`: make generated variables, numbered
  arguments, and infix tuple inputs CakeML-compatible;
- `6dc0a72`: use a CakeML-executable but wire-compatible fingerprint encoder;
- `eabe9a6`: keep the unneeded optional `compute.ml` extension outside the
  default HOL base;
- `a0c8a51` and `131e434`: repair `GROUP_RULE` local typing and comparison;
- `a6f5641`, `1c8c4c7`, and `358f52e`: repair primed variables, a sum-product
  type, and OCaml integer-division behavior;
- `3d3a723`, `f54a39c`, `60cf506`, and `72cd0a3`: propagate explicit
  finite-function and sorting comparators through `Examples/sos.ml`;
- `cffad3b`: use the existing pure factoring path when unrestricted
  `Sys.command` is unavailable; and
- `3ef67e3`: remove mathematically inactive variables from an SDP and assign
  those free parameters zero during certificate reconstruction.

Exact, source-identity-bound diagnostic normalizations in controller branch
`codex/flyspeck-v13-great100-compatibility-localizer` cover generated-name
identity differences and the fixed CSDP handshake.  The controller head is
`9a7b250`.  The localizer remains fail-closed and cannot emit S1 or promotion
markers.

Focused evidence includes:

- the 20-target `GROUP_RULE` sweep, followed by exact 10-GiB reruns for Buffon
  and Cubedissection;
- exact Heron, Ramsey, Machin, Birthday, Derangements, Pascal, and Fourier
  runs; and
- exact Ceva and Thales runs after the SOS/CSDP repair.

## SOS/CSDP localization

The diagnostic external solver is fixed to:

```text
/project/deps/hol-light-external-tools-v9/usr/bin/csdp
SHA-256 50a07f934ffac42b774e0991ff5e44cc68a6d8db180258a8fc1d1676cc7e898d
```

The controller invokes that binary with a fixed argument vector, sanitized
single-thread environment, per-target directory, and exact request paths.
General `Sys.command` remains disabled.  HOL parses the returned floating-point
solution, rationalizes it, and reconstructs and checks the exact certificate.

An initial Ceva bridge run reached the solver but CSDP returned status 206 for
an input containing two empty constraints.  An isolated read-only native HOL
Light replay retained all 26 reference CSDP exchanges.  It established that
the explicit-comparator finite-function representation selected a different,
but algebraically valid, Gaussian free-variable basis.

The failing Candle problem had 146 candidate variables.  Variables 1 and 2
had zero objective coefficients and no entries in any matrix.  Removing only
those inert dimensions produced a 144-constraint input with zero empty
constraints and changed CSDP's result from malformed-input status 206 to the
ordinary expected infeasible-search status 2.  The retained replay is:

```text
/project/flyspeck-candle-runs/csdp-ceva-inactive-reduction-001/
```

Commit `3ef67e3` performs that reduction before solver invocation and assigns
the omitted free parameters zero for later exact reconstruction.  It does not
change the polynomial goal, assumptions, SOS certificate condition, or HOL
proof translation.

The resulting focused evidence is:

```text
/project/flyspeck-candle-runs/
  great100-compact-localizer-9cbaef1-ceva-sos-active-002/report.json
/project/flyspeck-candle-runs/
  great100-compact-localizer-9a7b250-thales-sos-active-001/report.json
```

Ceva passes after 26 solver exchanges.  Its complete return-code sequence is
the same as the native replay, and its `CEVA` theorem, empty hypotheses,
conclusion, and three global axioms match the approved expectations exactly.
Thales passes after 32 solver exchanges, with the same exact checks for
`THALES`.  No retained input in either run has a zero-objective empty
constraint.

The controller now stores immutable input, parameter, output, stdout, and
stderr snapshots for every solver request.  This is covered by 13 focused
unit tests, including a two-request overwrite regression.

## Complete coherent-head census

The completed run used Candle `3ef67e3`, controller `9a7b250`, seven workers,
and a 10,240-MiB heap cap per worker.  This kept the campaign below ten CPUs
and normally near 60--75 GiB resident memory.  Every target had an independent
5,400-second wall bound and 1,800-second output-inactivity bound.  Total
campaign wall time was 11,353.2 seconds; the final and slowest target,
`100/transcendence`, passed in 2,942.8 seconds.

The machine-readable summary is:

```text
reported targets                  65
theorem compatibility matches    65
failures                           0
timeouts                           0
functional_compatibility_65_of_65 true
post-state exact matches           0
promotion eligible                false
S1 evidence                       false
schema-7 evidence                 false
```

Every result has status `PASS` and compatibility status `theorems_matched`.
For every requested theorem, the diagnostic compares the structural theorem,
hypothesis, conclusion, and global-axiom wire encodings with the approved
reference expectations using MD5 plus exact byte length.  The report's own
warning and promotion object bind it to diagnostic use only.

G100-F is complete.  Work now moves to exact common-base composition, explicit
serializer authority, one representative exact theorem-plus-state comparison,
and then G100-S over all 65 targets.  No release bootstrap,
checkpoint/security work, provenance-schema work, or compiler specialization
is authorized before those exact-state gates.

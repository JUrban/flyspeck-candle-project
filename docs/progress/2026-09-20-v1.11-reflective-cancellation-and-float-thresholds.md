# Direct Flyspeck and reflective certificate milestone

Date: 2026-09-20 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still running and has not earned
  its success marker.
- The cumulative action-184 process has now completed all terminals in
  `hard_1.dat` (2,899/2,899), `hard_10.dat` (58/58), `hard_11.dat` (291/291),
  and `hard_12.dat` (182/182). At the 2026-09-20T07:03:50Z sample it was at
  terminal 46/197 in `hard_13.dat`.
- The independent focused action-296 nonlinear process remains alive but has a
  zero-byte buffered log and receives no action, S2, S3, or release credit.
- PFT remains alive as the independent oracle. None of these processes was
  interrupted by the reflective work.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Nonlinear proof-producing first leaf

The fourteenth fresh first-leaf attempt ran for 4,210.637 seconds, used
1,178,240 KiB peak RSS, completed `arith/float_pow.hl`, the large
`trig/exp_log.hl` dependency chain, and `trig/poly_eval.hl`, then failed during
inference of `trig/exp_eval.hl:184`. Native OCaml inferred `r <= t` over two
doubles while Candle's unqualified ordering is integer-only. This is a
frontend/runtime compatibility failure before closure completion, not a
nonlinear theorem failure.

The repair at Candle commit `d3d758749b3c123e4d463c2f3c3f716f3d596fa1`
does not patch only the observed line. It mechanically covers all 12 active
raw-double threshold and angle-reduction comparisons across seven
authenticated checker sources with typed IEEE operators. The central runtime
now exposes typed `<`, `<=`, `>`, and `>=` helpers.

The focused differential gate compares all four operators for every pair in a
seven-value matrix containing a finite negative and positive, both signed
zeros, both infinities, and NaN. All **196 native OCaml/Candle observations**
match byte-for-byte. The authenticated nonlinear closure remains 90 sources
and 193 edges; it now has 26 normalized sources and 138 exact operations. All
37 nonlinear closure, target, parser-controller, and smoke regressions pass.

Fresh attempt v15 is active on those exact committed bytes under
`/project/flyspeck-candle-runs/cv-nonlinear-first-leaf-v15`. It was loading the
same large analysis dependency chain at this report sample. It makes no proof
or performance claim until it terminates and writes its result.

## Reflected LP cancellation endpoint

The accepted real `hard_2` bulk-soundness evidence remains locally material:
the selected constraint-construction/combination slices for terminals 15, 5,
and 17 are 9.87x, 4.15x, and 3.66x faster than the corresponding legacy
slices. This is not an inclusive terminal speedup because the accepted run did
not include the exact reifier, evaluator, public rendering, variable bounds,
cancellation, or final discharge.

The isolated LP branch has now advanced to
`9e4abad8e1666cbb3ed570a4dc1526cb93674358`. Its Flyspeck adapter:

1. computes and canonicalizes the complete exact coefficient vector through
   `Kernel.compute`;
2. requires every coefficient to cancel to zero and the exact right-hand side
   to be strictly negative;
3. re-renders and reifies that computed result; and
4. derives `F` through an ordinary HOL theorem.

The focused source-interface test combines `x <= 0` and `--x <= --1`, obtains
the exact computed result `([(0,0)],(0,1))`, preserves both source hypotheses,
and leaves the global axiom set unchanged. This reaches the structural boundary
of the legacy per-variable `add_cancel_step` fold. It is a machinery result,
not a real-terminal timing result.

The earlier faithful `hard_2` profile measured 33.874 seconds in variable-bound
theorem construction and 22.042 seconds in variable cancellation, so this is
the next performance-bearing target after the already accelerated constraint
aggregate. The complete real old/new comparison remains queued: a fresh
18-GiB checkpoint restore is unsafe while swap is nearly full and the three
long-lived verification computations are useful. Rejected partial checkpoint
states are not reused.

## Plan impact

The evidence continues to move work away from `compute_all` list machinery,
which occupied only 1.269% of the faithful `hard_2` verifier profile, and toward
coarse proof-producing certificate checks:

1. let nonlinear v15 determine the next exact formal-verifier boundary;
2. when memory pressure safely permits, run the complete LP public-interface
   adapter and cancellation endpoint on real terminals 15, 5, and 17, checking
   conclusion, hypotheses, theorem digest, axioms, wall time, CPU, and RSS;
3. if inclusive benefit remains material, integrate behind the existing LP
   verifier interface with the legacy path as a fail-closed development oracle;
4. continue from a proof-producing nonlinear leaf toward reflected partition
   reconstruction and ultimately direct interval/nonlinear leaf checking.

No cumulative replay will be restarted merely for this side lane. A fresh
action-zero confirmation remains reserved for a coherent successful direct
integration batch.

# v1.61: Late-verifier checkpoint and adaptive rational normalization

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T11:13:08Z`, the protected cumulative action-184 run had
  freshly passed `hard_7.dat` terminal **6,814 / 9,080** and remained live.
- The independent reverse LP diagnostic remains complete and green from
  terminal **7,476 down through 5,623**.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## The prime-denominator growth is removable, but fixed deep reduction is costly

Candle commit `499e6e0` raises the shared ordinary/reflected Euclidean candidate
budget from 128 to 256 steps.  This does not enlarge the trusted boundary: the
candidate remains untrusted, every exact divisibility identity is checked, and
failure still falls back to the unreduced rational.

The exact same 917-monomial, degree-six asymmetric-prime case now completes all
136 required Euclidean steps.  Its final representation changes from
3,720/3,703 numerator/denominator bits with a 3,451-bit residual common factor
to **269/252 bits with gcd 1**.  Thus the previously observed growth is a
normalization-budget effect, not inherent arithmetic growth.

The sufficient fixed budget is not a good production policy by itself:

| Measurement | 128 fixed | 256 fixed | adaptive 128+128 |
| --- | ---: | ---: | ---: |
| degree-six narrow box | 32.191 s | 44.167 s | 35.235 s |
| degree-six prime box | 44.268 s | 50.730 s | **41.099 s** |
| degree-six wide box | 33.738 s | 44.642 s | 32.416 s |
| complete degree-six family | 112.472 s | 141.497 s | **110.801 s** |
| full degree-two-through-seven session | 6:30.65 | 8:15.05 | 6:42.87 |
| peak RSS | 2,251,648 KiB | 2,248,960 KiB | 2,251,648 KiB |

The session remained single-core and green through degree seven.  Candle commit
`b557363` therefore implements a proof-producing resumable reducer.  It carries
the original 128-node reflected fuel term, returns the exact unfinished
Euclidean `(a,b)` state, and supplies a second 128-step chunk only if `b` is
still nonzero.  The candidate remains untrusted and passes through the same
exact checked-normalization boundary.

A focused assumption-free test uses a Fibonacci pair whose reduction remains
unfinished after the first chunk, proves that the state is preserved, resumes
it to gcd 1, and also checks an ordinary short gcd.  The exact-byte green run is:

```text
/project/flyspeck-candle-runs/cv-exact-rational-normalize-adaptive-v14-run-001
```

Its result-log SHA-256 is
`e3342e274471bcafdcc36e8e989f9e969fbb3aa7d4fc1168c862e484a5c2c3f6`.

The complete adaptive scaling session is green through degree seven.  It keeps
the compact **269/252-bit, gcd-1** prime result, makes the whole degree-six
family 1.5% faster than the original insufficient 128-step policy, and cuts
the fixed-256 full-session penalty from 26.7% to 3.1%.  The isolated prime box
is 7.2% faster than the old insufficient run and 19.0% faster than fixed 256 in
these measurements.  This is the intended reusable policy: common reductions
do not carry the large fuel term, while deep cases still finish exactly.

Adaptive evidence root:

```text
/project/flyspeck-candle-runs/cv-polynomial-expression-dim-jet-scaling-timed-v14-adaptive-run-001
```

- result log SHA-256:
  `4368670bda7d71e65e6a478f086c95c3ee6c04792190d33a0699521c4fc177b5`;
- external phase telemetry SHA-256:
  `aefb6e0fb80bfd6ef42dab2838c1cdfddccc676a96b3078f226298c88cddac4a`;
- resource record SHA-256:
  `4b302d8ef66703544d558e4f479b10f090004180676eb2db2d4adc09b9a1893d`;
- generated input SHA-256:
  `6077d8195230105e8ce37650f8f5cb119334e527289dd03cbe85f2599833d29c`;
- input acknowledgement SHA-256:
  `b35e35dc68aeace2a975c82443a974d02d37818c63adebc8c056810e5058a995`.

Evidence root:

```text
/project/flyspeck-candle-runs/cv-polynomial-expression-dim-jet-scaling-timed-v13-fuel256-run-001
```

- result log SHA-256:
  `49902391925d904fd21b63535d42120e7831a79d4a6eccc0489f4b31a7e72639`;
- phase telemetry SHA-256:
  `e331931d168e4f16da031bc66e1ffb7cee9a66ac277d5120ccf889c7439f3edd`;
- resource record SHA-256:
  `9800ab7a8d168e0e2ab22b0548bbf5ece6a6ed5d3cade464de7b4d6009254505`;
- generated input SHA-256:
  `95e48b81c0efebed9661a68d36968f8e81e58ac728c5aafe8d7b199faa097270`;
- input acknowledgement SHA-256:
  `24ed29e8ca9befe9bfc65ca83e866168559578ee60549194bb0b76f78d9bf952`.

## The 11–13 minute prefix is now a reusable boundary

The exact nonlinear retry first passed all of `informal_search.hl` and
`m_verifier_main.hl`, emitting `CANDLE_NONLINEAR_VERIFIER_CLOSURE_LOADED`.
It then stopped in the late historical Azure support source, rather than the
verifier proper.  That successful closure was captured as a sealed DMTCP
checkpoint before `definitions.hl` and the experimental driver.

The first checkpoint is:

```text
/project/flyspeck-candle-runs/cv-nonlinear-verifier-closure-checkpoint-v1
```

Its checkpoint SHA-256 is
`7bedf246faaf29bfd86dc30cc968455243ed546f52770b1bad7cd32d5455e5f5`;
its base-log SHA-256 is
`22de43673490e58bbc551f80af439fe1220f733338c7026f6f8b7662add16edb`.
Each restore reauthenticates the checkpoint, suffix, prelude, and driver, then
requires a nonce-bound fresh-input acknowledgement.  A late failure now costs
roughly **9–60 seconds**, depending on how far the driver gets, instead of
replaying the full prefix.

The first build controller initially missed the readiness marker because the
interactive runtime prefixed it with a prompt.  The loaded state was preserved,
checkpointed with the same DMTCP operation, and sealed after validating exactly
one marker and no source error.  The controller now counts the marker as a
substring, so the harness cannot repeat that stale-log/readiness mistake.

## Bounded late compatibility review

The cheap restore loop classified the complete currently visible late failures:

1. The five `Vectors` and two `Transcendentals` qualified theorem names used by
   the authenticated historical `definitions.hl` were absent.  Commit
   `aa7198d` restores exactly those seven names to the identical current
   assumption-free theorem values.  A focused load passed before the actual
   support-source retry.
2. The first driver incorrectly sent the still-encoded outer `ineq` term to
   `dest_ineq`; the next version now uses the exact source-expansion chain from
   Azure's `main_verifier.hl`.
3. The verifier's two `List.sort compare` sites reached Candle's deliberate
   fail-closed polymorphic-comparison guard.  They now use `Term.compare`, the
   explicit structural order already checked against native OCaml ordering.
4. The historical finite-type-name decoding in `get_dim` failed on a binder
   independently confirmed as `real^6`.  It now resolves the type against the
   verifier's own bounded `n_vector_type_array`, which is the authoritative
   table consumed by every downstream dimension-indexed array.
5. The refreshed checkpoint then reached the first real polynomial but raised
   `Bind` before constructing its differentiability theorem.  A bottom-up probe
   reduced this to the single term `x$1`; the identical explicit
   `diff2c_domain_x` proof passed.  The defect was not theorem search: the
   historical file had two private copies of the old dimension decoder inside
   `gen_diff2c_domain_poly` and `gen_partial_poly`.  Both now call the validated
   central `get_dim`.

The ordering and central dimension repairs are on Candle branch
`codex/cv-nonlinear-whole-box-v1` (`aa7198d`).  The historical-source development
branch records the corresponding runtime work in `17958f72` and the two private
decoder replacements in `40e3e6dc`.  The immutable original source identity
remains unchanged; authenticated overlays carry the development changes.

Preserved failure-log SHA-256 values are:

- missing `Vectors.SQRT_LE_0`:
  `95e89da838aac9b5f0cfc1f210b7203a0c27f65cfd60ba0972dacce620224132`;
- missing `Transcendentals.ACS_ATN`:
  `95dee65937727103f57d774f1f708625b9616ff3475f153ddb90d6f02686c56f`;
- incorrect pre-expansion `dest_ineq` boundary:
  `1fe4e27c97fe3d4111735f3ad8b947d94efdd859d75a41b1072abdc2bcceb4f7`;
- unsupported polymorphic ordering:
  `3e6fee700c5f326c9286dc26f1611aba24bc9845be6811acb3000473a02303a2`;
- historical `get_dim` failure on `real^6`:
  `961a406700a0890563346cf871cc511305cee1e8670f835d5111ab7eb375b887`.
- first polynomial failure through the stale private decoder:
  `f1b2b2610f306389b1270f4580cc6599e8ea36fa7d97f0a060c86ffa3bef6604`;
- smallest-subterm proof that the same failure occurs at `x$1`:
  `ad70735cdddd73d51111e78c5f86f398d12ac604016423649078c1d4e273b934`;
- explicit unchanged `diff2c_domain_x` proof passing immediately before the
  stale private decoder fails:
  `faa7df6a33196d7b672d737a6fa6b2e03fc8cb3432cce1b5a72fab696af9e09d`.

The new close checkpoint containing all five coherent repairs is:

```text
/project/flyspeck-candle-runs/cv-nonlinear-verifier-closure-checkpoint-v3
```

- checkpoint SHA-256:
  `b303cabf2866433d6d9f4b8c3d222708b501375cc7e673e2da04a1b6a6729881`;
- base-log SHA-256:
  `1e832f518fdae58b1e84750ecabb2d1a483ad4b88829ddef5fe1e79f4eec3350`;
- authenticated suffix SHA-256:
  `042931f2aaa5462f04bc5a9e2e0b43ee3a2e48562a91aed5c1c68305fac0be94`.

## Complete real-inequality milestone

A clean driver is running from the refreshed closure checkpoint.  It follows
the real Flyspeck source conversion, constructs the
expected **16-pass / 15-glue** certificate, measures an ordinary first-leaf
Taylor proof, then reconstructs every leaf and glue node.  Final assembly must
produce an assumption-free theorem whose conclusion is syntactically identical
to the original six-variable `ineqm` target.  The driver fails closed on any
certificate-shape, theorem-interface, conclusion, assumption, or global-axiom
drift.

The clean run has crossed the stale decoder boundary and is constructing the
first real six-dimensional polynomial verifier record.  This is the next
integration result; reflected Taylor replacement will be compared at the leaf
boundary only after the ordinary complete theorem is green.

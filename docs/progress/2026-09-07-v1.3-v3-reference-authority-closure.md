# V3 native-reference authority closure

Date: 2026-09-07 UTC

## Result

The fresh candidate-V3 native-reference campaign is closed, independently
reviewed, approved, and committed into Candle.  The exact evidence-bearing
Candle head is:

```text
cba422200896b46196dffc86568cd95bcf3313d9
```

This freezes the serializer, final consumer, two-sweep native evidence,
identity approval, and generated Great-100 manifest as one reviewed commit.
The next admissible operation is the fresh cache-disabled canonical bootstrap
from this head; no later Candle source or evidence change may be substituted
without reopening the authority and bootstrap gates.

## Closed collection and independent review

The immutable native collection is:

```text
/project/flyspeck-candle-runs/
  candidate-v3-reference-v9-two-sweep-afc95fb-c00408a-001
```

Its receipt records a closed, complete schema-5 campaign: 130 of 130 target
runs succeeded, comprising two nonoverlapping sweeps of all 65 targets, with
zero pending targets and zero failed attempts.  The producer identities are
Candle `afc95fb` and project `c00408a`; the native HOL reference head is
`1258c129`, and the V3 serializer SHA-256 is
`444edaba460b2e9ab01c535fbfb18e9fe9932dcf6fe7f8996e2008b47991b110`.

The independent final audit ran without the incomplete-review allowance.  Its
report is:

```text
/project/flyspeck-candle-runs/
  v3-reference-authority-review-83a583f-final-130/report.json

SHA-256
  3b81e889d0adf80c380ad0fdc81f5caa45e8eb5ed78deb7b25193adcab9dc393
```

The audit reviewed 130 of 130 runs, found zero failures, established 65
identical two-sweep pairs, and cross-matched all 65 identities to the completed
G100-S diagnostic.  It was performed with reviewer Candle head `83a583f` and
the project review implementation at
`35f44a61faa767f32e01f846ebe287d91d86c22f`.  The report intentionally stopped
at `review_complete_unapproved`; promotion was enabled only by the separate,
mechanically generated approval committed after that review.

## Evidence-bearing Candle head

Candle commit `cba422200896b46196dffc86568cd95bcf3313d9` imports exactly the closed
collection contract, receipt, and two sweeps.  The imported evidence contains
1,172 regular files and 2,699,170,830 bytes, with no symlinks and no shared
hardlinks.  Controller lock and mutable status files are excluded.

The resulting control identities are:

| Control | SHA-256 |
| --- | --- |
| `candle/top100_identity_approval.json` | `d82d0e8a413fdb78ded902e5a6d8537d95da65ef5bb194a39883b80a751a3de8` |
| `candle/top100_manifest.json` | `822f7f42fcb6f8ab4490c573763595a3224bfcfdc92c5bde68451666d222ed5c` |
| approval evidence inventory | `3021163dbf52b66cb8e8f733b5eb7c409ccd9f21b94e20d55cb3569388fb8d99` |

The schema-v2 approval binds all 65 targets and all 130 audited runs and sets
`approval_status: approved` and `promotion_allowed: true`.  Manifest
regeneration replayed the complete imported authority.  An explicit
`top100_manifest.py --check` then reported 65 targets, 66 source files, and one
declared exclusion.

The full Candle test suite passed after approval: 359 tests ran in 399.168
seconds with no failures.  The previously deliberate stale-V2 manifest failure
is therefore closed without weakening the approval contract.

## Gate status and next action

This closes the V3 authority and final-head-freeze gate.  It does not itself
constitute schema-6 bootstrap evidence, either of the two clean final Candle
Great-100 executions, independent S1 finalization, S2, S3, or a release claim.

The immediate next action is one fresh, cache-disabled canonical CakeML
bootstrap with `Holmake -j1`, using Candle head `cba4222`, CakeML head
`8a8926906`, and HOL head `a390cba`.  After its linked artifact is recorded,
the roadmap requires two clean full Candle Great-100 executions followed by
independent S1 finalization.

# Diagnostic bootstrap transition — 2026-08-29

## Result

Candle commit `45f65aa1bb68b2e91bcc43945f23779ec1bba8df` adds a
fail-closed, **diagnostic-only** way to reuse an earlier canonical CakeML
bootstrap when every Candle-side bootstrap/link input is byte-identical at a
later Candle commit.  It does not turn the earlier receipt into final-head
release evidence.

The transition produces the distinct schema-7 kind
`candle-linked-pinned-cakeml-transition`.  Great100 promotion continues to
accept schema 6 only.  The original two-argument exact-root linker remains the
release path, so S1/S2 promotion still requires a clean canonical bootstrap and
ordinary schema-6 link at the exact final Candle head.

No transition or link has been launched: the live canonical bootstrap receipt
is not yet present.

## Bound transition

The transition reconstructs and compares this committed Candle closure:

- `candle/flyspeck_manifest.json`;
- `candle/cakeml_artifact_provenance.py`;
- `build-local-cakeml-bootstrap.sh`;
- `candle/cake.S.patch`; and
- `candle/insulate.py`.

For source `27617a9...` and final `45f65aa...`, both sides have closure SHA-256
`addc41ad709fae3f551b105ea97072b49ff1a295ff026cb9e872a474f404a7be`.
The original provenance controller remains byte-identical at SHA-256
`df15291e7c95e8aefbd7880d249f819701244173ff4446a8a607cc56c51255fe`.

The controller requires distinct clean exact Git roots and heads, source
ancestry, no grafts/replacements/hidden index flags, exact manifest pins, an
external ordinary bootstrap receipt, and an external ordinary transition
record.  Canonical receipt validation is performed by the exact source
controller in an isolated `python3 -I -S` subprocess.  The retained schema-7
record cross-binds:

- source and final roots, heads, ancestry, and committed closure;
- CakeML root and head;
- the canonical receipt and retained preflight controller identities;
- the transition record and final transition/link controller sources;
- native link derivation, patched assembly derivation, outputs, ELF closure,
  and runtime version output; and
- the archived transition copy in direct diagnostic runtime snapshots.

Removing, changing, rebinding, or consistently rehashing only the transition
claims fails against live Git/source reconstruction.  Schema-6 dispatch also
requires the retained canonical receipt's root/head to equal the linked
root/head, blocking a schema-7 deletion/downgrade from masquerading as an
ordinary exact-root link.

## Review and validation

An independent security review passed schema 7 for its diagnostic claim and
found no remaining concrete implementation blocker.  It retains one explicit
release-policy limit: linked JSON is not a signature, so a party able to replace
and consistently rehash an entire ignored schema-6 linked tree still requires
external final-head canonical receipt validation or independently authorized
immutable evidence.  The transition deliberately does not weaken that rule.

Parent reruns at clean `45f65aa` passed:

- transition suite: 16/16;
- direct stratum suite: 27/27;
- full Candle discovery: 232/232; and
- compatibility discovery: 63/63.

The implementation was kept in nine reviewable commits from `0b16bfd` through
`45f65aa`, including explicit authority-rebinding, schema downgrade, omitted
transition, altered controller, adjacent bytecode, graft/replacement/index
flag, and diagnostic-promotion regressions.

## Use after the live receipt

When the canonical `27617a9...` receipt arrives, validate it first at its exact
source root.  A schema-7 transition may then link `45f65aa...` only for direct
diagnostics and speed gates.  It must not be cited by the Great100 approval or
the eventual S1/S2 finalizer.  In parallel, launch and retain the clean
final-head canonical bootstrap needed for an exact-root schema-6 release link.


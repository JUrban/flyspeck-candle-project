# Direct Flyspeck and reflective LP real-evidence milestone

Date: 2026-09-19 UTC
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative status remains 185 actions reached / 184 passed at the
  `05-lp_support-through-184` boundary.
- The fresh cumulative action-184 run remains healthy inside the real
  `hard_1.dat` LP certificate. At this report sample it had reached terminal
  1,354 of 2,899; action 184 is not yet passed.
- The focused action-296 nonlinear run remains active from a fresh copy of the
  clean post-295 checkpoint. It has not produced a pass marker, so it receives
  no cumulative, S2, S3, qualification, promotion, or release credit.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Real `hard_2` bulk result

The generic bulk-soundness prototype has now passed the same three selected
terminals from the real `hard_2.dat` certificate in three successive fresh
checkpoint attempts. Attempt 11 corrected only the profiling marker pair and
provided an accepted 50 ms-sampled profile:

| Terminal | Rows | Hypotheses | Raw selection | Bulk soundness | Selected slice | Legacy construction + combination | Ratio |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 15 | 402 | 68 | 1.625 s | 1.016 s | 2.641 s | 26.077 s | 9.87x |
| 5 | 480 | 78 | 1.828 s | 2.132 s | 3.960 s | 16.449 s | 4.15x |
| 17 | 382 | 65 | 1.724 s | 1.117 s | 2.841 s | 10.407 s | 3.66x |

This is a material local benefit, but deliberately not an end-to-end terminal
speedup claim. The measured prototype slice performs raw inequality selection
and one generic bulk soundness proof. It does not yet include exact Flyspeck
reification, `Kernel.compute`, canonicalization, public `lin_f` rendering,
variable-bound construction, cancellation, or final target discharge.

The run also measured 1.026 seconds for prototype loading, 55.644 seconds for
inequality initialization, 0.053 seconds for certificate decoding, and a peak
Cake RSS of 18,060,168 KiB. The accepted evidence is
`/project/flyspeck-candle-runs/cv-lp-bulk-hard2-v11`; attempts 8 and 10 are
retained as rejected harness evidence, and attempt 9 is functional-only
evidence.

## Proof-producing core progress

The isolated `codex/cv-lp-linear-combination-v1` branch now adds two coherent
kernel-checked layers beyond the prior realization milestone:

1. commit `de4fd3b` reifies each authenticated source inequality into an exact
   signed-natural row, proves that row denotes the source row, applies bulk
   soundness once, evaluates the complete fold through `Kernel.compute`, and
   returns an ordinary HOL inequality with the complete source hypothesis set;
2. commit `f862f33` performs a second verified computation that canonicalizes
   every signed pair and proves scalar and vector real denotations unchanged.

The full generic core test passes with marker
`CANDLE_CV_LINEAR_COMBINATION_CORE_OK`. It deliberately checks an unreduced
right-hand side `(21,4)` becoming `(17,0)` and verifies the exact computed
coefficient vector, ordinary conclusion, and hypotheses.

The Flyspeck-facing wrapper selects `Linear_function.lin_f`, converts
Flyspeck's base-200 numeral representation by theorem-producing conversion,
and renders the canonical result back through
`Arith_int.my_mk_realintconst`. Its first real-interface smoke run exposed a
bounded wrapper defect: `DEPTH_CONV (TRY_CONV NUM_TO_NUMERAL_CONV)` repeatedly
succeeded without changing nonmatching subterms. Replacing it with Flyspeck's
own `DEPTH_CONV NUM_TO_NUMERAL_CONV` pattern removed that non-progress loop.

The complete fresh-state wrapper test now passes. It covers normalized and raw
checker-shaped inputs, requires raw normalization as an assumption-free kernel
equality, checks the exact accumulator and public conclusion, preserves the
complete raw hypothesis set, and leaves the global axiom state unchanged. The
real post-action-181 integration will inject the already-loaded
`Prove_lp.lin_f_conv`; a stripped fresh state cannot rebuild the full normalized
`Prove_lp` module because it lacks part of that action's predecessor context.
The independent old/new oracle comparison therefore remains a real-checkpoint
gate rather than being replaced by a duplicate parser in the wrapper.

That wrapper is committed as `365f0b3` on
`codex/cv-lp-linear-combination-v1`. Both the complete generic regression and
the fresh Flyspeck-interface regression pass on those exact bytes.

Three attempts to perform the independent real-checkpoint old/new comparison
have not yet produced comparison evidence:

1. attempt 1 was resource-rejected during source action 165 when it overlapped
   another 18-GiB checkpoint restore;
2. attempt 2 completed actions 165--183, decoded the real certificate, and
   initialized all 301 inequalities, but the prototype `#use` requests failed
   because its replacement search path omitted `Filename.currentDir`; the REPL
   continued after the errors and printed an ineligible false pass marker; and
3. attempt 3 corrected that source resolution and made the terminal calls and
   pass marker one dependent phrase, but its otherwise isolated checkpoint
   restore was stopped after 125,054 new swap-out pages beyond the guard
   baseline (about 488 MiB). Its proof log remained empty.

Attempts 1--3 and all partial states are retained as rejected DEVELOPMENT
evidence and are not reused. No inclusive old/new result or speedup is claimed.
With only about 1 GiB of host swap currently free, another 18-GiB restore is
deferred while the three useful long-lived verification processes continue.
The corrected attempt-3 harness remains the exact next LP experiment once host
pressure is safe.

## Nonlinear reconstruction lane

The largest known reconstruction tree remains the target: 167 serialized
leaves, 166 bisects, 324 facets, 491 subdomains, and depth 25. The initial
largest-case and first-leaf processes were independently terminated by their
resource guards at 2026-09-19 16:53:50 UTC after a sudden system swap-out
event exceeded the 256-MiB limit. Both had zero-byte buffered proof logs and
produced no result evidence; their partial states are rejected and were not
reused.

A consolidated replacement was then launched from another fresh clean
checkpoint at
`/project/flyspeck-candle-runs/cv-profile-nonlinear-consolidated-v2`. It was
also resource-rejected during restore when it overlapped LP attempt 1; the log
remained empty and the partial state is not reused. A fresh consolidated
attempt remains queued rather than consuming the host's remaining swap.

Low-memory development has nevertheless removed a source-authority and runtime
compatibility unknown. Commits `379d3f8`, `b204336`, and `fda3b9c` on the
isolated `codex/cv-nonlinear-verifier-v1` branch add a mechanically generated
authenticated closure and parser gate for
`formal_ineqs/verifier/m_verifier_main.hl`. It reuses the direct manifest's
loader scanner and lexical resolver and finds exactly 90 source nodes and 193
literal source edges, with no missing, dynamic, or ambiguous dependency. The
closure contains 28 Candle/HOL nodes and 62 Flyspeck `formal_ineqs` nodes; 34
are already in the direct 400-source manifest and exactly 56 Flyspeck nodes
require a bounded identity extension. Each extension entry is authorized by
repository identity, logical relative path, MD5, and SHA-256, never basename or
absolute path alone. Five closure regression tests pass, including exact
published regeneration, complete resolved-edge closure, and an inventory of
404 qualified uses of 34 runtime members with zero unsupported uses.

The same central batch (`fda3b9c`, followed by `768d61e`) implements the exact
runtime subset required by that
inventory without changing Flyspeck source: `Big_int.mult_big_int`,
`Big_int.eq_big_int`, exact floor `Big_int.sqrt_big_int`, order-preserving
`Array.to_list`, qualified and implicitly opened `abs_float`, `Stdlib.ignore`, and the
`Format.std_formatter` callback value. Focused native-OCaml/Candle differential
gates pass for square-root boundaries through `2^128 - 1`, multiplication
beyond signed 64-bit range, equality, negative-input rejection, array order,
binary64 absolute value, discarded results, and formatter callback typing.
These focused Candle tests load only the compatibility slice and finish in
about five seconds each instead of paying for an unrelated full HOL replay.
The closure also records 13 lexical uses of the supported top-level names,
including the implicit `abs_float` use at `trig/exp_eval.hl:345`. The formatter
value does not yet faithfully flush its accumulated diagnostic
tokens to stdout; this is an explicit output limitation, not a bypass of
kernel theorem checking.

An exploratory parser-only survey passed at least the first 34 of those 56
exact sources but exited without a retained receipt. Its result is therefore
not promoted. The official retained run at
`/project/flyspeck-candle-runs/cv-nonlinear-verifier-parser-v1` has separately
passed its first 25 sources on exact commit `768d61e` and continues. It uses the
existing dedicated `caml_parser$run` protocol, performs no inference or
evaluation, and receives no S2/S3 or release credit. The 469,938-byte generated
`multivariate_taylor-compiled.hl` source passed but occupied its parser process
for about 27 minutes; this is a concrete frontend-cost hotspot, not a syntax
failure or proof-execution measurement. The persisted gate therefore uses a
30-minute per-source timeout.

The exact substantial first target no longer needs a new checkpoint restore
merely for discovery. Flyspeck's retained Azure output identifies global case
7,853 / local case 0 of `prep-4680581274 delta issue-cayleyR` as the first
domain returned by the tree. Native `main_verifier.hl` ran the formal checker
at precision 6 with `eps = 1e-10`, took 365.672506 seconds, and emitted the
legacy `ineqm` theorem hash `f7ff5f16bf03b6885aa4d46bb1490001` with no
reported failure. The retained stdout is 71,301 bytes with SHA-256
`824d7c7c514c58ac60ec758417d3ab1cc0fa6d3661412c53616e121701ea6dc2`.
The first facet is mechanically visible in `break_case_log.hl` as
`Iarg_facet ((0,false),0.7637,800,...)`; its public conversion changes the
first upper bound from 6.3504 to the exact rational
`36218753 / 6250000 = 5.79500048` and leaves the other five bounds unchanged.
This historical result pins the target and supplies an oracle only; it is not
Candle proof evidence.

Once the parser gate completes, the next nonlinear experiment is to load the
authenticated formal-verifier closure in a fresh development state, construct
that exact public leaf, and compare the theorem from
`M_verifier_main.verify_ineq` to the serialized theorem used only as an oracle.
This is a proof-producing leaf-checker route, not merely reflective partition
traversal.

## Decision impact and next gates

The evidence changes the LP priority from “is bulk proof production worth
trying?” to “measure the complete public-interface adapter and remaining bound
phases.” The local gain clears the response's 3x continuation threshold on all
three sampled terminals, but broader integration still depends on inclusive
end-to-end results.

Next gates are:

1. after host swap pressure is safe, rerun the corrected complete new
   constraint aggregate against `transform_le_ineq`/`add_step'` on the three
   real `hard_2` terminals, including conclusion, hypotheses, global axioms,
   wall time, and peak RSS;
2. profile and then reflect the variable-bound/cancellation phase if it remains
   material;
3. finish the 56-source parser gate, complete first-leaf extraction, and attempt
   the now source-closed formal nonlinear verifier on that exact leaf in
   another isolated state; and
4. reserve a fresh cumulative replay for a coherent successful integration
   batch rather than restarting the active action-184 run.

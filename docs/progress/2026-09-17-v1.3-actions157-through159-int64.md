# Direct Flyspeck: actions 157--159 and OCaml `Int64`

Status: DEVELOPMENT / NON-RELEASE. This report makes no S1, S2, S3,
qualification, promotion, or release claim.

## Headline Flyspeck progress

- Exact focused clean-predecessor prefix: actions **000--164 = 165/165**.
- Fresh action-zero prefix: actions **000--154 = 155/155**, independently
  reconfirmed by two naturally completed replays.
- Current v59 fresh action-zero replay: actions **000--048 = 49/49**, now
  evaluating action 49 `text_formalization/nonlinear/merge_ineq.hl` without a
  diagnostic.
- Current cumulative boundary: `05-lp_support-through-184`.
- Last repaired failure: action 159,
  `formal_lp/ineqs/constants_approx.hl`, line 242 of the pinned source,
  `Undefined variable: Int64.to_string`.
- Current functional frontier: action 165,
  `formal_lp/hypermap/ineqs/lp_ineqs_quads-compiled.hl`, evaluating with no
  diagnostic at the time of this report.
- Fresh v59 action-zero replay: active from 2026-09-17 10:21:36 UTC and exact
  through action 48 at the time of this report.
- Full direct S2: open.
- Nonlinear plus LP / S3: open.

The focused run is
`/project/flyspeck-candle-runs/v165-focused-post154-lpproc-float-compare-faithful-a193ef7-001`.
It used a fresh copy of the authenticated clean post-action-154 checkpoint and
emitted exact nonce-bound success markers for actions 155--158. In particular,
the unchanged 215,795-byte action 157
`formal_lp/hypermap/ssreflect/list_hypermap_iso-compiled.hl` completed with its
terminal theorem, and action 158 `formal_lp/hypermap/ineqs/lp_ineqs_defs.hl`
completed before the action-159 runtime-API failure. The log SHA-256 is
`e4c1f2a09865af8731fb97c4fb0491bd18464bb40eacd255beb49ed84d206ca6`.

The standalone module expressions in action 157 were accepted unchanged.
Consequently this failure set supplies no evidence for applying the recurring
`let _ =` normalization pattern to that file or for reopening a frontend
grouping investigation there. The central investigation is already represented
by CakeML commits `8dc5e5d96` and `cc36ecadd`: the former lowers OCaml
structure expressions to wildcard `Dlet` declarations, and the latter
preserves nested-module item separators. Their parser fixtures cover mixed
standalone result types and a bare effect between named bindings. Older
per-file `let _ =` normalizations are therefore legacy overlay debt, not an
active frontend diagnosis; they should be retired only through separately
authenticated raw-source replays, not during this functional frontier run.

## Action 159 diagnosis

The normalized `constants_approx.hl` reaches its interval square-root helper
and calls `Int64.to_string (Int64.of_float n)`. Candle had no `Int64` module,
so this is an OCaml runtime compatibility omission rather than a theorem,
tactic, parser, search-order, or Flyspeck source defect.

No `Int64` implementation or patch exists in `fix-top100` or the relevant
Candle branch history. A mechanical scan of the complete pinned Flyspeck tree
finds exactly five used members: `of_float` and `to_string` at the current
boundary, `to_string` in `formal_lp/more_arith/prove_lp.hl`, and `of_int`,
`shift_left`, and `logor` in the later LP binary-certificate reader.

The isolated `codex/v59-ocaml-int64` candidate implements exactly those five
members using CakeML's 64-bit word operations. Signed rendering uses
`Cake.Word64.toIntSigned`; shifts and disjunction remain modulo 2^64. Negative
OCaml integers are converted by word subtraction from zero so the
implementation does not rely on `Cake.Word64.fromInt` outside its proved
natural-number domain. The candidate is commit
`5cf688b6bebe8454f670d4be62d87799582d82b4`.

A shared OCaml/Candle fixture covers truncating positive and negative float
conversion, negative integer conversion, signed minimum, left-shift
wraparound, bitwise disjunction, and signed decimal rendering. The native
OCaml 4.14.1 oracle passes. All 18 normalization tests, all 28 manifest tests,
and the independent 297-root/400-source/43-generated-input manifest fixed-point
check pass. The manifest authority records the exact five-member supported
surface and the generated source-digest program and loader authentication pin
were mechanically refreshed. `candle/ocaml.ml` has SHA-256
`85ccdd99ab56f738d511b9fb2c39073544ea23357e7ae3aec74a10683be020cc`.

The full standalone Candle/HOL fixture boot reached its 300-second development
timeout without output while host memory was constrained by protected long
runs; it produced no semantic diagnostic. The higher-value fresh checkpoint
replay tested the exact v59 definitions against real actions 155 onward as
`/project/flyspeck-candle-runs/v167-focused-post154-int64-faithful-5cf688b-001`.
It emitted exact success markers for actions 155--159, including normalized
action 159 with output SHA-256
`ccc3a817e76c8cd9612696db55e8d5cc130d3c957a17ff8569535071bed1b5a5`.
This establishes the real functional gate for the bounded runtime module and
raises focused direct progress to actions **000--159 = 160/160**. The same
clean evaluator subsequently completed unchanged action 160
`lp_ineqs_proofs-compiled.hl` and action 161 `RNSYJXM-compiled.hl`, raising
focused direct progress to actions **000--161 = 162/162**. It then completed
unchanged action 162 `lp_main_estimate-compiled.hl` and action 163
`delta_ineq.hl`, raising focused direct progress to actions **000--163 =
164/164**. It then completed unchanged action 164
`lp_ineqs_proofs2-compiled.hl`, with output SHA-256
`de4d20ee4ff45f792326ed67fa62e03950b581b06d8691a5b5e7c2b1e1806c1e`,
raising focused direct progress to actions **000--164 = 165/165**. It continues
into action 165 to collect the next genuine failure set; its final log hash
will be recorded only after the run terminates.

The exact v59 host plan is
`/project/flyspeck-candle-runs/v168-stratum-plan-int64-5cf688b-dev-001`,
SHA-256
`4f701c7b72425258e288633ad18a6bd82a2aecd376c158c24cc487f152e607eb`.
Its 297 action records are byte-for-byte identical to the v58 plan. The
mandatory fresh action-zero boundary replay was launched from its
hash-validated preparation at 2026-09-17 10:21:36 UTC, concurrently with the
18 GiB focused evaluator after a resource check showed 171 GiB available. It
is running at
`/project/flyspeck-candle-runs/v169-dev-direct-boundary05-int64-5cf688b-heap16-prepared-001`.
Its receipt binds Candle `5cf688b`, CakeML `cc36ecadd`, 185 cumulative actions,
51 normalizations, and the unchanged checked non-promotable development
runtime. The oracle and both Candle evaluators together use about 75 GiB and
three CPUs, within the owner-approved limits. This replay remains DEVELOPMENT /
NON-RELEASE and supplies no completed cumulative credit until its exact action
markers are observed.

## Fresh cumulative confirmation

Two independent action-zero replays have now naturally completed through exact
action 154 and reached only the expected computed-`needs` parser failure in
their superseded action-155 overlays:

- `v127-dev-direct-boundary05-f958692-cc36ecadd-heap16-attempt-001` ran for
  11:12:37, used at most 17,118,976 KiB RSS, and has log SHA-256
  `b3e983d63e85fdfb0874f153417fa3274a76434635ae5a126b9e937731d493ee`.
- `v133-dev-direct-boundary05-ca3e129-cc36ecadd-heap16-prepared-001` ran for
  11:10:37, used at most 17,122,560 KiB RSS, and has log SHA-256
  `c5e12f0023c8c4c71f8b13b85f4255da2deb5576a6a526297881c8ad2bb7ee81`.

Neither run was killed or interrupted. They establish the fresh cumulative
metric only through action 154; the v59 branch still requires its own fresh
action-zero confirmation now that the focused candidate has cleared action
159.

## Preemptive action-181 `Big_int`/`Num` bridge

A bounded static pass over only the active actions 164--184 found one further
source-backed runtime gap while action 164 was evaluating. Action 181,
`formal_lp/more_arith/prove_lp.hl`, converts an `Int64` to a canonical signed
decimal string at line 73 with `Big_int.big_int_of_string`, then calls
`Num.num_of_big_int` at line 74. Neither member existed in the v59 runtime.
No corresponding implementation or repair exists on `fix-top100` or in the
inspected Candle history.

The correction is isolated from both live v59 evaluators in worktree
`/project/worktrees/candle-big-int-v60`, branch
`codex/v60-big-int-bridge`, commit
`c79ae7e9cd39d3136f973067e5dfe38cdec5b7ad`. CakeML integers are already
arbitrary precision, so `Big_int.big_int_of_string` parses directly to that
representation and `Num.num_of_big_int` wraps it as `Num.Int`; there is no
machine-word narrowing, FFI, theorem change, tactic change, or Flyspeck-source
normalization.

A shared native/Candle fixture passes canonical positive and negative values,
the signed 64-bit endpoints, and values beyond 64-bit range. It also exercises
the exact selected composition from `Int64.to_string` through `Big_int` into
`Num`. All 29 manifest tests, all 18 normalization tests, and all 10 stratum
planner tests pass. The independent manifest check reaches the unchanged fixed
point of 297 actions, 400 source nodes, and 43 generated inputs, with exactly
one selected
`Big_int.big_int_of_string` use and one selected `Num.num_of_big_int` use and
no unsupported compatibility member. The resulting `candle/nums.ml` SHA-256
is `7cabf002e6aac6908c813c1424095658d3a6a01ec2ebd900303828a154cfe8f1`;
the generated source-digest program MD5 and loader pin both equal
`6e74075cdfb23f5c9fb584b45d0d6402`.

This is a prepared compatibility candidate, not action-181 pass evidence. The
unchanged v59 focused run remains authoritative for the next actual action
result. The v60 host plan is
`/project/flyspeck-candle-runs/v170-stratum-plan-big-int-c79ae7e-dev-001`,
SHA-256
`cd5806f14e84dc0b6939bcd97262db919bc519951762897283059a950f7c69c6`.
Its action array is byte-for-byte identical to the v59 plan and its cumulative
LP prefix retains SHA-256
`04c31ad4d9addf320e2acc7f2d05289b140415f7be9e2a3f9532e4cd0fc5eb86`.

An exact fresh action-zero v60 replay was launched at 2026-09-17 11:00:41 UTC
from
`/project/flyspeck-candle-runs/v171-dev-direct-boundary05-big-int-c79ae7e-heap16-prepared-001`.
Its receipt binds nonce `19713359fde4eccc7cf0cd090bba351b`, Candle
`c79ae7e`, CakeML `cc36ecadd`, Flyspeck `1ce0353`, the same 185 actions,
51 normalizations, and 47 aliases. The v60 replay remains DEVELOPMENT /
NON-RELEASE and must reach action 181 before the bridge can raise direct
progress.

To shorten that feedback loop, a separate focused v60 replay was launched from
a fresh byte-identical copy of the clean post-action-154 checkpoint as
`/project/flyspeck-candle-runs/v173-focused-post154-big-int-faithful-c79ae7e-001`.
The checkpoint SHA-256 is
`6cee39c4b566bbbe75a41ae54e0c0216e935e1ee6c102f18b3a6dce3135aad33`
and its input SHA-256 is
`b5f03b1b3cb783f4ae4083d9bdc29cf2b8dc3e3bfc0a9552917df543417079c1`.
The input preserves the restored complete `Num` value/operator surface,
injects the exact committed v60 bridge, and replays actions 155--184. It has
already emitted the exact action-155 marker, confirming the restored state and
candidate prelude were accepted. This is focused diagnostic evidence only and
does not replace the fresh action-zero replay. The four development evaluators
plus the protected native oracle use five project CPUs and approximately
110 GiB RSS, within the owner-approved limits.

## MESON ordering correction retained

The GRUTOTI incident remains closed as a name-resolution defect. Transitive
module opens exposed `Trigonometry2.SET_TAC`/`SET_RULE` where native HOL Light
used its standard bindings. Restoring the intended bindings made all 2,364
MESON progress records match native output byte-for-byte. Comparator ordering,
pointer equality, and ambient-context size are not active explanations for
that incident.

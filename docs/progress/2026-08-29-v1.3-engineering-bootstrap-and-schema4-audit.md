# v1.3 engineering bootstrap and schema-4 audit — 2026-08-29

This checkpoint records two engineering results.  Neither result promotes a
Great 100 target, proves S1, or supplies direct-source S2/S3 evidence.

## Non-canonical CakeML bootstrap timing

The exploratory x64 bootstrap completed successfully from these exact heads:

- CakeML: `36e2245f42d4759063615c97fec51865798ca894`;
- HOL: `a390cbabd3a4521bab4ee20281e3e42933a8a3ae`.

The single-core `Holmake -j1 cake.S` invocation exited zero after 8:44:31 and
reported maximum RSS 64,484,760 KiB, no swaps, and 100% of one CPU.  The log is
`flyspeck-candle-runs/v13-dopen-bootstrap-36e2245f4.log`, 685,849 bytes, SHA-256
`a4fdb29045b2dd9f749ca9075968872a569802f7d7af9a7d173fb146a7b34854`.
Its principal outputs are:

- `cake.S`: 41,560,891 bytes, SHA-256
  `fcc27da98ffc2067c0ff7e7956f5d9d3dd38e096735430615e7d94115536392f`;
- `config_enc_str.txt`: 7,138,602 bytes, SHA-256
  `ba6b9e0e9f1599d10e8e921905919ae71dbae70e99be3d318794621b3cbd70eb`.

This run is deliberately non-canonical and non-promotable.  It used raw
`Holmake`, predates the final Candle integration head, and was not launched by
the provenance-capturing bootstrap controller.  It is retained only as an
engineering feasibility and resource measurement.  A fresh controller-owned
bootstrap is still required after the integration head passes independent
review; the observed measurement gives an initial wall-time estimate of about
nine hours and a memory estimate of about 65 GiB for that stage.

## Independent schema-4 criticism and repairs

Independent review of Candle commit
`5f04864` found three promotion blockers:

1. a theorem with a positive hypothesis count could be accepted;
2. the runner trusted a pre-exit parse even if the child appended fingerprint
   records while exiting; and
3. hash-consistent approval attachments were retained but not mechanically
   replayed.

The integration branch now reaches Candle commit
`27617a90dd2aa9662ab549d26b26847139c402dc`.  It requires every promoted
theorem to have zero hypotheses and the canonical empty-list serialization
hash; reparses the final, closed transcript and requires it to equal the live
prefix observation; and replays each exact schema-v6 candidate, plan, request,
transcript, and source contract while cross-binding the target, selected source
hashes, reference head, process nonce, serializer, policy, and derived identity.
It also rejects every unrecognized or conflicting suite, process, linked, and
fingerprint protocol record, including records appended after completion, and
requires the order `suite < START < PASS < linked < COMPLETE`.

The combined noncompiled Candle suite passes 199/199 tests.  The Great 100
manifest regenerates exactly 65 targets, 66 selected source files, and 97
theorem requests.  The pinned direct Flyspeck closure independently regenerates
297 roots, 400 source nodes, and 43 generated inputs.

The project finalizer reaches project commit `2a22e68`.  It has the same
closed-theorem, exact staged attachment-replay, closed protocol-namespace, and
PASS-ordering guards and passes all 54 project static tests.  Independent
rereview explicitly passed both heads after rerunning a hash-refreshed hostile
transcript with an alternate linked digest and same-nonce `FAIL` appended after
completion.  The committed Great 100 approval remains intentionally unapproved
and therefore promotes 0/65 targets.  The green rereview authorizes the fresh
canonical bootstrap; it does not authorize Great 100 promotion or claim S1.

## Canonical launch and final host plan

The first controller-owned launch at Candle `b9ef8a2` stopped before Holmake.
The controller had created its log before the transition validator rechecked a
preflight rule that requires all output receipts to remain absent, so that
validator rejected its own newly created log.  The read-only failed-attempt
artifacts are retained as:

- preflight SHA-256
  `6f0db3fc6e3bf09d9d6f6983f3eb9014d5c8a58c11d014469e90d797f6b013ff`;
- log SHA-256
  `dd2aa4ce2f8590d5e97ab130a1961982364cbc23c793bde5133332e9099c38e6`.

Commit `27617a9` makes the lifecycle consistent: the locked controller
publishes the immutable preflight, revalidates and archives/removes all
preimages while log and final receipt are absent, and only then exclusively
creates and inode-checks the log before launching Holmake.  Its 199-test Candle
suite and a separate 43-test provenance review pass.  A new canonical launch
from this head passed that transition and started the single-core build with
fresh `v13-dopen-bootstrap-canonical-27617a9` receipt names.

The final-head host materialization is also complete:

- normalized overlay: 18 entries, receipt SHA-256
  `852ccd8b086377ebdb7654d1709c6a1916a865f693da12aa749bcfb30a0c71c4`;
- generated LP archive: 119,855,733 bytes, receipt SHA-256
  `6979641bc74cbaeafbcce2e5f90b1086601651b4d58dc3840ccec2a252deb9a6`;
- stratum plan: 297 actions, eight cumulative boundaries and two diagnostic
  cutpoints, SHA-256
  `bb938af212fe90726e5e5a0df69e6cd7ea25a95c7f7d4b82ef583f06e1645bb1`.

The plan directory is an exact 0555/0444 thirteen-file publication rooted at
both Candle integration base and materialization head `27617a9`.  It remains a
host-only schedule and is not Candle execution or S2/S3 evidence.

## Resource boundary

The user-authorized 120 GiB heavy-stage value remains a ceiling, not a target.
At this checkpoint the host had about 188 GiB available while the restartable
PFT oracle used one CPU and about 24 GiB RSS.  The final canonical bootstrap is
expected to remain near the measured 65 GiB; later stages may receive a larger
explicit limit only after a live availability check and only when measurement
shows that it is needed.

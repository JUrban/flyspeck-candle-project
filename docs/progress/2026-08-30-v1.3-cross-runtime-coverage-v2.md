# Cross-runtime coverage-v2 and role-correct comparison (2026-08-30)

## Status

The direct protocol now separates evidence that can honestly match across
compiled Candle and pristine OCaml/HOL Light from Candle-specific detailed
execution evidence.  This is protocol and fixture implementation only.  It
does not authenticate a real full candidate, create a pristine reference
candidate, approve S2/S3, or authorize a release.

Project commits:

- `cc81507`: initial coverage-v2 schema and compiled schema-6 adapter;
- `3c24450`: exact target-alias and action-closure P1 repairs;
- `1aa8565`: all three real leading-parent alias families in the fixture;
- `60324a5`: bind both coverage projections into the authenticated capture;
- `8dadedc`: role-specific descriptors and comparison-v2 boundary.

## Evidence split

Coverage-v1 remains the exact compiled-runtime artifact.  It retains action
`load`/`skip-ledger` outcomes, loader request nesting, cache state, Candle
control keys, selected normalization execution records, raw certificate event
order, and nonce-free event digests.  It is required in compiled descriptors
and is never accepted in pristine-reference descriptors.

Coverage-v2 contains only common facts independently reproducible from the
shared authenticated plan and each runtime's retained native observations:

- final boundary and 297 ordered action identities with successful completion;
- the exact 400-node original-source inventory;
- explicitly labelled Candle plan execution selections, without claiming that
  pristine OCaml executed the normalization overlay;
- every action root plus authenticated selected nested logical sources;
- all 43 generated inputs;
- structural, LP, nonlinear, final-premise, and final-implication completion;
- 39 canonical certificate identities, each with one successful
  deserialization; and
- categorical `pft_used = false`.

It omits Candle physical events, cache outcomes, controls, raw LP order, and
per-event identifiers/digests.

## Exact alias and closure repair

The first hostile review found that a generic safe-relative validator rejected
46 real plan targets with one leading `..`, and that the synthetic fixture did
not require action roots in the selected closure.  The repaired resolver treats
targets as lexical paths relative to `text_formalization`, permits at most one
leading parent, rejects every escape or embedded parent, and requires the
resolved identity to equal the authenticated `flyspeck:` selected source.

The resolver passes all 297 current plan actions: 39 `formal_lp`, six
`jHOLLight`, one `formal_graph`, and 251 ordinary text-root targets.  The
coverage validator now requires every action selected source to occur in the
common logical closure.  The compiled adapter constructs the closure as the
union of validated completed action roots and the non-control,
non-derivation, non-setup-harness nested records already authenticated by
coverage-v1.

Independent hostile rereview of `3c24450` reported PASS with no P0/P1.

## Pristine-boundary harness correction

The later reference-producer design audit found one remaining compiled-only
identity in the common closure:
`candle:candle/flyspeck_source_integrity.ml`.  It is an authenticated member of
the exact 400-node source inventory, but it is a Candle setup harness and is
not an original Flyspeck action or a source executed by a pristine
`strictbuild.hl` run.  Requiring it in byte-identical coverage-v2 would force a
reference adapter to manufacture a Candle control observation.

The coverage-v2 adapter therefore keeps this source in the 400-node inventory
while excluding it, the derivation-only full-build driver, and both generated
Candle controls from the runtime-neutral selected execution closure.  The
validator also rejects all four exact identities if they are inserted as an
action or closure observation.  Fixture coverage retains the setup harness in
the inventory and hostile-tests a copied harness, copied derivation input,
copied generated control, and an action relabelled to the harness.  A real
297-action/400-source plan probe confirms the setup harness occurs once in the
inventory and never as an action selected source.

This is a correction to the common boundary only.  Coverage-v1 still retains
the Candle setup/control evidence appropriate to compiled candidates.

## Role-correct authority

Authenticated schema-6 capture-v2 rederives and binds both coverage-v1 and
coverage-v2 from the same retained receipt and plan.

Comparison descriptor-v2 has exact disjoint roles:

- compiled: semantic projection, common coverage-v2, detailed coverage-v1;
- pristine reference: semantic projection, common coverage-v2, and a distinct
  authenticated native execution-closure content record.

Comparison-v2 requires exact three-way semantic and coverage-v2 equality, one
shared plan, three distinct candidates/nonces, two distinct reference closure
records, exact producer/validator authorities, and the status
`three-way-semantic-and-cross-runtime-coverage-match-unapproved`.  Detailed
coverage-v1 is bound only to the compiled descriptor.  Every promotion,
approval, S2/S3, release, and PFT field remains false.

Inline compiled descriptor production still occurs only after final raw
receipt/log/snapshot/tree/link/plan/repository/source revalidation, named
directory checks, and the last held-lock check.  There is no detached
descriptor assembler or public minting helper.

Independent hostile review of `8dadedc` reported PASS with no P0/P1.

## Checks

- direct protocol suite: 30/30 pass;
- direct-result consumer/control-flow suite: 18/18 pass;
- Python compilation: pass;
- real 297-target and 400-source plan probes: pass;
- `git diff --check`: pass.

Hostile cases cover target escape/mismatch, missing/reordered actions, source
inventory omission, action-root closure omission, duplicate/reordered closure,
normalization relabelling, copied Candle controls, missing/reordered/duplicate
LP completion, PFT contamination, numeric type confusion, wrong-role evidence,
descriptor/report projection mismatch, authority/nonce/plan/candidate splicing,
and reference-closure reuse.

## Next boundary

The opaque reference execution-closure record is not self-authenticating.  A
new pristine-reference producer/validator must define its exact raw schema,
authenticate original-source execution with serialization disabled, retain raw
native loader and LP order, independently derive semantic and coverage-v2, and
only then emit a reference descriptor.  The production comparator must replay
that exact validator over both retained raw bundles before accepting their
descriptors.

The design audit established an honest minimum observation route without
editing Flyspeck proof sources: use the original `Build.build_sequence_full`
297-action list; require `FLYSPECK_SERIALIZATION` to be absent as an environment
key; wrap `Lp_certificate.read_lp_certificates` only after its original module
loads and emit each of the 39 records only after the original read succeeds;
then load the exact final target and common serializer.  Stock HOL Light
exposes a successful `loaded_files` basename/MD5 ledger, not exact nested
request parentage or cache outcomes.  The reference execution closure must say
that explicitly and fail closed on unsupported identities rather than
relabelling it as Candle's loader-owned physical trace.

# Float-performance provenance closure — 2026-08-28

This interim assurance report records the host-side performance-gate closure
for the v1.3 direct-source route. It does not report a performance observation,
a compiled Candle pass, S2, or S3. The gate remains explicitly marked
`performance_accepted = false`; execution awaits the newly bootstrapped and
linked verified CakeML binary and a sufficiently quiet measurement window.

## Final implementation

The clean Candle integration branch `codex/flyspeck-v13-candle-integration`
is at `a9ba565f424b66aa4b00a7a081fde1203feabd95`. The provenance closure was
developed in four reviewed stages:

- `5792f2ef570ba42574128f53b509d52a1f530cba` authenticates the direct
  `/usr/bin/python3 -I -S` controller, source/runtime inputs, linked runtime,
  and retained evidence;
- `225c56db5f425a4931b79c0d178ba305d970c597` adds fail-closed RSS cadence and
  duration coverage, persistent JSON revalidation, and explicit host-program
  trust assumptions;
- `b3c5885de15b9d44355feaae6b76355ccc394306` rehashes each exact scenario
  transcript and binds it to the receipt inventory; and
- `a9ba565f424b66aa4b00a7a081fde1203feabd95` requires the final one-shot
  inventory to equal the complete closed set of every report-bound retained
  file, including generated inputs and all controller, Python, pexpect, linked
  runtime, provenance, and source archives.

The sampler requires positive root RSS, a tree value at least as large as the
root, root presence in every snapshot, clean sampler-thread completion, a
maximum inter-sample gap of `max(250 ms, 4 * requested interval)`, and
duration-derived minimum coverage. The report retains the window, maximum-gap,
and minimum-count witnesses. A missing root, sampler exception, starvation, or
invalid sample fails the attempt.

Successful receipts reread and hash-bind the running-attempt record, all three
scenario journals, all three canonical transcripts, and the persisted report.
The inventory rejects noncanonical or duplicate bindings, missing/changed/
unexpected files, symlinks, and non-ordinary entries before receipt creation.
Failure receipts retain the feasible postflight and evidence inventory without
upgrading a failed observation.

The recorded trust boundary assumes no hostile same-UID transient mutation and
states the host-program roles used by Git/ELF/patch validation and by the
launcher. The controller's resolved Python image and base ELF closure are
archived; the separate launcher validation invocation and the listed host
program executions are not independently execution-bound. The Python standard
library, initial top-level compilation, kernel, vDSO, and frozen/built-in
modules remain outside a hermetic claim.

## Validation and independent review

At the final clean commit, the following passed independently:

- 27 focused float-performance tests under `/usr/bin/python3 -I -S`;
- 60 compatibility tests and 136 broader Candle tests under isolated Python;
- exact commit-blob binding for all four executed controller sources;
- Python compilation and `git diff --check`; and
- the independent OCaml completeness oracle: 15,775 occurrences, 1,741 exact
  decimal spellings, and nine runtime files.

The adversarial rereview reproduced and then closed four successive classes of
false-green: starved RSS coverage, mutable attempt/journal/report records,
mutable transcripts, and partially cross-bound retained inventories. Its final
synthetic full-shape audit collected every representative retained path and
rejected configuration/source/archive mutations, path escapes, aliases,
duplicates, extra files, symlinks, and FIFOs. It found no remaining concrete
completed-receipt inconsistency within the documented trust boundary.

No compiled performance workload was run while the verified bootstrap and PFT
oracle were active. This avoids treating contention-biased timings as a release
baseline.

## Commit-bound host materialization

The final host inputs were regenerated at the reviewed Candle commit and
Flyspeck `1ce0353008eba83d3c76ae9a25c3c242e4802d53`:

- normalization overlay
  `flyspeck-candle-runs/v13-normalized-overlay-a9ba565-ac925270`, with receipt
  SHA-256 `e234c83d12d1b9e6525ed6e92de4244cbe2158634032ed113212d1e318a221d1`;
- generated LP input
  `flyspeck-candle-runs/v13-generated-lp-a9ba565-0ca1b5b6`, with receipt
  SHA-256 `77d099cb5035c20645f83f5ab5e19abd77b28eb2b32292e8f3314d4867366841`;
  and
- 297-action/eight-boundary plan
  `flyspeck-candle-runs/v13-stratum-plan-a9ba565`, with plan SHA-256
  `c2ccfdaa536386733d3e0406098e5e77a1e166d46d5819210723bd917e932aab`,
  host-materialization SHA-256
  `228a72783d8b9835f4081fd9bc688b5675e03e85c5997e5d723c1c7bc0bfe40b`,
  and schedule SHA-256
  `093e37c12dad123e63b5cef38c3463085d0d3d7d88daf49ef3f39b67c69bf9fb`.

These are authenticated host artifacts only. The next promotion step is to
finish and record the verified CakeML bootstrap, link this exact Candle commit,
run the compiled compatibility gates, and only then execute the bounded
performance observation and cumulative direct-source strata.

# Pristine semantic state-machine rereview (2026-08-30)

## Exact reviewed result

Independent review of the state-machine integration at exact clean commit
`a98cafcb43f5636ceb5e3ecbfdf8069a9626549b` found one P1 content-record
omission.  A self-consistent mutation could replace both copies of the
necessarily empty stderr SHA-256 with a false 64-hex value and recompute the
dependent transcript content records.  The final value and canonical-byte
source-rederivation validators accepted that impossible content record because
they required zero bytes but not `SHA256(empty)`.

Commit `2a6b9930b896fec1e0ab076e065447aeddd113da` requires the exact empty-byte
digest at both the raw transcript and final source-rederivation boundaries.
Its regression starts from a real parser result, changes the top-level and
nested stderr digests, recomputes the native-closure and semantic-completion
transcript bindings, and requires both public final validators to reject.

Rereview of that repair found no remaining P0/P1 issue and confirmed that the
original self-consistent mutation is closed.  It found one adjacent P2: the
standalone raw transcript still allowed a zero-byte stdout content record,
although a completed 297-action, 39-LP session necessarily contains marker
output.  The final source-rederivation already rejected this case, so the
defect did not reach the final raw envelope.

Commit `20ec65d118eef6547238faae228cff7a1956fbd5` removes the empty-stdout
allowance and tests both a false zero-byte digest and the correct empty-byte
digest.  Final independent rereview of that exact clean commit returned PASS
with no P0, P1, or P2 findings.  It reproduced both stdout cases through the
transcript, native-closure, and semantic-completion validators and reproduced
the earlier stderr attack through both final public validators; all reject.

The reviewed source SHA-256 values are:

- `scripts/pristine_direct_reference_protocol.py`:
  `3c8fd6b5cfc2240a57ff45e3d1aa1592e3bb73f713101c975fa1dc4cd6f1fc84`;
- `scripts/test-parse-pristine-direct-reference-output.py`:
  `1a99a88b08127e206d4ae4c118bc7e2967a5427ed437c1fdadb7fa3458397fe1`.

## Verification and claim boundary

The five-suite authority/consumer regression passes 97 of 97, the direct
compatibility suite passes 3 of 3, focused protocol/parser review tests pass
42 of 42, Python byte compilation passes, and `git diff --check` passes.

This remains passive, unauthenticated raw-parser evidence.  It creates no
collector, request generator, capture envelope, coverage object, candidate,
approval, promotion, PFT, S2, S3, or release evidence.

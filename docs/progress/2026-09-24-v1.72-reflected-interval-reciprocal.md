# v1.72: Guarded reflected interval reciprocal

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T18:49:24Z`, the protected cumulative action-184 run had
  freshly passed `hard_7.dat` terminal **7,891 / 9,080** and remained healthy
  at one computation core with approximately 17.2 GiB RSS.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## Interval reciprocal and checked domain

The first post-checkpoint analytic interval primitive is now complete. It
adds an exact endpoint reciprocal, a computed interval-domain test, and a
guarded real-enclosure theorem:

```text
|- !i x.
     candle_q_interval_not_zero i /\
     candle_q_interval_contains i x
     ==> candle_q_interval_contains (candle_q_interval_inv i) (inv x)
```

`candle_q_interval_not_zero` accepts exactly the intervals whose lower
endpoint is strictly positive or whose upper endpoint is strictly negative.
The reciprocal reverses the endpoints. Its proof covers both positive and
negative intervals and connects exact rational nonzeroness to real
nonzeroness. An interval crossing zero is rejected before its computed result
can authorize a real theorem.

The reflected operations have representation theorems for both the domain
decision and the reciprocal result:

```text
|- !i.
     candle_cv_q_interval_not_zero (candle_cv_q_interval i) =
     Cexp_num (if candle_q_interval_not_zero i then 1 else 0)

|- !i.
     candle_cv_q_interval_inv (candle_cv_q_interval i) =
     candle_cv_q_interval (candle_q_interval_inv i)
```

This closes the interval-level reciprocal primitive and its essential domain
guard. It does not yet add an `inv` node to the generic analytic expression
language or its jet evaluator, so it is not yet an end-to-end action-296
formula check.

## Fresh checkpoint-backed validation

The production form was loaded from a fresh disposable restore of the stable
reflected-support checkpoint. The restored process acknowledged nonce
`73ecf47eb3f8031ad357427ba2cc10b6710d6bcf0b4c8c601ae4ba56f434c742`
at log line 1 before loading the three post-checkpoint fragments. It then
reported:

```text
CANDLE_CV_EXACT_INTERVAL_INV_RESULT domains=3 accepted=2 rejected=1 sound=2
CANDLE_CV_EXACT_INTERVAL_INV_OK
```

The computed cases cover `[2,4]`, `[-4,-2]`, and the rejected zero-crossing
interval `[-1,2]`. The positive and negative cases were separately handed
through the generic enclosure theorem at interior points. All computed and
semantic results are assumption-free, and the global axiom set before and
after the test is identical.

The complete restore, source proof, computation, and theorem-handoff test took
**18.59 seconds** wall time and **2,252,544 KiB** peak RSS. The generated
result manifest revalidated the fragment manifest, generated input, and full
log. This confirms that the support-only checkpoint remains at the right
editing boundary: stable library and expression infrastructure stays frozen,
while analytic primitives and changing adapters can be tested as cheap
post-checkpoint overlays.

Evidence root:

```text
/project/flyspeck-candle-runs/cv-exact-interval-inv-v18-run-001
```

Evidence identities:

- rational reciprocal source SHA-256:
  `18ba691e7fc002f72ac3eb252774280bd16cb2a72eb19b2b89e0b468a674aa8e`;
- interval reciprocal source SHA-256:
  `4c074f6e1c37008e19dd9507cf1a2efe07b0c33514bd886b21467ad5384e1b40`;
- focused test SHA-256:
  `1a0e0cd9049a330bd6e47f40ba68ac26cc7c819a2c1ff4fe428e458e14453b30`;
- wrapper SHA-256:
  `33f40a853fad9a19f9b3fcadb854275f18bc6cea8938f8a4333fa1e324e7d52b`;
- fragment-set SHA-256:
  `f36bc8d3328ceee14cd5c0b0467a42d9d24c869eab986ab8e9de2d942f32b08a`;
- generated-input SHA-256:
  `e2a0f2d5f531a849be798cef50e02690704c91fefe4b6109c4d35d65c4e83895`;
- result-log SHA-256:
  `deffb1a6ae2cc86f3d772ddb04426c5d03c645e3501351ae185619962be6c70e`;
- fresh-input receipt SHA-256:
  `41da9cb67319e4a500f25a2dbfb1f7b7bc524c0d504ee86d3b5df31ba445fc30`.

## Source record and next boundary

Candle commit `4a067c7de8a798ee10f4ecc12eac31d68421e930` records the
exact nonzero bridge, guarded interval reciprocal, reflected evaluator, and
focused regression.

The next coherent stage is the generic analytic AST/reifier connection for
reciprocal, exercised first on a small composed term from the same disposable
checkpoint. The corresponding square-root domain and enclosure primitive can
then use the same guarded pattern. Arctangent and a checked interval enclosure
of `pi` remain required for the full authenticated action-296 source. The
checkpoint boundary should advance only after a substantial composed analytic
stage is stable and reusable.

# Final S1 bootstrap and clean-shutdown correction — 2026-09-07

## Outcome

The first final-head canonical bootstrap and schema-6 link completed, but the
first promotable Great-100 process exposed one shared host-runner shutdown
defect after its theorem and post-state fingerprints had completed.  The
runner sent the source phrase `exit 0;;`; the final Candle environment has no
value named `exit`, so the transcript acquired a top-level error and the REPL
waited indefinitely for more input.  That attempt was stopped immediately and
is permanently nonpromotable.

Candle commit `4c0ab6380bb24badfaca0be0fb5800e89df22fa2` replaces the invalid phrase
with PTY EOF.  The final linked runtime was probed directly before the edit:
after its prompt, EOF produced ordinary process exit status zero in 0.33
seconds, with no signal or error text.  Unit coverage now checks the EOF call,
a live PTY child that exits only on EOF, and rejection of a nonzero status.
The complete Candle suite passes 362/362 in 393.688 seconds.

## Superseded canonical artifacts

The successful but now superseded bootstrap is retained at:

`/project/flyspeck-candle-runs/cakeml-canonical-bootstrap-8a8926906-cba4222-attempt-001`

It recorded all 18 Holmake targets, exit status zero, 7:43:03 wall time,
62,516,748 KiB maximum RSS, and no swaps.  Its immutable bootstrap provenance
record has SHA-256
`826d8ba4a8714145e72fa3f56fbd2a77742870f5996046b3a82865cb2bf0fa8f`.
Independent `check-bootstrap` replay passed.  The corresponding linked record
has SHA-256
`7486a9eb589b4ef8534b06390e34cb5c63fa21f862ceea1328a86f78dc2ee79b`,
and independent `check-linked` replay passed.

Those records bind Candle commit
`cba422200896b46196dffc86568cd95bcf3313d9`.  They cannot authenticate the
shutdown-fixed head and will not be used for S1.

## Failed Great-100 attempt

The retained attempt root is:

`/project/flyspeck-candle-runs/great100-s1-cba4222-schema6-run-001`

Target `100/arithmetic_geometric_mean` reached its nonce-bound COMPLETE marker
after producing the requested V3 theorem and streamed post-state records.  The
subsequent invalid `exit 0;;` phrase produced `ERROR: Undefined variable: exit`
and left the process at the prompt.  Schema 4 forbids conflicting top-level
errors even after COMPLETE and requires an ordinary zero process exit, so the
attempt was already irrecoverably invalid.  The exact Candle child and runner
were terminated; both were confirmed absent.  No schema-4 report was emitted,
and no part of this attempt advances S1.

## Required consequence

Because canonical bootstrap and linked provenance bind the complete clean
Candle head, a controller-only repair still changes the authenticated S1
execution contract.  The project will therefore pay for a new cache-disabled
canonical bootstrap at `4c0ab6380bb24badfaca0be0fb5800e89df22fa2` in fresh
Candle and CakeML worktrees.  Rebinding or externally manipulating the old
runner would not be promotable evidence.

After the replacement schema-6 link passes independent replay, the immediate
sequence remains:

1. run two fresh serial 65-target schema-4 Candle Great-100 executions;
2. obtain the separately reviewed finalization authorization for their exact
   reports and nonces;
3. run the independent S1 finalizer against the existing approved 130/130 V3
   reference authority; and
4. begin the 30-action cumulative direct Flyspeck base stratum.


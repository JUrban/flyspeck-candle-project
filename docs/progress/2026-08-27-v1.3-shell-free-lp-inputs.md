# v1.3 shell-free LP inputs and selected-source FFI elimination — 2026-08-27

This report records a direct-S3 implementation checkpoint.  It is not an S1,
S2, S3, or G4 promotion claim.

## Implemented checkpoint

On Candle branch `codex/flyspeck-v13-loader`:

- `d9f1c616c6e8c5369c544dfd15947f179a20c14c` removes the custom `chdir` and
  `system` ML/C bridges from the selected source and build recipe.  The launcher
  starts at the repository root through relative generated boot/config links,
  while `Sys.command` fails closed.
- `a08e551a4398907776112eb72db1573f65cf2012` adds the exact LP archive
  preparation contract, the static runtime certificate inventory, and durable
  shell-free frontier evidence.

The selected compatibility inventory at `a08e551...` now contains zero
`Runtime.customFFI` calls.  This replaces the historical two-call source result;
the two ledger entries remain visible as elimination obligations rather than
being deleted.

## LP input boundary

The only selected compressed certificate is prepared outside the proof runtime:

- input archive:
  `formal_lp/glpk/binary/hard_7.tar.gz`;
- archive SHA-256:
  `7056e306a03d7eb02db3c0bed898f3eef6f84f06556c040ac38ba93eb0a12453`;
- accepted archive shape: exactly one ordinary member named `hard_7.dat`, no
  links, traversal, or extra members;
- prepared bytes: `119855733`;
- prepared SHA-256:
  `0ca1b5b6ceba53537ac5d95ffddd883bb297e7d48c30ac241de4f3ec71ab5526`;
- prepared receipt SHA-256:
  `77d099cb5035c20645f83f5ab5e19abd77b28eb2b32292e8f3314d4867366841`.

Preparation is atomic and separate from the proof runtime.  The direct loader
authenticates the prepared file and supplies an exact sorted list of 39 `.dat`
certificate paths.  Runtime directory scans, shell commands, extraction, `.gz`
fallback, and cleanup commands are forbidden.

Two new exact, hash-bound Flyspeck normalizations implement the runtime side:

- `PROJECT-FFI-S3-LP-SHELL-ELIMINATION-001` removes the selected
  `Sys.command`/`tar`/`rm` branch and rejects compressed inputs;
- `PROJECT-FFI-S3-LP-STATIC-INVENTORY-001` replaces the selected directory
  scan with the authenticated 39-path value.

The complete normalization contract now has nine entries and 19 ordered
operations, SHA-256
`ea38ed5b4515af81eaace1ab83859695d052f42579d8eb0c0689234fbf268067`.
The generated overlay receipt has SHA-256
`6558c61c8ff707ef0cf24eb1a526daaf5883b2abb51967119178d006fbf2e61c`.

## Tests and direct frontier

The manifest, normalizer, prepared-input, static-loader-action, Filename,
Unix-metadata/fail-closed-command, Digest, and Str gates pass.  The updated
manifest has 297 build roots, 400 source nodes, 706 dependency edges, 43
generated inputs, and SHA-256
`220bd2c6ecbb4ae85a4af552ee8ccd58d4917d29c1f82b7e9491428bcc6832fc`.

The compiled direct run using the prepared input and nine-output overlay
completed the authenticated preflight, exposed the exact 39-path value, loaded
normalized `general/parser_verbose.hl`, and stopped at the already-known
`open Parser_verbose` declaration in `general/debug.hl`.  Its durable log is:

`/project/flyspeck-candle-runs/v13-direct-overlay-frontier-shell-free.log`

with SHA-256
`69fd5740657dcd06f0639a93f4f5802848a0ab4432e1e320a15c1d8d7414dc03`.
The run took about 3.5 minutes, dominated by pure Candle authentication of the
119.9 MB prepared certificate.  This is initial G8 evidence, not an accepted
scale result.

## Promotion blockers and next actions

The executable used for this frontier is the preserved binary with SHA-256
`d361be3839f31811328d5a0da1ecea15a8a73f369c77e34a288355f16bb930d3`.
It predates the source remedy and still embeds the historical FFI C patch.
Therefore G4 remains **in progress**, not `not required` or proved.

The next required steps are:

1. finish the verified Dopen integration and rebuild the generated Candle
   executable from the selected source/build recipe;
2. inspect and regression-test that clean executable to exclude the historical
   FFI patch;
3. advance the direct run beyond `general/debug.hl` into the LP/nonlinear
   modules;
4. execute all 39 certificate checks and negative mutations;
5. match reference certificate, theorem, definition, hypothesis, and assumption
   fingerprints in the complete selected run.

PFT remains running as an independent oracle.  Per `audit1.md`, it cannot close
any of these direct-source gates.

# v1.3 exact direct compatibility closure — 2026-08-27

The repository-wide compatibility inventory is now projected onto the exact
400-node direct-source manifest.  This closes source-membership uncertainty; it
does not close semantic compatibility or any S milestone.

## Artifact

Generated artifact:

`compatibility/generated/direct-closure-summary.json`

- artifact SHA-256:
  `fb5704da090d3fc62b91411618a0f60ba4109785b2ec75d9b0be45f88e4327a2`;
- manifest SHA-256:
  `dd4707140892dd531b55b3d5ae5e05a13bdbcd474aba060c37502ede58497db8`;
- inventory SHA-256:
  `e713ab19cfd207a8cd4db191436df467db763ba73f2799a288f6a50fa4c71b02`;
- selected findings: 3,689 in 329 source files;
- exact site digest:
  `28a1cc07d6c9e34f8fc979f75a1421c7101ecf1c585c271a22faf57c1360e372`.

Every selected record requires both an exact `repository:path` node match and
the same source SHA-256 as the manifest.  A stale selected source aborts the
generator.  The output records category counts, source paths per category, and
counts by earliest build stratum.  A byte-reproducibility check is now part of
the compatibility validator and release lock.

## Exact selected counts

The 3,689 findings split into 3,180 separately governed Dopen declarations and
509 other compatibility-sensitive occurrences.

| Category | Occurrences | Files |
| --- | ---: | ---: |
| declaration `open` | 3,180 | 234 |
| parenthesized local open | 79 | 23 |
| `let open ... in` | 0 | 0 |
| module structures | 389 | 306 |
| module aliases | 1 | 1 |
| other module declarations | 15 | 7 |
| includes | 1 | 1 |
| module-type declarations | 9 | 9 |
| functor/first-class-module syntax | 0 | 0 |
| pointer infix/binding/reference tokens | 12 / 1 / 2 | 3 / 1 / 2 |
| custom FFI / external declarations | 0 / 0 | 0 / 0 |

The 79 parenthesized local opens have 78 occurrences in the base stratum and
one in LP support (`formal_lp/glpk/glpk_link.ml`).  Generator v2 classifies all
79 as operator references (20 distinct operators) and finds zero selected
general-expression bodies.  A simple/dotted-path OCaml/Candle differential
passes with deliberately shadowed outer operators.  A separate negative oracle
records Candle's `Undefined variable: Array.get` divergence for the broader
`M.(general expression)` form; all seven such broad-inventory sites are outside
the selected direct closure.  The release gate now rejects any selected body
outside the exact operator-reference subset.

The exact module set contains no selected local let-open, functor declaration,
or first-class module.  Those broad repository findings therefore cannot drive
direct-route implementation work.  The remaining selected module forms still
need semantic grouping and minimized regressions before G2 can close.

## Gate impact

- G2 advances from repository-snapshot inventory to exact selected membership;
  the selected parenthesized local-open subcategory is resolved.
- G3's 15 reviewed selected tokens are independently reproduced.
- G4's zero selected calls are independently reproduced.
- Dopen's 3,180-site count is independently reproduced without changing its
  proof obligations.

Dynamic reachability and semantic equivalence are deliberately not inferred
from membership.  Failures found by the Great-100/direct prefix runs continue
to determine remedy order.

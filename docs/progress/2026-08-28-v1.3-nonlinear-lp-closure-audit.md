# Direct nonlinear and LP closure audit — 2026-08-28

## Outcome

The direct manifest has exact source, normalization, and generated-input
boundaries for LP and nonlinear work, but S2 and S3 remain open.  There is no
provenance-bound compiled LP/nonlinear observation under CakeML
`936219bbc3021fa20418d62e85155f2d0092b9f9`, and PFT evidence is explicitly
excluded from this assessment.

The authenticated static inventory contains 43 generated inputs totaling
554,708,472 bytes, an exact 39-file LP runtime inventory, 19,715 distinct
archive IDs, 812 nonlinear `add_inequality` records, 463 explicit `add_case`
partitions, 7,479 `Iarg_leaf` constructors, and a source-stated 23,242 case
parameters.  These counts are future coverage targets, not execution evidence.

## LP boundary and missing gate

The selected route authenticates and prepares the sole `hard_7.tar.gz` member
outside the proof runtime, consumes ordinary `.dat` inputs through an exact
static table, and applies the exact immediate-int normalization before parsing.
Important anchors are:

```text
prepared hard_7.dat  0ca1b5b6ceba53537ac5d95ffddd883bb297e7d48c30ac241de4f3ec71ab5526
preparation contract fdebe3aca0b693f367ac284c28d2b2e01f1b00b5342a4366d4cdd093016f5024
preparation receipt  77d099cb5035c20645f83f5ab5e19abd77b28eb2b32292e8f3314d4867366841
39 basenames         4a4f95cbb6ad331c12945097645dfaf011567ebcb2309b603887b60a12d3eeec
19,715 ID list       61d0ce68f376d7cf6eedb0a185c55072d9d9711a53bff215495a272abfebd11d
```

The source fails on a missing archive ID through `Hashtbl.find`, but it does
not establish exact result coverage: duplicate results can overwrite or
coexist, and extra verified IDs are ignored by the final traversal.  Full LP
acceptance must therefore require exactly 19,715 results, equality with the
archive ID set, multiplicity one for every ID, consumption of all 39 declared
files once in order, no shell call, and the exact theorem/hypothesis/axiom
fingerprint for `|- linear_programming_results`.

A one-ID `Verify_all.verify_all ["229367313231"] None` workload is a useful
authenticated diagnostic only.  It cannot be reported as LP theorem closure.

## Nonlinear boundary and missing gate

The selected route pins `prep.hl` at SHA-256
`3ccef1bf65b13ca02e7ab1f409ebb49c979862ed5ea6ff1d57ed1e4e4761723d`
and `break_case_log.hl` at
`2b3c74156a5ee9a6b3b5b6905ff28a7fb21e7c50052ad37887b90b9ed3d5e499`.
Hashing those inputs does not show that Candle reconstructed every proof leaf.

Two apparent source checks are not gates:

- `List.length all_parameters = 23242` evaluates a boolean but does not abort;
- the final comparison with expected digest
  `e607b9e5e7f4c495236c6546d6889963` is also only a boolean binding.

The authenticated runner or exact normalization must make either false result
fatal.  Full nonlinear acceptance additionally requires exactly 23,242
attempted and successful parameters, no `TRUTH` failure sentinel, complete leaf
coverage, empty hypotheses, the allowed axiom set, the expected digest, and the
canonical fingerprint of `|- the_nonlinear_inequalities`.  A single
`Break_case_exec.get_nth 0` run is only a diagnostic leaf.

## Ordered next gates

The smallest defensible sequence is:

1. finish the compiler provenance gate and real Parser_verbose/Debug prefix;
2. run the authenticated cumulative base prefix (actions 0–29);
3. run through nonlinear support (actions 0–49);
4. run the separately declared one-ID LP diagnostic;
5. run cumulative LP closure through action 184 with exact ID/file coverage;
6. run the separately declared single nonlinear-leaf diagnostic;
7. run cumulative nonlinear closure through action 296 with fatal count/digest
   checks; and
8. run the final target, require exactly
   `import_tame_classification ==> the_kepler_conjecture`, then repeat from a
   clean state with identical semantic artifacts.

Every standalone diagnostic needs its own authenticated manifest.  An
interactive command against ambient theorem state is not release evidence.

## Ledger interpretation

The authoritative main ledger already records the immediate-int normalization
as implemented with its language proof discharged, while leaving its compiled
regression pending.  It also records the system custom FFI as outside the
selected dependency closure: the exact selected route is shell-free, but the
complete 39-certificate execution remains a G7/G9 and S3 obligation.  Those
more mature entries are preserved rather than replaced with the audit branch's
older lifecycle wording.

This audit used only static hashes, source reads, JSON queries, and lexical
counts.  No certificate body, nonlinear proof, or theorem was evaluated, and
no performance claim follows from it.

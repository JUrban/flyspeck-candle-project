# v1.3 parser-to-Dopen frontier — 2026-08-27

The authenticated direct-source loader now completes
`text_formalization/general/parser_verbose.hl` and selects
`text_formalization/general/debug.hl`.  Its first remaining error is exactly:

```text
open-declarations are not supported (yet)
Parsing failed at line 16

  open Parser_verbose
```

This is pre-S1 compatibility-frontier evidence.  It does not advance S1, S2,
or S3, and it does not use the PFT route as source-execution evidence.

## Implemented slice

Two new hash-bound normalizations close the selected parser path:

- `PROJECT-PARSER-S3-LET-OR-PATTERN-001` authenticates
  `general/parser_verbose.hl`, moves its one selected let-binding or-pattern
  into an ordinary match with the same two alternatives, and replaces the
  file's sole `%s`-only `sprintf` call by concatenation with the same
  `string_of_num` result.
- `PROJECT-PARSER-S3-TRAILING-SEMI-001` authenticates `general/debug.hl` and
  removes its one trailing sequence separator immediately before a closing
  parenthesis.  The expressions, evaluation order, effects, exceptions, and
  unit result are unchanged.

The original and normalized fixtures pass under pinned OCaml 4.14.1, and the
normalized fixture passes under the compiled Candle executable.  The selected
400-node graph has only the one let-binding or-pattern; the similar Azure copy
is outside the direct dependency closure.

The source-action scanner also now distinguishes module item separators from
expression sequencing.  It removes `;;` only when the innermost context is a
`struct` or `sig`, retains it in `begin`, and has a compiled nested-module
fixture plus a negative `begin 1;; 2 end` case.  This closes the false EOF
frontier at the start of `module Debug` without accepting an expression-level
separator Candle cannot parse.

The combined normalization contract contains seven entries and sixteen exact
operations.  All original and output sizes, MD5 values, SHA-256 values,
operation order, anchors, and source lines are authenticated.  The overlay is
not added to `load_path`; the loader registers seven exact original-to-output
mappings after authenticating the original source tree.

## Evidence pins

- Candle loader: `eb88bc98789591d793626ebd894399095b3019d2`;
- CakeML source actions: `1b17732f902fde2efd985905d277a972da73ae0f`;
- CakeML `candle_boot.ml` SHA-256:
  `55a0e08b515a06e9b047ccedb62eb98eb4808f3b45e9c4b11e13eac7f7abba94`;
- runtime boot: the exact 38,504-byte source prefix plus the 615-byte
  working-directory suffix, 39,119 bytes total, SHA-256
  `ecd0da6408863d140cff3988ccc111e5b6677ebc48a4b1f91f5f05698643e705`;
- generated direct manifest SHA-256:
  `5897debad9e9004b8fef240c848d1b9b552960f717f6124c04d56e3c3829546b`;
- seven-entry, sixteen-operation normalization contract SHA-256:
  `dfe12cb8171e8f8d63330bf4c5f647072ffda5a5676f4d70f005ce8cccaf46e3`;
- materialized overlay:
  `/project/flyspeck-candle-runs/v13-normalized-overlay-dfe12cb8171e8f8d63330bf4c5f647072ffda5a5676f4d70f005ce8cccaf46e3`;
- clean direct-frontier log SHA-256:
  `cc75153644c72a85bd0ee6acb88e66288e903f08a5aa3cd28f462d29d4b3b5c7`.

## Gates

- 23 manifest and 10 normalization unit tests: pass;
- pinned 297-root, 400-node, 42-input manifest regeneration: pass;
- exact seven-entry normalization check: pass;
- OCaml-original, OCaml-normalized, and compiled-Candle parser oracle: pass;
- compiled source-action, phrase/EOF, nested-module, and negative-`begin`
  regressions: pass;
- clean compiled full-loader replay: expected Dopen gap, with parser action
  completion and no debug action completion or full-build marker.

## Critical decision and open gates

Rewriting this first `open` to qualified names would merely move the frontier
and create source-level name-resolution obligations at every later open site.
The v1.3 architecture already identifies verified Dopen as the systematic
remedy, so the direct lane now waits for that compiler integration and will
replay from this exact checkpoint when it is available.

The full selected run, disabled-loader non-use, all later compatibility
frontiers, theorem/definition/assumption fingerprints, nonlinear and LP
evidence, versioned checkpoints, two clean matching runs, and a resume run
remain open.  The parallel PFT/Great100 work remains an oracle and independent
checker only.

# 2026-08-27 — direct static-library capability contract

## Result

Candle branch `codex/flyspeck-v13-loader` at
`58c32cbf24b5a0bfc06fdff1e8b8e7e18949faab` turns the direct loader's first
parser failure into an exact, promotion-blocking G6 contract.  This remains
inventory and policy evidence; it does not claim that Candle can yet parse or
execute the directives, and it does not advance S1, S2, or S3.

The governing source is the unmodified Flyspeck commit
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`.  The PFT development source is not
used in this lane.

## Exact reachable contract

The recursively reached authoritative full-build graph contains six `#load`
directives:

- three `unix.cma` directives;
- three `str.cma` directives; and
- no reachable `nums.cma` directive.

The larger repository has 17 standalone directives, including two
`nums.cma` sites, but those two files are not in the selected full-build graph.
The manifest records that distinction rather than promoting a repository-wide
text count into an execution claim.

The comment-, string-, and HOL-quotation-aware scanner records 41 qualified
capability uses:

- 19 `Unix` uses: `close_process`, `close_process_in`, `gettimeofday`, `mkdir`,
  `open_process`, `open_process_in`, and `putenv`; and
- 22 `Str` uses: `first_chars`, `global_replace`, `regexp`, `split`, and
  `string_match`.

Each directive and use carries its source identity and line.  The contract's
activation state is `blocked-pending-static-binding-evidence`.  Unknown library
names block manifest promotion, and directive erasure or a generic no-op is
explicitly forbidden until every listed operation has adequate semantics.

## Reproducibility correction

Regeneration initially found 400 nodes instead of 399 because this worktree's
ignored compiled build happened to contain `candle/build/insulate.ml`.  The
generator had incorrectly allowed local build state to decide whether that
path was source or generated output.  Known generated dependencies are now
always recipe-governed contracts.  With the ignored build still present, the
manifest is back to the stable 399 source nodes, 419 selected edges, and two
explicit generated-runtime contracts.  A regression assertion prevents
`insulate.ml` from entering the source-node set.

The regeneration also caught and corrected a stale source-line review for the
self-contained loader's dynamic map over `Build.build_sequence_full`.

## Validation

At the exact pins:

```text
manifest ok: 297 roots, 399 source nodes, 42 generated inputs
Ran 13 tests in 0.009s
OK
```

All promotion diagnostics are closed: zero unresolved roots, unreviewed
dynamic dependencies, missing or ambiguous ordinary sources, path escapes,
cycles, and unsupported runtime libraries.  The manifest SHA-256 is
`978781b088fccbec62210ace5b559d09cde5596e9f70e5ceb8264c8f69c1c811`.

## Next semantic gates

The direct compiled loader still correctly stops on
`strictbuild.hl:21` at `#load "unix.cma";;`.  Before that directive can be
accepted, the release path needs static bindings and differential tests for
the twelve listed members.  In particular, process creation cannot become an
unrestricted shell escape: strictbuild's metadata probes need deterministic
substitutes, while GLPK process calls need a narrow executable/argument/I/O
contract.  `mkdir`, time, environment mutation, and regular expressions each
need their own sandbox or pure semantics and failure behavior.

# Pristine semantic-v3 stdout state machine (2026-08-30)

## Implemented result

The immutable stdout parser now accepts the exact ten-line
`CANDLE_PRISTINE_DIRECT_SEMANTIC_V3` session only after all 297 action
completions, all 39 LP successes, and the exact two post-action native events
for `candle:candle/flyspeck_l2_target.ml` followed by
`candle:candle/fingerprint.ml`.  The ten lines must be consecutive and are
followed immediately by the terminal reference-session completion marker,
which remains the final stdout line.

The parser rejects a missing, duplicate, reordered, early, interrupted or
post-terminal semantic record.  Ordinary diagnostics and every other protocol
marker are forbidden inside the semantic session.  The existing categorical
PFT scan still runs before classification on every stdout line.  The exact
semantic decoder then validates framing, payload composites, nonce, names,
indices, counts, boundary and dependency grammar against the same plan/request,
validated transcript and native closure.

Successful parsing derives the strict nonce-free direct semantic projection
and an exact schema-1 run-bound
`candle-flyspeck-pristine-direct-semantic-completion-observation-v1`.  The
observation content-binds the plan, request, full transcript and derived
projection; records exact four-theorem/four-dependency counts and ordered-name
digest; and keeps approval, PFT and S2/S3 false.  Projection serializer identity
is copied from, and cross-validated against, the final native serializer event.

The returned envelope now has the reserved final schema-3 kind
`candle-flyspeck-pristine-direct-raw-source-rederivation-v3`, exact semantic
status `complete-observed-unapproved`, and full nested transcript, native
closure, projection and completion-observation objects.  Cross-runtime coverage
remains exactly null with status
`not-derived-requires-authenticated-inventory-and-generated-inputs`.
Authentication remains `not-authenticated`; candidate, approval, promotion,
PFT, S2 and S3 fields remain false.

## Validation boundary

`validate_source_rederivation` enforces a closed top-level field set, exact
types and status values, all plan/request/output/process bindings, the full
transcript and native validators, the existing strict direct semantic
projection validator, final-native-event serializer equality, and the exact
run-bound completion validator.  The public canonical-byte entrypoint rejects
duplicates, mutable bytes and noncanonical JSON before applying those nested
validators.

This is still raw, passive parser evidence.  The canonical value validator
does not possess retained stdout bytes and cannot replace the later held-file
parser replay.  No coverage adapter, authenticated capture envelope, collector,
request generator, candidate, value-only bundle/pair consumer, comparison
descriptor, approval, release decision or live pilot is enabled.

## Verification

The focused protocol/parser suite passes 42 of 42 tests, including hostile
state-machine placement/interruption cases and nested source-rederivation
type/content splices.  The five-suite authority/consumer regression passes 97
of 97, the compatibility suite passes 3 of 3, Python byte compilation passes,
and `git diff --check` passes.  No OCaml, HOL Light, Flyspeck, bootstrap or
runtime workload was started.

# Direct Flyspeck and reflective nonlinear milestone

Date: 2026-09-21 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still executing and has not earned
  its success marker.
- The protected cumulative process is healthy in `hard_5.dat`. At the latest
  sample it had advanced to terminal 1,474/3,257.
- PFT remains alive as an independent oracle. It is not counted as direct S2
  or S3 evidence.
- No qualified Great100 runtime, source, report, transcript, semantic evidence,
  or other S1 byte was changed.

## Staged reflected Taylor boundary

Candle branch `codex/cv-nonlinear-analytic-v1` now has three committed layers:

- `4074368` proves staged native-float arithmetic;
- `b95a1f6` proves staged folds; and
- `9162f2c` proves staged polynomial evaluation.

Flyspeck branch `codex/flyspeck-cv-nonlinear-analytic-v1` adds symbolic staged
Taylor bounds (`03c762e5`) and an `M_taylor` cell adapter (`7006a142`). The
adapter preserves the existing theorem interface: the reflected result must be
turned back into the ordinary bound theorem consumed by the verifier.

The exact symbolic test passed in 283.62 user seconds, 287 seconds wall time,
and about 1.09 GiB peak RSS. A deliberately fine-grained staged-polynomial
benchmark took 16,896 ticks versus 163 ticks for the legacy conversion, or
**103.66x slower**. This rejects per-arithmetic-operation reflection as the
production shape. Reflection must amortize translation and proof overhead over
a whole Taylor cell or certificate verdict.

## Why the historical action-296 allowlist cannot be repaired narrowly

Two bounded history-reconstruction experiments were run from the exact clean
post-closure checkpoint:

- `v311-action296-missing-primitive-policy-v66-001` omitted only the missing
  primitive `_mk_tybit0`; its first leaf digest became
  `597fa446ccd15af8a2a23b5fc54024a8`, not the required historical digest.
- `v312-action296-structural-constructor-resolution-v66-001` resolved the
  constructor structurally; its first leaf digest became
  `3a33932c30e048adf7e4bc2b15bd40c8`, also not the required digest.

The underlying reason is now source-authenticated. Flyspeck's `INSTALL.md`
states that the current HOL Light/Flyspeck pair is incompatible with the old
serialized nonlinear inequalities and identifies the older commits used for
serialization. In that older HOL Light basis, finite types 2, 3, and 4 were
primitive `define_finite_type` constructions; the current basis implements
them through `tybit0`/`tybit1`. Consequently, current `real^3` and `real^6`
theorems have genuinely different dependency histories. The 2014 leaf
allowlist cannot be reproduced by another path or constructor exception.

The two negative runs are preserved. No allowlist was weakened and neither
candidate patch was applied. The correct current-source path is to reconstruct
ordinary kernel theorems with the real nonlinear verifier, then replace coarse
checker work reflectively where measurements justify it.

## Real nonlinear verifier frontend localization

The unsegmented first-leaf replay v18 spent 11,258.60 child-user seconds
(about 3.13 hours) and stopped after emitting the marker immediately before
the 3,701-line `Taylor_interval` module phrase. It emitted neither the first
module-body marker nor any proof marker. This localizes the failure to parsing,
type inference, or elaboration of that one phrase, before a Taylor theorem is
executed.

Focused parser-only probes expanded HOL quotations exactly as the Candle loader
does. A prefix through line 834 passed in 156.71 seconds. Seven disjoint module
segments, bounded at authenticated top-level structure items, all passed:

| Segment | Parser seconds |
| --- | ---: |
| through line 834 | 156.71 |
| 835--1355 | 101.78 |
| 1356--1913 | 109.46 |
| 1914--2468 | 104.72 |
| 2469--2901 | 57.88 |
| 2902--3577 | 213.33 |
| 3578--3700 | 41.70 |
| **sum** | **785.58** |

The isolated-segment sum projects a **14.33x frontend reduction** relative to
the failed single phrase. It is an informative estimate, not yet the measured
whole-loader result: every parser-only sample used a fresh process, while the
real loader will parse the chunks sequentially in one authenticated state.

Candle commit `850022d` on `codex/cv-nonlinear-profile-v3` implements the
opt-in segmentation. It authenticates the exact source hash and six fixed
structure-item anchors, reconstructs the original body byte-for-byte, and
creates seven internal modules. Each later module reopens the original
dependency namespaces and includes its exact predecessor; the public
`Taylor_interval` module includes the final chunk. Theorem terms, tactics,
section operations, and the public module members are unchanged. Fourteen
discarded markers distinguish each chunk's frontend entry and completion.

Eleven focused segmentation, closure-profile, and first-leaf controller tests
pass in 25.03 seconds. The complete transformed parser probe and exact
first-leaf replay `cv-nonlinear-first-leaf-v19` are active. They receive no
theorem, cumulative, S2, S3, qualification, or release credit unless their
ordinary kernel-theorem checks complete.

## Plan impact

1. Finish the segmented legacy first-leaf closure and require its exact target
   conclusion, empty hypotheses, and unchanged axiom set.
2. Once that baseline is green, integrate the already proved `M_taylor` adapter
   at a whole-cell boundary and profile translation, informal search, formal
   leaf checking, domain splitting, and theorem glue separately.
3. Start with the smallest real current-source action-296 leaf to establish the
   machinery, then retain the substantial `prep-4680581274 delta issue-cayleyR`
   leaf as the scaling target.
4. Extend translation only for constructs reached by real certificates. Keep
   the existing verifier theorem interface and compare complete verdict cost,
   not isolated arithmetic micro-operations.
5. Preserve the direct cumulative and PFT processes throughout. A coherent
   reflected repair will be confirmed at a fresh cumulative boundary; no
   action-zero replay is justified while the prototype is still changing.

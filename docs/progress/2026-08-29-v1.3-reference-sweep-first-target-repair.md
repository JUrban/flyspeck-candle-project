# v1.3 first reference-target repair — 2026-08-29

The first reviewed Great 100 collection stopped fail-closed on sweep 1 target
1. No candidate was published, no identity was approved, and the stopped root
must not be resumed after the serializer repair because its immutable contract
pins different Candle and serializer bytes.

## Retained failed collection

The failed evidence root is
`flyspeck-candle-runs/s1-reference-v6-two-sweep-c2b55b1`. Its contract pins
project `4ca56a9e5d1f99e2e6794de2cccaebce910b7122`, Candle
`c2b55b183387be33b6bbe71c5a6b1f1b9b043dbc`, exact reference
`1258c129c3ddf0b239b649ba7024eab677cd953b`, and serializer SHA-256
`f2b7322e73eef2f71479b0f0287c83eb407f44418facec9d66a6490c54832617`.
The published contract SHA-256 is
`83306e94fe4ba6d20fa9735580029db548108dbd232e622be1187e968edccc77`.

The controller attempted `100/arithmetic_geometric_mean` once and exited one.
Its immutable failure receipt SHA-256 is
`5bd50a1b808db550570db52e950262c40c5c4e10605aeb820614eead76fff546`;
the retained transcript SHA-256 is
`ed5c26dc8f4699ab3a81258967486c6b5a7037cbd3c31c5b80d97772d969bfd9`.
The collector reported `expected exactly one post-state fingerprint; got 0`.
The transcript contains exactly one session marker, complete theorem records
for `AGM` and `AGM_ROOT`, then `Stack overflow during evaluation`, then the
session completion marker. Both partial theorem records reconstruct exactly,
have zero hypotheses and three global axioms, but they are not an acceptable
candidate without the unique post-state record and its axiom cross-check.

## Exact localization

Three independent exact-runtime probes localized the overflow. All structural
state components build successfully after loading the target:

- type constants: 1,083 bytes;
- term constants: 114,103 bytes;
- primitive definitions: 2,063,581 bytes;
- global axioms: 4,734 bytes;
- combined kernel state: 2,183,544 bytes.

The old encoder also hex-encodes the 2,063,581-byte definitions component, but
overflows on the combined state. Its implementation expands the input with
`explode` and then applies non-tail-recursive `List.map` once per character.
The fresh-process contract already pins `OCAMLRUNPARAM=l=2000000000`, so
raising a stack limit would not repair the algorithm and was rejected.

## Iterative wire-preserving repair

A new isolated Candle worktree on branch
`codex/flyspeck-v13-s1-scalable-fingerprint` reaches commit
`30996f4015988270e028842e9d0ab9e29b262697`. The serializer now allocates the
exact two-byte-per-input-byte result and fills it with a bounded `for` loop.
It does not change the lowercase hexadecimal wire mapping. Exact OCaml tests
compare old and new encoders on all byte values `0x00` through `0xff`; the new
encoder also completes on the reproduced 2,183,544-byte state and returns
4,367,088 bytes. The new serializer SHA-256 is
`ec150fcb1af48c5554cf535b0b076b0607e44e67d12367b7a0fb8a565e5639ff`.

The focused fingerprint/reference tests pass 25/25. The complete noncompiled
Candle suite passes 206/206 and the worktree is clean.

A separate exact-runtime behavioral probe is sealed at
`flyspeck-candle-runs/s1-hex-wire-30996f4-behavioral`. It compares old and new
encoders over all 256 byte values, obtains exact equality and a 512-byte wire,
then encodes a 2,200,000-byte input to exactly 4,400,000 lowercase hexadecimal
bytes with correct boundaries. It exited zero in 1:57.38 with 353,156 KiB
maximum RSS and no swaps. The request, transcript, and timing SHA-256 values
are respectively
`2aa0408cb8543bbcb1aa13b7a33485c35b7aed57106886c38593000afcfaec5d`,
`33bc616755b33ef8c1221bc48ccdb9e8bc83feaa2a968036b05397aa48412438`,
and `5cada3be5b674f8ecd0950a1ad62f447cc45b5d182427a6483878448d52dc731`.

## Exact repaired AGM smoke

A fresh manifest-exact collector run at the repaired head completes and
validates again offline. Its immutable artifact SHA-256 values are:

- candidate: `6de994d9c28d7bc6a74ea950d0cb4709b4592e23b4578eefb5c6a5483dc204bb`;
- plan: `a10c5b5b95f0055fd4c1f62d8ac155b10adcc01ef473d90d43e2d696ec2ccda4`;
- request: `fcb5a1ef96c839089b7e57f41d1f22299354ec189f6dea42fed64b5da2958f12`;
- transcript: `ccd722b687a1d813183ad137afe19be84035e0d9d7efefcff30528072beac3d1`.

The transcript has two theorem records, one post-state record, one session
marker, one completion marker, and no stack overflow. The post-state line is
8,734,135 characters. `AGM` and `AGM_ROOT` remain closed with exactly three
global axioms; the candidate is `observed_uncompared`,
`candidate_unapproved`, and `promotion_allowed = false`.

## Next launch gate

Independent replay approved commit `30996f4`, verified that the repaired
theorem wire records are byte-for-byte equal to the pre-failure records, and
revalidated the repaired smoke as complete but unapproved. The project runbook
and frozen controller checkout must now be repinned to the new Candle head, and
a fresh real-input contract must pass preflight. Only a new external artifact
root may be launched. The stopped `c2b55b1` root remains diagnostic evidence
and will not receive another attempt.

## Corrected reviewed launch

The dedicated project controller checkout was frozen clean at project commit
`82f5450439eeb887732a5e4e94d7bfb495a3f030`; its controller bytes remain
SHA-256
`79cf708e2ed356ca8a9f6d600b83faab5ed6aeda9fca922333118d0cb38c373c`.
Independent rereview reconstructed the corrected contract and returned an
explicit relaunch PASS. The canonical contract SHA-256 is
`f27d18e7c97c1f1bc1a518e62043ca8512ab5ca6ca5a2f584cd9dfb9b1a299b6`;
its compact semantic digest is
`a725a701a4910c2decc358737045319a0b2af9560f508f9b44a1e470b0c5e6f3`.

At 05:08 UTC the exact isolated command created the fresh private root
`flyspeck-candle-runs/s1-reference-v6-two-sweep-30996f4` and started sweep 1
target 1. The published contract binds project `82f5450`, Candle `30996f4`,
serializer `ec150fcb`, and exact reference `1258c129`. Initial status is open,
0/130 complete, zero failures, zero publication interruptions,
`approval_status = candidates_unapproved`, and `promotion_allowed = false`.
The old `c2b55b1` controller is stopped and its root remains unchanged.

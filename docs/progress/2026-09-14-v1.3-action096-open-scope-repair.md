# Action 096 heap localization and OCaml module-open repair — 2026-09-14

## Flyspeck progress

The fresh action-zero DEVELOPMENT / NON-RELEASE replay at
`/project/flyspeck-candle-runs/v47-dev-direct-boundary04-047da56-attempt-001`
reached 97 actions and passed 96. Actions 000 through 095 passed in order,
including `marchal3.hl`, `GRUTOTI.hl`, `KIZHLTL.hl`, `bump.hl`, and
`sum_gamma.hl`. Action 096, `packing/UPFZBZM.hl`, was entered but emitted no
completion marker.

The process ended at the configured 4 GiB CakeML heap ceiling with generic
nonzero status and maximum RSS 4,499,712 KiB. It emitted no HOL exception,
parser error, theorem failure, or MESON progress after entering action 096.
CakeML's runtime maps that exit path to heap exhaustion. The failed process is
closed and is not a reusable checkpoint.

Current headline remains:

- cumulative boundary: `04-geometry-through-151`;
- actions reached / passed: 97 / 96;
- first blocker: action 096, `packing/UPFZBZM.hl`;
- classification: CakeML heap exhaustion before theorem-level output.

## Localization

The completed MESON sequences from `GRUTOTI.hl` and `sum_gamma.hl` match native
HOL Light in rule order and node counts. This weighs against polymorphic
comparison changing the successful searches and points instead to allocation
outside those completed proof searches.

`UPFZBZM.hl` defines a module containing 29 bare `open` declarations. The
CakeML OCaml frontend previously translated both OCaml `open` and `include` to
the environment-producing CakeML `Dopen`. Inside `Dmod`, each opened
environment was consequently accumulated into the module result, transitively
re-exporting imported identifiers. Besides differing from OCaml, this can
multiply already large Flyspeck module environments.

An obsolete post-Marchal checkpoint was tested only far enough to establish
that it lacks later modules required for faithful action-096 inference. All
copies were retired; the probe architecture was not extended.

## Narrow central repair

On isolated CakeML branch `codex/flyspeck-v15-ocaml-open-scope`:

- commit `0e4ac6b78f2733f91ff1ee54829489bce95b46a9` scopes bare opens over the
  remaining module declarations using existing `Dlocal` semantics;
- OCaml `include` is represented distinctly as `Dlocal [] [Dopen ...]`, so it
  retains its exporting behavior;
- top-level `open` remains unchanged and persistent;
- parser, inference, and evaluator regressions prove that `open` exports a
  later declaration but not the imported name, while `include` exports both;
- commit `50d54fab2117eb5069487efc0bfa0c0b20403285` registers the helper with
  the verified parser translation.

Native OCaml 4.14.1 independently exhibits the same observable contract. The
focused `camlTestsTheory.uo`, `dopenTestsTheory.uo`, and compiled
`caml_parserProgTheory.uo` targets pass. The compiled parser target completed in
41m56s and discharged the enclosing module-definition preconditions.

## Current build and next functional gate

The broad x64 assurance proof was stopped after dependency review because it
would rebuild 136 backend theories before the next functional observation.
The necessary 18-target executable bootstrap is active with two Holmake jobs
and one HOL thread per worker at:

`/project/flyspeck-candle-runs/cakeml-ocaml-open-development-bootstrap-50d54fab2-attempt-001`

This is development/non-release validation only. After the x64 development
compiler build passes, the new runtime will first run the small
native-OCaml-equivalent `open`/`include` oracle and then a fresh action-zero
cumulative replay with a bounded 16 GiB CakeML heap. Checkpoints made by the
old parser/runtime will not be used.

For later minimized `SET_TAC`/MESON runaways, the first cheap discriminator
will be a native-versus-Candle comparison at the MESON boundary: rewritten
goal and assumptions, rule order, early search choices, and node counts. A
rule-order divergence will reopen the OCaml polymorphic comparison/sorting
replacement as a possible central cause. Matching search with materially
slower per-node execution will instead reopen physical/pointer equality and
sharing. Isolated context-free proof rewrites remain acceptable compatibility
repairs, but an accumulating family of similar rewrites will trigger this
central comparison before further local normalization.

No Flyspeck theorem source, qualified Great100 runtime, qualified report,
transcript, semantic evidence, or formal S1 artifact was changed.

# Great100 GCD reference candidate — 2026-08-28

## Outcome

Candle commit `79ba0f4c6459b75a33788ed9d5d9a7e3c45c8c84` repairs the
HOL Light reference collector and a fresh `100/gcd` collection now completes.
The artifact is intentionally a schema-v2 review candidate, not an approved
expected identity and not S1 evidence.

The first documented launcher-based attempt failed closed. `hol.sh` activated
an incomplete local OPAM switch that could not load Camlp5, while the
interactive prompt prefixed the nonce marker. No theorem record was accepted.
The repaired collector invokes the pinned `ocaml-hol` runtime directly with
the pinned `hol.ml`, exact include root, sanitized environment, required
`dllzarith.so`, and `-noprompt`. It re-derives all pins after execution.

## Candidate evidence

The successful artifacts are:

- plan: `flyspeck-candle-runs/s1-reference-gcd-v2-plan.json`, SHA-256
  `53bc6d9e48ad7767bffda24e39f1b7e944b71ffd91847905d50d7ece725e0226`;
- request: `flyspeck-candle-runs/s1-reference-gcd-v2-request.ml`, SHA-256
  `379b7206aebd7c62cef75171967c1b3bb50362bf2277c2b537cf7166ab32e498`;
- transcript: `flyspeck-candle-runs/s1-reference-gcd-v2.log`, SHA-256
  `e4ed45b05208d924225f07ec94484ce4b669425095827f1c80ac8c39c4d039d6`;
- candidate: `flyspeck-candle-runs/s1-reference-gcd-v2-candidate.json`,
  SHA-256
  `bbc9bc528b1cd0e4b74c3cfd58f6f60ba1747bdea1412cce9a1136a9d2d73899`.

Validation reports exit 0, one exact `EGCD` record, zero hypotheses, and three
global axioms. The candidate records `observed_uncompared`,
`candidate_unapproved`, and `promotion_allowed: false`. Its collector hash is
unchanged at the clean commit above.

## Remaining review boundary

The reference Git tree is the clean PFT-producer branch
`a2674c3005da788bb6f1ac9046444edbc70983aa`; the direct GCD source hash equals
the manifest, but a reviewer must still decide whether that reference revision
is suitable, review the serializer's cross-runtime canonicality, and account
for unenumerated OCaml/Camlp5 standard-library dependencies. An independent
read-only review is in progress. Until it accepts the evidence, approved
identity coverage remains 0/65 and the candidate must not enter
`EXPECTED_IDENTITIES`.

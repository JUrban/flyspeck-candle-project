# 2026-08-27 — selected Unix process/filesystem route

## Outcome

Candle commit `87f1fd965313c090fd4d87bcfcc493a3ad7bc79f` closes a
future source-typechecking gap without granting a new host capability.
`formal_lp/glpk/lpproc.ml` contains `Sys.chdir` in the deferred historical GLPK
generator route, but Candle did not bind that member.  `ocaml.ml` now supplies
the name as an explicit failure, matching the existing fail-closed
`Sys.command`, `Unix.open_process`, `Unix.close_process`, and `Unix.mkdir`
policy.  Any unexpected invocation aborts the one-shot direct run.

## Exact route classification

The manifest now separates three cases:

- strictbuild's exact `date` and `whoami` requests remain deterministic,
  manifest-hashed ordinary-file reads;
- the LP certificate source's original mkdir/chdir/command/readdir/tar/rm lane
  is absent from execution through normalization
  `PROJECT-FFI-S3-LP-SHELL-ELIMINATION-001`; and
- `glpk_link.ml` and `lpproc.ml` retain shared pure definitions plus historical
  generator/debug bodies.  The source scanner freezes all 32 identifier
  occurrences in that internal chain, zero selected qualified external
  `Glpk_link.*`/`Lpproc.*` calls, and the sole `open Glpk_link` inside `Lpproc`.
  The route root `Lpproc.execute` is definition-only in the selected graph.

This is bounded lexical non-use evidence.  It cannot rule out reflection,
constructed identifiers, or external input.  The ledger entry remains
`regression_pending` until the complete compiled route and final theorem
statement/assumption/fingerprint gates pass.

## Evidence

- `scripts/test-unix-process-route-compatibility.sh` observes ordinary
  `Sys.chdir "."` behavior in OCaml 4.14.1, then checks compiled Candle's
  deterministic positive path and command/process/mkdir/chdir negatives.
- All 26 Candle manifest unit tests pass.
- Manifest regeneration reaches a fixed point at 297 build roots, 400 source
  nodes, and 43 generated inputs.
- Compiled Candle verifies all 399 preflight source digests and rejects the
  corruption fixture.
- Direct manifest SHA-256 is
  `dbc6da53c60981660ca80763057a359e0f21c8404c89adf052974dd5a64f37ad`.
- The broad inventory remains 1,364 files/6,074 findings; its SHA-256 is
  `4521df8a230be72fbd5c52db1f5e7d4747cfa37c1b497945f8e3269715f404b6`.
- The exact direct closure remains 3,689 findings/329 files; artifact SHA-256
  is `cd5744e91977b60f91a7c6b8807df25d9c618c8a4b6b564db7db8f9ed36844dc`
  and site SHA-256 is
  `f355b17244d953f1c290def6142b47efc7306dfcf3bd25d5988c03fa6b6b9ce3`.

This advances G2 runtime compatibility but does not claim general Unix/GLPK
support and does not advance S1, S2, or S3 by itself.

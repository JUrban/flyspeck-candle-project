# Frozen-head direct Flyspeck stratum plan — 2026-09-08

## Outcome

The cumulative direct-source plan has been freshly materialized against the
frozen final Candle head
`5e6362f4df4979d9a35e99c4673d177f32eb3145` and the clean Flyspeck head
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`.

The new host-only publication root is:

`/project/flyspeck-candle-runs/v13-stratum-plan-5e6362f-attempt-001`

The authenticated plan contains 297 actions, eight cumulative boundaries and
two diagnostic cutpoints.  Its exact hashes are:

- `plan.json`: `d384068b5ea3e5231bbe5e2e1d6d224d8c6f3f6ec66c4007f3cb0461c1815680`;
- `host-materialization.json`: `edf2296688bbc371ad6f726cbb3ca5c13bc6969e23cc94ef36a223d799505805`;
- `host-schedule-template.json`: `00c4aef291f9ac7b2a2eeb451ace81d83959bbd009739defe629c3c8428a84e7`.

The publication root is mode `0555`; every plan, receipt, schedule and prefix
file is mode `0444`.

## Reuse decision

The prior normalization overlay and generated LP tree did not need to be
rebuilt.  Their current Candle contracts are byte-identical to the contracts
bound by their receipts:

- normalization contract SHA-256
  `2e7274c15ec56ffe864e11c100b2fb8493471111117b83036a116e2cfff00f0b`;
- generated-input contract SHA-256
  `dc9f9e5d60b3b43a1275a450e643bb1d8ce4843d517cdf4d84a6dc5c318a8816`;
- planner source SHA-256
  `73ca6798beddc34c69b51e3ae7de1a331b2e8679193628be753062be2da7add0`.

The planner independently re-authenticated the clean Candle and Flyspeck
trees, 400 selected source nodes, 20 normalization outputs and 43 generated
inputs.  The old and new `plan.json` values are identical after removing only
`repositories.candle_materialization_head`, and all ten generated prefix files
are byte-identical.  Reusing the immutable overlay and generated-input tree is
therefore content reuse, not reuse of stale head-bound plan evidence.

## Evidence boundary and next step

This is deterministic host scheduling and source-authentication evidence only.
It does not execute a source action in Candle and is not S2 or S3 evidence.
The first cumulative boundary, `00-base-through-029`, remains gated on the
second clean schema-4 Great100 run, external authorization and independent S1
finalization.  Once those gates close, it can be consumed in a fresh Candle
process directly from this frozen-head plan.

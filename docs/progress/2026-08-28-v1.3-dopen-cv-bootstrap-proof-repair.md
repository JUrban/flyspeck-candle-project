# Dopen CV-bootstrap proof repair — 2026-08-28

## Outcome

The CakeML integration branch now includes
`0c170aa374ec178e5db8a9fe9276244ed7e0dcf7` (`fix: repair Dopen CV
bootstrap proofs`).  The commit repairs the next two proof obligations exposed
by adding `Dopen` to the verified source language.  It changes two proof lines
in `cv_translator/to_data_cvScript.sml` and adds no axiom, admission, unchecked
theorem constructor, or runtime behavior.

The affected target was rebuilt from source with exit status 0:

```text
HOLDIR=/project/worktrees/HOL-cakeml-dopen-v13 \
  /project/worktrees/HOL-cakeml-dopen-v13/bin/Holmake -j1 \
  to_data_cvTheory.uo
```

The transcript is
`flyspeck-candle-runs/v13-to-data-cv-proof-repair-2.log`.  It records both
`compile_decs_cons` and `compile_decs_thm` as saved, exports theory
`to_data_cv`, and reports 13m20s elapsed time with peak RSS 2,178,268 KiB.

## Failure and repair

The first failure was a record-eta case introduced by `Dopen`: opening a
module can return an arbitrary source environment, so the proof must establish
that the environment equals the record rebuilt from its `c` and `v`
components.  Rewriting with the existing proved theorem
`source_to_flatTheory.environment_component_equality` closes that case.

The second failure was positional branch drift in `compile_decs_thm`.  `Dopen`
adds one residual induction case, so the old sequence of branch tactics sent
the following `Dlocal` case to the wrong tactic.  One additional existing
`metis_tac[FST,SND,PAIR]` branch closes the new case.  The complete repaired
proof was first replayed against the saved failing HOL state and then checked
again by the clean target rebuild above.

## Audit boundary and next gate

This is compiler-proof and bootstrap evidence.  It is not Flyspeck S2 or S3
evidence, because no freshly linked Candle executable has yet run an
authenticated Flyspeck source stratum.  This classification follows the
central correction in `audit1.md`: the PFT run remains an independent oracle,
while verified direct-source execution is the primary v1.3 path.

The cached x64/64 `cake.S` bootstrap has resumed from the new commit with 16
theory targets remaining.  After it succeeds, the Candle manifest and artifact
provenance must be regenerated to pin `0c170aa...`; the executable must then be
linked and pass its compiled Dopen/runtime tests before the diagnostic direct
prefixes are admissible.  Any later proof failure remains an open Dopen
integration defect rather than a reason to weaken the source-execution gate.

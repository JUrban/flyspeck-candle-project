# Dopen soundness repair and bootstrap resumption

The integrated CakeML branch now proves declaration opening through the
semantic type-soundness path. The pinned head is
`a336d8493b1ffc81b3a93348c4326d6200cf2d78` on
`codex/flyspeck-v13-integration`.

The first full x64 bootstrap exposed an inherited exact-output weakening
statement that is false for `Dopen`: opening selects a slice of the input type
environment, so weakening that input can legitimately change the selected
schemes. Commit `d4ed3e6d5b811dd2457f46d494b30bc67b706fdf`
keeps the historical theorem only for recursively Dopen-free declarations.
The targeted `weakeningTheory.uo` build passed in 21 seconds at approximately
298 MiB peak RSS.

The next bootstrap frontier was `decs_type_sound_no_check`. Commit
`a336d8493b1ffc81b3a93348c4326d6200cf2d78` adds:

- a symmetric `nsAll2` opening theorem for a namespace selected on the
  relation's right side;
- constructor and value namespace relations through `open_tenv`;
- a semantic/type-environment opening correspondence;
- preservation of `type_sound_invariant` after extending both environments
  with the opened slice; and
- the corresponding `Dopen` case of declaration soundness.

The proof contains no cheat, admit, or replacement axiom. Targeted evidence is:

- `namespacePropsTheory.uo`: pass, 19.1 seconds, 472,744 KiB peak RSS;
- `typeSoundTheory.uo`: pass, 2 minutes 53 seconds, 839,832 KiB peak RSS;
- exported theorems include `decs_type_sound_no_check`, `decs_type_sound`, and
  `semantics_type_sound`.

The complete x64 `cake.S` bootstrap has been restarted from this repaired
proof graph. Its live transcript is
`flyspeck-candle-runs/v13-dopen-bootstrap-a336d8493.log`. This report records a
green soundness target and an active bootstrap; it does not claim that the
bootstrap or rebuilt-Candle corpus gate has passed yet.

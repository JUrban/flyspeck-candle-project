# v1.3 clean no-custom-FFI Candle executable — 2026-08-27

This checkpoint closes G4 for the exact pinned direct route by elimination.  It
does not close G1/G2, G7/G9, S1, S2, or S3.

## Clean build

An isolated worktree and branch were created at the selected Candle source:

- path: `/project/worktrees/candle-clean-build-v13`;
- branch: `codex/flyspeck-v13-clean-build`;
- source: `a08e551a4398907776112eb72db1573f65cf2012`;
- compiler archive SHA-256:
  `4e074090eb04d8fdfb62536457adb31c98e1b17f6cd66c9483806601251053b7`;
- reported CakeML source:
  `4e312c0f7e18b9c5789c8ac4e0af257bff895cf5`.

The selected build recipe patches only the compiler assembly.  It does not
apply the historical `basis_ffi.c.patch` or append `chdir_to_root.ml`.

The clean artifacts are:

| Artifact | SHA-256 |
| --- | --- |
| executable `candle/build/cake` | `c20b3ec65fc01b6f50a0101c706e18271920530030aa3c381a36ff0fdbd3b23f` |
| patched `cake.S` | `b55547fb4a7e4586a5503b7e7c2cb65fd850a82fe7ca2504d30fb31f7d350327` |
| `basis_ffi.c` | `04da4d58c97ad7ea649bf94c9d6f1af8313913078330442a901b501d5289c735` |
| selected generated boot | `55a0e08b515a06e9b047ccedb62eb98eb4808f3b45e9c4b11e13eac7f7abba94` |
| config | `162ea59dac4c00177530ecbdb7a5ad72a03460825c5160a8c596dd4f53a5f3cb` |

The clean `basis_ffi.c` is byte-identical to the member extracted from the
pinned compiler archive.  The selected Candle source inventory and generated
boot each contain zero `Runtime.customFFI` calls.  Dynamic-symbol and source
checks find no `chdir` or custom system handler in the clean executable/basis.

## Compiled regression

The clean executable passes:

- static `#load` and exact Flyspeck source-action boundaries;
- Filename, Digest, Str, and deterministic Unix metadata compatibility;
- fail-closed `Sys.command` behavior;
- immediate and allocated identity normalizations;
- parser normalizations;
- complete selected-source digest authentication and corruption rejection;
- non-full build-mode rejection before source loading.

It also passes the full direct-loader frontier gate with the nine-output overlay
and prepared LP input.  The durable log is:

`/project/flyspeck-candle-runs/v13-direct-clean-noffi-frontier.log`

with SHA-256
`ffa64df26d4fbf015d67dc189fb8b8f509686c364a26d5c9b6e0cf8bb0eb8d3b`.
The log exposes the exact 39-certificate list, completes normalized
`general/parser_verbose.hl`, and stops at the unchanged verified-Dopen gap in
`general/debug.hl:16`.  It contains neither the full-run success marker nor a
completed debug source action.

## Gate disposition

G4 is `not required` for this exact route: no custom call or custom C handler
remains to specify or refine.  The historical `chdir` and `system` ledger
entries are retained with `outside_dependency_closure`/`deferred` disposition.
Any source, generated-boot, build-recipe, C-basis, executable, or manifest drift
that reintroduces a call or handler reopens G4.

The 39 LP certificates have not yet executed because verified Dopen remains the
earlier frontier.  Their complete execution, negative mutations, and semantic
fingerprints remain open G7/G9 obligations and are not hidden by the G4
elimination result.

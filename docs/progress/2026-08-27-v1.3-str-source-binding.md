# 2026-08-27 — pure direct-source `Str` compatibility

## Result

Candle loader branch `codex/flyspeck-v13-loader` at
`eed3d0952a01f17d792c57d31f298310edf3e885` supplies all five `Str` members
named by the direct-source contract as pure Candle source.  The implementation
does not dynamically load `str.cma` and introduces no FFI.  It is partial
semantic evidence, not permission to erase `#load` directives and not an
S1/S2/S3 acceptance result.

The governing Flyspeck source remains the unmodified upstream commit
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`; no PFT source is used.

## Inventory correction

The independent static-library audit found that qualified-name scanning alone
missed three calls after `open Str` in
`formal_lp/hypermap/computations/list_hypermap_computations.hl`.  The manifest
now records:

- 41 qualified uses;
- two reachable `open Str` sites and no reachable `open Unix`;
- three reviewed open-attributed `Str` candidates; and
- 44 total capability occurrences: 25 `Str`, 19 `Unix`.

Open-attributed records carry their opening line and the status
`lexical-reviewed-not-compiler-proved`.  Both unknown library names and unknown
qualified/opened members block promotion.  This corrects, rather than hides,
the earlier 41-only ledger.

## Pure implementation and boundary

`candle/ocaml.ml` implements `regexp`, `split`, `string_match`,
`global_replace`, and `first_chars`.  The regular-expression subset covers all
literal forms in the selected graph: literals, dot, character classes with
negation/ranges, `*`, `+`, `?`, and start/end anchors.  Splitting preserves
interior empty fields and trims edge empties like OCaml 4.14.1.

Unsupported escaped grouping, alternation, back-references, replacement
back-references, and empty-match global replacement fail explicitly.  One
loaded definition constructs `".*" ^ s` from an argument, so arbitrary dynamic
regex behavior is not claimed.  The manifest therefore labels `str.cma`
`partial-pure-source-differential-gate`; `unix.cma` remains `unimplemented`,
and the overall contract remains
`blocked-pending-static-binding-evidence`.

## Evidence

The differential test runs the same cases under OCaml 4.14.1's `Str` and a
compiled Candle compatibility preload.  It covers every selected literal
pattern family, offset and negative matching, negated/ranged classes, maximal
delimiter runs, adjacent single-character delimiters, underscore deletion, and
prefix extraction.  It also checks explicit rejection of unsupported grouping.

```text
PASS: pure Candle Str compatibility matches OCaml 4.14.1 oracle
Ran 15 tests
OK
EXPECTED GAP: full loader stops at allowlisted #load compatibility
```

The final line is important: after loading the new source through the full HOL
startup, the clean direct loader still stops at the original
`text_formalization/build/strictbuild.hl:21` `#load "unix.cma";;`.  No generic
directive no-op or source bypass was introduced.

The regenerated manifest has 297 ordered roots, 399 source nodes, 419 selected
edges, 42 generated inputs, no blocking diagnostics, and SHA-256
`dffb3bed57b5904ce6da68545a0daae75b82a8a723de0025a27d662d49b13f1d`.

## Next gate

The release path should next eliminate immediate `Unix` needs by replacing
strictbuild's `date`/`whoami` process probe and the source's placeholder
`putenv` mutations with deterministic loader-owned data.  GLPK process helpers
must be proven unnecessary for theorem checking or receive a narrow,
sandboxed executable/argument/channel contract; they must not be backed by an
unrestricted shell compatibility layer.

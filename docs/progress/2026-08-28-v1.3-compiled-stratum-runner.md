# Provenance-bound compiled stratum runner — 2026-08-28

## Outcome

Candle integration commit
`c5ede8af32104be16d7c8bdbd2e683ae811c6557` adds the execution half of the
authenticated cumulative stratum plan.  The runner selects one of the eight
boundaries and always replays its cumulative prefix from action zero in a new
compiled Candle process.

Before preparing an attempt it requires the linked CakeML provenance record,
an exact clean linked Candle commit, the pinned clean Flyspeck commit, the
exact plan and materialization hashes, all 400 source nodes, all 16 normalized
outputs, all 43 generated inputs, both deterministic process inputs, and the
selected prefix.  It also authenticates the generated 399-entry in-process
MD5 preflight program.

The selected prefix is instrumented with one output-only phrase immediately
after every exact `#flyspeck_needs` action.  The runner requires all action
markers exactly once and in order, a final in-process boundary check, no
pending source identity, every selected identity in HOL Light's loaded-file
ledger, zero process exit, and no top-level error.  A new attempt directory
retains the config, instrumented program, stdin, log, initial running record,
and final success/failure receipt.  A timeout terminates the whole process
group and is recorded as failure.

## Validation

All 98 Candle host tests pass.  The new runner's five tests cover exact
instrumentation order, target drift, duplicate/missing/reordered markers, and
top-level error rejection.  A full plan/input reauthentication dry run took
about 3.5 seconds; it reached the intentional linked-provenance prerequisite.
The linked record does not exist yet because the verified x64-64 CakeML
bootstrap is still running.

The locked runner/setup/check SHA-256 values are respectively:

- `580b53b6c0dacd89df4472bcd2b83183cbcbd6a4e779e90e7128051262f5453f`;
- `74abac96240f9c2f36b2c74fd4ef47ea2d12f574e1b77e3dd63e36db75e9f168`;
- `2348eea910b74e4f33257327c6f99b652f3737dc1c55f3f5ec8dcd6c7c873c77`.

## Assurance boundary

A successful attempt receipt establishes only the authenticated execution of
the selected cumulative source actions.  It is not S2/S3 evidence by itself.
Promotion still requires the approved semantic fingerprints, complete
LP/nonlinear coverage and assumption accounting, and repeatability required by
the release contract.  PFT remains a comparison/debugging oracle and cannot
supply those direct-source obligations.

# Direct nonlinear fatal guards — 2026-08-28

## Outcome

Candle integration commit
`aac2a6ab4498574f805af0771e98abec996bb27d` makes three previously
non-aborting nonlinear comparisons fail closed:

- the 23,242-element `all_parameters` comparison in `break_case_exec.hl`;
- the `1f054717131cf915bd8cc95ab7b645c3` assumed-theorem surface digest;
- the `e607b9e5e7f4c495236c6546d6889963` final-theorem surface digest.

All use the existing `comparison || failwith message` idiom.  Successful
evaluation still computes each comparison once and returns the original
`true : bool`; false now aborts the containing source action before
neutralization, later actions, or checkpoint promotion.

## Scope and critical caveats

The 23,242 value counts the pre-`setify` diagnostic work-unit list used by
`get_nth`.  The later proof path shadows `all_idv` with a setified list, so the
fatal count is a partition-shape regression guard—not proof-leaf coverage,
category disjointness, or a multiplicity proof.  A count-preserving mutation
can evade it.

Both digest checks use `Serialization.simple_digest_thm`.  They bind serialized
hypotheses/conclusion surfaces (with deserialization hypotheses filtered), not
definition/proof history, generated-input provenance, leaf coverage, or the
permitted axiom set.  They therefore remain subordinate to complete compiled
reconstruction and exact structural/history fingerprints.  PFT cannot satisfy
those S2/S3 obligations.

The normalized outputs are:

- `break_case_exec.hl`: SHA-256
  `20ddb8a5a0610fb5920de48349f14bdc31a91465564d5d85b3ba4fadb2a5deec`;
- `mk_all_ineq.hl`: SHA-256
  `f24f418351f52214a3c257aba2ef40622f90ebd8fe088e5ed41352c4900f176f`.

## Authenticated integration artifacts

- direct manifest: `df24482a3a5a3fbcf07d0f95dd376b3aa9aa1a7841e78e7775331f18d40ae057`;
- 16-entry normalization contract: `e24fd9785115aa261438682a3d049520a49e14af2ae831b3785bd51a1a36a5fb`;
- overlay receipt: `d2c967beef1f30eb55a3a48e6ff324042d288ff6286b81437a8f0bd340cdf4e8`;
- full-build driver: `b0b42a913de1032eeb6ebb1afc6e812aba68147a071a78e48289621c40bbc6d1`;
- source-digest program: `4b4d1793fd865bbf42924e0589dc2b76dbe6e9955ee3910f3087f5ce8f24ed65`;
- cumulative plan: `ca34246de6faf58a9b4e47586fd225938113f2930c6c271cc88f61e01179d224`;
- ordered-action digest: `4fda9469e478c7feaa67c9f507eeba4b5abae8901ed381772e720d1c73afd673`.

The exact action order remains 297 entries/eight cumulative boundaries; action
296 now records the final digest normalization.  The nested
`break_case_exec.hl` load is independently authenticated through the loader's
original-to-normalized mapping.

## Validation and remaining work

The 16-entry contract validates against pinned Flyspeck
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`, and all 93 Candle host tests
pass.  Independent audit confirmed that both digest bindings have no selected
consumers and that accepted bool types are preserved.  No nonlinear theorem or
certificate was executed for this report.

S3 still requires complete attempted/successful work-unit and leaf accounting,
no `TRUTH` sentinel, exact final theorem/hypothesis/axiom/history fingerprints,
all authenticated inputs, and an identical second clean compiled run.  The
ongoing CakeML bootstrap has already passed the Dopen-relevant parser target;
the first provenance-bound compiled nonlinear run remains pending.

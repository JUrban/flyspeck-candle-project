# v1.47: bounded certificate frontend repair

## Direct Flyspeck status

- Direct actions reached / passed remain **185 / 184**.
- The protected cumulative action-184 replay has passed LP terminal
  **3,507 / 9,080**.
- It and the two diagnostic shards remain CPU-active with no failing terminal.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## Preserved predecessor failure

The v17 nonlinear predecessor completed all seven Taylor segments and all ten
multivariate-Taylor segments.  It then failed before checkpoint readiness in
`formal_ineqs/verifier/certificate.hl:get_results0` with:

```text
Type mismatch between _65 list -> _65 list and
((string * int) list * result_tree) list * result_tree -> _76
```

The failed state remains preserved at
`cv-first-leaf-predecessor-checkpoint-v17/CHECKPOINT-FAILED`.  No partial state
was reused.

## Faithful reproducer and minimal repair

The exact `result_tree`, `mono_status`, and `get_results0` definitions reproduce
the error in a bare Candle frontend in 3.7 seconds.  The trigger is each branch
that places

```ocaml
acc' @ [rev p, r]
```

directly inside a returned triple.  Parenthesizing the tuple and adding full
function type annotations did not fix it.  Binding the unchanged pure append
immediately before the triple does:

```ocaml
let acc'' = acc' @ [rev p, r] in
false, acc'', Result_glue (...)
```

The repair is confined to the two symmetric branches.  It preserves append
order, tree identity, result shape, effects, and exceptions.  No Flyspeck
source byte was edited: the authenticated nonlinear closure now records the
two exact source-to-overlay operations.

Candle commit `5b78274` contains the repair, the regenerated closure and both
dependent first-leaf target receipts, and fixture updates pinning the nonlinear
tests to the dedicated frozen Flyspeck worktree.  All **68** focused nonlinear
closure, parser, segmentation, target, controller, and checkpoint tests pass.
All three closure/target generators also pass their fail-closed `--check`
modes.

## Fresh authenticated replay

The replacement predecessor bundle is:

```text
/project/flyspeck-candle-runs/
  cv-first-leaf-predecessor-checkpoint-v18
```

Its receipt records Candle head `5b7827484ab59e769ae2e20f24928e618d9ad207`,
the unchanged Flyspeck head `d6cdcd9f3e45f4ceb5fd1ba08f5b5ec4ce6a65eb`,
93 authenticated source nodes, and driver SHA-256
`6dbb5eb8c5e7488451fb342f21c24f86b080793db7b93eb6b6e0c6068edb3a27`.
The guarded replay is CPU-active.  It will emit a checkpoint only after the
single first-leaf-support readiness marker and after rechecking all recorded
input identities.

## Analytic-bridge impact

This failure and repair are independent of the universal analytic theorem.
The intended scalable boundary remains:

- one constructor-level calculus/smoothness theorem for every valid AST in the
  supported language;
- one generic vector `partial`/`partial2` and Taylor-checker handoff;
- per expression, only mechanical reification/source equality, validity and
  domain certificates, box data, and computed acceptance; and
- no expression-specific derivative or Taylor-remainder development.

The committed symbolic derivative compiler remains the executable baseline
while v18 rebuilds the real nonlinear predecessor state.

# S1 reference provenance re-review — 2026-08-28

## Outcome

The schema-v2 `100/gcd` observation remains unapproved. Independent review
found its theorem and global-axiom record internally coherent, but rejected
promotion because the runtime trust closure was incomplete, the reference
kernel was PFT-instrumented, no Candle differential fingerprint existed, and
offline validation did not replay all evidence artifacts. Approved identity
coverage therefore remains 0/65.

Candle commit `efa0cab334de4831ac2d202349ba808b01057308` implements a
schema-v3 collector that closes the identified provenance mechanics without
adding an approval path. The 109-test host suite passes.

## Schema-v3 closure

The collector now pins the `ocaml-hol` shebang interpreter, ignored generated
`hol_loader.cmo` and `pa_j.cmo` objects, `load_camlp5_topfind.ml`, a versioned
whole-tree inventory of `/usr/lib/ocaml` covering topfind/findlib/Camlp5, the
runtime-stub tree, and the recursive ELF closure including GMP. It records the
collector repository commit/status and refuses execution unless the collector
is exactly the clean committed HEAD version. Current pins are recomputed before
and after collection.

Candidates bind the canonical plan, exact request, and complete transcript by
SHA-256. Offline validation requires all three artifacts, reconstructs the
candidate byte-for-byte, and rechecks the current runtime and source closure.
The artifact remains `candidate_unapproved`, sets `promotion_allowed` to false,
and cannot be converted automatically to `EXPECTED_IDENTITIES`.

## Pristine-reference plan

A separate clean worktree and branch now pin pristine upstream HOL Light
`433477862bb90b328a593e012e09390e99b2439b`. Its runtime and generated boot
objects were built within that worktree using OCaml 4.14.1 and the pinned local
Zarith stub. The clean, not-yet-executed plan artifacts are:

- `flyspeck-candle-runs/s1-reference-gcd-pristine-v3-plan.json`, SHA-256
  `d9a3bbe4ce11458ef5f30bc5bbac06a38ab11e84111da054611b49255e14d314`;
- `flyspeck-candle-runs/s1-reference-gcd-pristine-v3-request.ml`, SHA-256
  `5120a84e200be58a323c6ea1c673dc5da695fdf6980170318f40239437292635`.

The plan records pristine reference status, clean collector head `efa0cab`,
5,720 OCaml-library inventory entries, four resolved dynamic libraries, and
the exact runtime/boot artifacts. It is planning evidence only. Execution is
deferred until the concurrent CakeML bootstrap releases its current memory
peak.

Even an identical pristine observation will remain unapproved until a linked
verified Candle runtime emits the same complete serializer record in a fresh
differential run. This report does not advance S1, S2, or S3.

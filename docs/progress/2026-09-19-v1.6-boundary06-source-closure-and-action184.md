# Direct Flyspeck: boundary-06 source closure and cumulative action 184

Status: DEVELOPMENT / NON-RELEASE. This report makes no S1, S2, S3,
qualification, promotion, or release claim.

## Headline Flyspeck progress

- Actual cumulative direct Flyspeck actions reached: **185** (actions
  000--184).
- Actual cumulative direct Flyspeck actions passed: **184** (actions
  000--183).
- Current cumulative boundary: `05-lp_support-through-184`.
- Current action: 184,
  `text_formalization/tame/linear_programming_results.hl`.
- Action 184 completed its 301-definition setup and entered the real pinned LP
  certificate corpus. At this report point it was proving `hard_1.dat`, at
  terminal **14 / 2899** of its first certificate.
- Focused source compatibility is green for every later action **185--290**.
  This is checkpoint-accelerated development evidence and grants no cumulative
  action credit past 184.
- Remaining compatibility failure classes: none known through source 290.
- Full direct S2: open.
- Nonlinear plus LP / S3: open; complete action-184 execution over all 39
  pinned shards remains the active gate.

## Cumulative checkpoint transitions

Run
`/project/flyspeck-candle-runs/v220-focused-post164-through170-current-checkpoint-v66-001`
finished all 19,715 action-170 archive cases, committed action 170, reached a
clean 171-event boundary, and was kill-checkpointed. The completed checkpoint
has SHA-256
`74d36b16d6000ad3b6daacb7e51659992afe018e7c4b62a1da0160443be72c4e`.
No exception or error marker occurs in its log.

Run
`/project/flyspeck-candle-runs/v236-cumulative-post170-through183-checkpoint-v66-001`
restarted a freshly hash-verified reflink copy with phrase-free input. It
authenticated the restored 171-event boundary, passed actions 171--183 with
thirteen normal action commits, and reached the clean 184-event pre-action-184
barrier. Its 153,975-byte log has SHA-256
`3e8b4461e7b980d71c0c636a21b6f225babbfae0a10cea5be7cc5a4f70ca5001`.
The completed pre-action-184 checkpoint has SHA-256
`73cb5d14242faa8492fbe2513e79f46a28556f6aa21df808f4859309f9992f05`.

The original monitor's two-minute durable-state polling window expired while
DMTCP was finishing the 3.18 GB temporary checkpoint. The write then completed
normally: the temporary file was atomically replaced, both Candle and timeout
processes were gone, and no `.temp` file remained. A second invocation of the
same fail-closed monitor recorded
`V220_PRE184_CHECKPOINT_COMPLETE` against that stopped state. This was a
monitor deadline issue, not a Flyspeck or checkpoint-content failure.

Run
`/project/flyspeck-candle-runs/v237-cumulative-action184-v66-001`
restarted a fresh copy whose hash matched the pre-action-184 source checkpoint,
authenticated the separately pinned action-184 file and phrase-free input,
and entered actual LP proof execution. Its own source prints that this stage
can take more than fifteen hours. It is the only Candle proof run left active;
older superseded computations were retired rather than allowed to duplicate
work.

To avoid repeating that LP corpus if later sources need another iteration, an
isolated monitor waits for the source's own
`The final linear programming result is constructed` report. That report is
after all 39 certificate shards and the 19,715-entry combined conversion have
returned. The monitor then requests a kill-checkpoint before the small final
theorem-packaging tail returns. Its SHA-256 is
`824c746d46eeba38a8d7515d3c8242f150fd870eb52b16ff9ac110502eb7817f`.
The live process input is unchanged; a future fresh checkpoint copy can resume
the retained tail and action-184 commit before reading a separately pinned
continuation.

That prepared continuation contains exactly 112 action directives and commits,
indices 185--296. An independent manifest-derived verifier recomputed every
target, repository/source identity, SHA-256, MD5, and complete logical-ledger
delta SHA-256; it also checked the clean 185-event input boundary, 291- and
297-event closing gates, normalized ZLZTHIC bytes, and final L2 target. The
continuation SHA-256 is
`1d7a8e1adb132893092bba1e319491ec575ce8039cd5f5cc6579f07419880878`;
its future restart input SHA-256 is
`d85237632d1e713745f59ffcdb242cd45b418ef92e9ef8f0d59f559fef8404ff`.
These are preparation identities, not evidence of completed cumulative actions.

## Focused boundary-06 source closure

Run
`/project/flyspeck-candle-runs/v234-focused-pre203-checkpoint-v66-001`
loaded unchanged actions 185--202 from a clean focused predecessor and then
stopped at a top-level pre-action-203 checkpoint. It emitted 18 source-success
markers. Its log SHA-256 is
`544d395603b41660b1c2987b348a79dce0d7f0f23da4f3dfd7470d4d2356fc93`;
the checkpoint SHA-256 is
`fe542aabc15b6921a05bfb880d264c9c7414c9d4f3cd2d1b4f047cb293956a32`.

An initial unchanged action-203 attempt failed inside theorem `XIV_ECAU` in
`text_formalization/local/ZLZTHIC.hl`. Its broad proof pushed eight ambient
assumptions through `ARITH_TAC`; unrelated higher-order hypotheses reached
`REAL_ARITH` and produced `Failure "dest_binop"`. Historical Flyspeck and
`fix-top100` searches found no patch for this exact site.

The replacement proves only the required local fact: membership in
`1..r+1` weakens to membership in `1..r+2`, while the retained disequality is
unchanged. A context-free MESON lemma uses `IN_NUMSEG` and the explicit
arithmetic implication `i <= r + 1 ==> i <= r + 2`; `ASM_REWRITE_TAC` then
supplies the three local premises. No theorem statement, hypothesis,
definition, proof intent, or axiom changes.

Run
`/project/flyspeck-candle-runs/v235-focused-zlzthic-local-bound-v66-001`
restarted a fresh copy of the pre-203 checkpoint, authenticated the candidate,
passed repaired action 203, and then passed every unchanged action 204--290.
It emitted 88 source-success markers and
`CANDLE_FLYSPECK_V231_BOUNDARY06_COMPLETE 185 290`, then exited normally. Its
log has SHA-256
`39525aef314c99c142f6b5e772d4f58a391d6613fbc66ff17be3717b4fc4740c`.

The two runs form one clean checkpoint lineage and establish focused source
compatibility for all 106 actions 185--290. Their predecessor deliberately
omits cumulative actions 170 and 184, so they do not establish cumulative S2
or LP/S3 evidence.

## Committed repair and reproducibility contract

The exact checkpoint-tested Flyspeck source bytes are committed on
`codex/action165-invf-direct-labeled` at `a76518cb`; their SHA-256 is
`3983453c53ae54210f877aa81a49048fda8c219bf30a122928f4876bb3dc317c`.

Candle commit `f13946a` adds a hash-pinned normalization from the untouched
source SHA-256
`ee0bbc735edc2796b580bf21b5243ed66a17d0d84e2d3808de4a58e38a026081`
to those exact tested bytes. It also regenerates the 400-source manifest and
static driver, updates the fail-closed loader's 53 selected normalization
identities, and pins the normalized MD5
`94685b843623c0918058a08cd8c984a8`.

Focused checks pass:

- normalization contract evaluation: 54 exact entries at pinned Flyspeck
  commit `1ce03530`;
- all 18 normalization tests;
- manifest fixed point: 297 roots, 400 source nodes, 43 generated inputs;
- all 29 manifest tests;
- whitespace/error checks.

No prover relink or action-zero replay was triggered by this one repair. The
next fresh action-zero replay remains reserved for the coherent successful
batch after action 184 closes.

## Next

1. Let the sole cumulative action-184 process finish the complete 39-shard LP
   corpus; preserve the first genuine proof failure or its post-construction
   checkpoint and normal action commit.
2. If the post-construction checkpoint completes, resume only a fresh verified
   copy to finish action 184 and cumulatively exercise the prepared 185--296
   continuation plus the direct L2 target.
3. Preserve the first genuine failure, or, if the cumulative continuation is
   green, run one fresh action-zero confirmation for the coherent repair batch
   rather than replaying the eleven-hour prefix after each repair.

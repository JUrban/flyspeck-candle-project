# Direct Flyspeck: action 098 confirmation and action 099 repair

Status: DEVELOPMENT / NON-RELEASE. This report makes no S1, S2, S3,
qualification, promotion, or release claim.

## Headline Flyspeck progress

- Fresh cumulative boundary-04 run: **100 actions reached / 99 passed**.
- Exact cumulative green prefix: actions **000--098**.
- Current cumulative boundary: `04-geometry-through-151`.
- First cumulative failure: action 099,
  `text_formalization/local/NKEZBFC.hl`.
- Focused clean-state development: actions **098--103 pass** after the action-099
  candidate; equivalently, the focused frontier has reached 104/104 through
  action 103. This focused result is diagnostic and does not increase the
  cumulative action metric.

The fresh action-zero run is
`/project/flyspeck-candle-runs/v50-dev-direct-boundary04-b0aede6-attempt-001`.
It ran from `2026-09-14T17:11:48Z` through `2026-09-14T18:16:58Z`, used one
CPU for 1:05:10 wall time, and reached a maximum resident size of 17,057,152
KiB. Its 13,441,201-byte log has SHA-256
`484ccf4d8ed05f8f57706ba087679a3501378e7e6520f4fca6a927f243752b95`.
The ordered action ledger contains exactly markers 000--098. In particular,
action 091 confirms the complete Marchal source from a fresh action-zero start,
and action 098 confirms the complete `local_lemmas1.hl` candidate from that
same clean start.

## Action 098: complete structure-effect normalization

The original action-098 failure was the Candle/CakeML structure-expression
type mismatch reported at the first anonymous theorem value. The cheapest
exact theorem-reference probe established that all four values independently
have type `thm`. A first candidate wrapped only those four values in wildcard
bindings; the complete source then failed at its later anonymous goal/tactic
sequence. That failed candidate is preserved in
`v48-local-lemmas1-full-candidate-post097-probe-001`.

The accepted candidate mechanically enumerates the complete anonymous
structure-expression set in the file:

- four theorem values;
- two calls of `g`;
- sixty calls of `e`.

Each of those 66 expressions is changed only from `expression;;` to
`let _ = expression;;`, in its original position and order. The expression,
effect, returned value, exception behavior, theorem statements, hypotheses,
and proof tactics are unchanged. The normalized source is 191,831 bytes,
MD5 `baaf863132dda96fa3ee962cbadb968f`, and SHA-256
`1ecd334b743330296e0824c1bf1bbc24b7de4ff722086914defb36e354db420f`.
A fresh post-action-097 focused run first established the final theorem, and
the new cumulative action-zero run now supplies the required independent
action-098 confirmation.

The cumulative run then attempted action 099 and reproduced the raw-source
failure exactly:

```text
ERROR: Type mismatch between thm and
  (term -> ... goal-state ...) -> _4 at line 1539
let lemma1=prove((parse_term "!A. ~A \\/ A"), SET_TAC[])
```

No action-099 success marker follows it.

## Action 099: minimized cause and correction

`upstream/fix-top100` contains no `NKEZBFC` source or patch, and the relevant
historical branches contain no pre-existing correction. The raw complete
source failure is preserved in
`v50-focused-action099-after-local-lemmas1-post097-probe-001`.

Two fresh-checkpoint minimizers establish that the apparent SET_TAC location
is not a MESON/search defect:

1. both Boolean tautologies pass when proved consecutively with `SET_TAC[]`;
2. they also pass under the exact `NKEZBFC` module-open sequence.

The mismatch is induced by the immediately following anonymous module
structure expression `g(NKEZBFC_concl2);;`, just as the action-098 mismatch was
induced by its raw structure effects. The narrow correction is therefore:

```ocaml
let _ = g(NKEZBFC_concl2);;
```

The call executes once on the identical term, at the identical position after
the two local tautologies and before `NKEZBFC_PREP`. The wildcard discards the
same returned goal-stack value while preserving the goal mutation, order, and
exceptions. It changes no SET_TAC/MESON implementation or invocation.

From a fresh immutable post-action-097 image copy, the complete corrected
`local_lemmas1.hl` plus complete corrected `NKEZBFC.hl` run establishes
`Nkezbfc_local.NKEZBFC_PREP`, emits one completion marker, and has no error or
exception. Its log SHA-256 is
`008aa4b74b1778a633de683301531d592ea263ff6d3f5d4e39b5357808886671`.
The normalized NKEZBFC source is 113,969 bytes, MD5
`1afcc4ab35e1b2a752f3c7890429c489`, and SHA-256
`b831405aa37284744c549aabfda51a764f6fd467b7d5275996f922bd3c3efc96`.

The correction and its fail-closed tests are committed as `d2da6d5`; the
regenerated 297-root/400-source authority and exact loader pin are committed
as `15d2cb5`. All 46 manifest/normalizer unit tests, the 61-entry normalization
contract check, and the independent manifest fixed-point check pass. The
Candle worktree is clean.

## Focused continuation and clean-state discipline

Fresh copies of the immutable post-action-097 image were used for every
candidate. No failed or partially loaded process was resumed. With the
action-099 correction in place, exact source probes also passed:

- action 100, `tame/arc_properties.hl`, through
  `Arc_properties.arc_lemma5`;
- action 101, `fan/CFYXFTY.hl`, through `Cfyxfty.CFYXFTY1`;
- action 102, `packing/YSSKQOY.hl`, through `Ysskqoy.YSSKQOY`;
- action 103, `packing/counting_spheres.hl`, through
  `Counting_spheres.XULJEPR`.

The counting-spheres trace's largest observed MESON search solved at 7,189
nodes. It showed no monotone runaway and therefore did not justify a native
versus Candle ordering/pointer-equality diversion.

To avoid replaying actions 098--102 on every later experiment, a new clean
development checkpoint was recreated from the immutable action-097 image and
the exact five-source sequence. Snapshot attempt 001 exposed and preserved a
bidirectional pipe deadlock in the harness; attempt 002 completed the image but
failed closed on DMTCP's post-checkpoint shutdown return code and issued no
receipt. Attempt 003 streams input/output concurrently and accepts shutdown
return 2 only after the checkpoint is complete and the target process is
demonstrably terminated.

The accepted attempt is
`v50-post-action102-focused-checkpoint-50d54fab2-15d2cb5-dev-003`. Before
capture it checked all five final values, found no parse/error/exception text,
observed the REPL sleeping in `pipe_read`, and recorded that this state has no
action-protocol credit. Its immutable mode-0444, single-link image is
2,798,220,229 bytes with SHA-256
`114ed1827dcd637ecd0a4c08cd1d52345fb6843bd9c06e73b94756cbf2ab5b87`.
The receipt SHA-256 is
`b5ceeb727b077911b9a3361c76f628f8026f24f430eb7153670ab348d75be11e`.

## Next

Action 104, `packing/REUHADY.hl`, is running from a fresh copy of the post-102
snapshot after replaying only action 103. Its existing broad SET_TAC site will
be investigated only if the real trace exhibits a runaway; historical direct
proof and module-scope work remain leads rather than authority. The next
cumulative action-zero replay will be launched after the current coherent
compatibility batch is known, so it can test all independently validated fixes
at once.

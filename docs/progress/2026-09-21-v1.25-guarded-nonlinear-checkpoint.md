# Direct Flyspeck and guarded nonlinear checkpoint preparation

Date: 2026-09-21 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still executing and has not earned
  its success marker.
- The protected direct process remains CPU-active in `hard_5.dat`. At the
  latest sample it had reached terminal **2,121/3,257**.
- PFT remains alive as the independent oracle and is not counted as direct S2
  or S3 evidence.
- No qualified Great100 runtime, source, report, transcript, semantic evidence,
  or other S1 byte was changed.

## The older closure attempts changed the checkpoint plan

The preserved v19 result ended after 17,317.897 seconds and 17,286.934 child
user seconds, with 1,204,224 KiB peak RSS. It completed all seven segmented
`Taylor_interval` chunks, then exited before the verifier-closure marker while
processing the still-unsegmented `multivariate_taylor-compiled.hl`. Its result
receipt confirms that this run used the seven-chunk Taylor transform only, not
the later combined analytic transform. It therefore remains a useful
localization result but cannot provide a reusable proof state.

The v4 experiment exposed a separate setup defect after about 68 minutes. It
loaded through the analytic Candle worktree, which predates the authenticated
central `abs_float` compatibility binding, and reported
`Undefined variable: abs_float`. Candle source errors are nonfatal to later
top-level phrases, so the old setup subsequently printed its independent ready
text and DMTCP captured the partially loaded state. That state is explicitly
quarantined by `ABORTED-INVALID-CLOSURE.md`, with the log, setup, and invalid
checkpoint hashes preserved. Only its four-process FIFO tree was stopped.

The immediate v5 replacement proved that readiness now fails closed: it was
started from the run directory, could not locate the linked runtime's
`config_enc_str.txt`, emitted no guarded ready marker, and produced no
checkpoint. Its input and failure log are preserved under
`cv-analytic-closure-checkpoint-v5`; the corrected v6 launcher starts the same
runtime from its authenticated link-output directory.

## Authenticated closure-only generator

Candle commit `9e575ec` adds a reusable closure-only input generator and makes
the verifier readiness contract indivisible. The loader identity check, an
actual reference to `M_verifier_main.verify_ineq`, and the ready marker now
occur in one top-level phrase. A failed `needs` can no longer be followed by a
misleading independent success print.

The generated v6 input is pinned to:

- 90 authenticated nonlinear closure source nodes;
- 29 materialized normalization overlays;
- both exact large-module segmentations: 7 Taylor chunks and 10 multivariate
  chunks;
- Candle source commit `9e575ecabbff33e9b506c07efd1ac8e95ebc0535`;
- driver SHA-256
  `8856ebdbc1bd184eeaf73c1e0d1cb5beca1d87f913bd1eda640940bae5d14df6`;
  and
- the unchanged development runtime SHA-256
  `3aaa248c0d5d6c96fa8e36445d1a945a751f0cf11a8b734a6142e9fab1088b2b`.

The checkpoint poller rejects any source-error marker, duplicate readiness,
multiple checkpoint files, temporary/incomplete checkpoint files, or an early
runtime exit. It requires a stable completed checkpoint and records the exact
input and log hashes before creating `CHECKPOINT-READY`. Sequential restores
are serialized, and each experiment still receives a fresh restored process.

Twenty-seven focused controller/segmentation tests pass in 59.258 seconds.
The exact split-composition kernel regression also passes in a separate fresh
Candle process. The v6 dual-segmented closure load is now CPU-active; it has not
yet earned a checkpoint claim.

## Proof-producing prototype status

The Flyspeck reconstruction source remains uncommitted until its combined HOL
test passes from a fresh v6 restore. Its intended proof boundary is unchanged:
host traversal builds a fail-closed ordinary pass/glue plan, each leaf must
return an assumption-free kernel theorem for the exact function and domain,
and one proved split-composition theorem derives the root. Synthetic tests
require the legacy and reconstructed conclusions to be alpha-equivalent and
the global axiom set to remain unchanged.

The measured 103.66x fine-grained reflected Taylor penalty remains a negative
result, so the first real profile compares native Taylor leaf proofs plus exact
once-only composition against legacy recursive composition. A staged reflected
leaf remains in the profile as a bounded measurement, not as the presumed
winner.

## Next gates

1. Let the protected action-184, PFT, and v6 closure processes continue.
2. Accept v6 only after the guarded marker and stable-checkpoint receipt are
   present with no source errors.
3. Restore a fresh v6 copy for the combined synthetic single/disjunctive proof
   test; do not continue from any failed restore.
4. If that passes, commit the Flyspeck adapter and profile the authenticated
   16-leaf disjunction, followed by the 1,061-leaf action-296 certificate.
5. Integrate only a measured theorem-interface-equivalent improvement.

# v1.43: dimension-generic polynomial source boundary

## Direct Flyspeck status

- Direct actions reached / passed remain **185 / 184**.
- The cumulative action-184 run is checking the 9,080-terminal `hard_5`
  certificate and has passed through terminal **3,266** after a protected
  checkpoint/restart under a fresh seven-day timeout.
- The unchanged-checker diagnostic high and middle shards have respectively
  passed terminal 8,144 descending and terminal 4,690 descending.  Neither
  shard has reported a failing terminal, but neither counts as cumulative
  action credit.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## Dimension-generic source semantics and compiler

Candle commit `12e3182` separates polynomial source semantics from the
two-coordinate numerical spike.  For every expression, arbitrary real
environment, and arbitrary initial stack, the kernel now proves:

```text
candle_poly_compile_real_run:
  !e env stack.
    candle_q_real_run env (candle_poly_compile e) stack =
    CONS (candle_poly_value_list env e) stack
```

The same commit adds `candle_poly_valid_dim nvars e` and proves that the old
two-variable validity predicate is precisely its specialization at dimension
2.  Focused tests establish that variable index 5 is valid at dimension 6 and
index 6 is rejected.  All generic theorems and specializations have empty
hypothesis lists.

The clean late-state replay is:

```text
/project/flyspeck-candle-runs/
  cv-polynomial-expression-jet-generic-v1-run-003
```

It ends with `CANDLE_CV_POLYNOMIAL_EXPR_JET_OK`; its Candle log SHA-256 is
`4f23eb0bcd3a4e8a45ff8e5ba1dc5179ba135c61e19958da59e5caa5bc52b733`.

Two preceding disposable restores found and isolated a late-state capture of
the existing topological constant `dimension`, followed by an incomplete
induction-tactic prelude.  The durable changes are respectively an explicit
`(nvars:num)` alpha-renaming and introduction/rewrite of the structural
induction hypotheses.  Neither correction changes a definition or target
theorem after elaboration.

## Mechanical source correspondence

Candle commit `b996e82` adds a strict theorem-producing reifier from supported
HOL real expressions to the polynomial AST.  It supports exact rationals,
variables, negation, addition, subtraction, multiplication, and natural powers
through exponent 16.  ML may select the AST, but the selection is accepted only
after the kernel proves all of the following:

1. the generated AST is valid for the supplied variable basis;
2. the AST's dimension-generic denotation equals the original HOL term;
3. compiling the AST produces exactly the independently reified postfix
   program; and
4. executing that program produces the original term.

The six-variable focused fixture produces closed validity, source-equality,
and execution theorems and a nine-instruction program.  Its clean replay is:

```text
/project/flyspeck-candle-runs/
  cv-polynomial-expression-reify-v1-run-002
```

It ends with `CANDLE_CV_POLYNOMIAL_EXPR_REIFY_OK`; its Candle log SHA-256 is
`c20cfa312113debf3c787a98ce7eb68920ee1e261d938a240a4fdea901fd3c20`.

This is the intended per-expression source boundary.  It produces a checked
equality mechanically; it does not construct an expression-specific
derivative, smoothness, or Taylor proof.

## First real nonlinear target is within this language

The authenticated first genuine target has three disjunct functions:

1. `cayleyR6u x1 ... x6 * -- &1`;
2. `unit6 x1 ... x6 * &10 + deltaL_x #4.0 x1 ... x6 * -- &1`;
3. `deltaL_x4 #4.0 x1 ... x6 * -- &1`.

Auditing their authoritative definitions shows that all three expand to exact
six-variable polynomials.  `deltaL_x` and `deltaL_x4` specialize the polynomial
definitions `delta_x` and `delta_x4`; no division, square root, trigonometric,
or other partial operator occurs.  The existing 16-leaf certificate therefore
does not require broadening the expression language before the first genuine
experiment.  It requires the numerical value/gradient/Hessian jet to become
dimension-parametric and the reusable Taylor theorem to be instantiated at six
variables.

A follow-on driver is queued to unfold and mechanically reify these exact
three source functions from a fresh nonlinear checkpoint copy.  It will run
only after the universal two-variable analytic bridge passes.

## Analytic bridge and loader status

The authenticated nonlinear predecessor remains healthy.  It has completed
all seven `taylor_interval` segments and entered multivariate-Taylor segment 2.
No source, proof, or loader error has appeared.

After that predecessor publishes its clean checkpoint, an automatically
chained pre-bridge checkpoint will load the committed numerical, soundness,
AST, and reifier libraries.  A fresh restore will then kernel-check the
universal Flyspeck `diff2c`/`partial`/`partial2` bridge and the generic
whole-box acceptance theorem.  Those bridge files remain uncommitted and are
not claimed as proved until that focused replay succeeds.

## Next bounded steps

1. Complete and, if necessary, repair the focused universal analytic-bridge
   replay from disposable clean checkpoint copies.
2. Run the exact three-function first-target reifier and record AST/program
   sizes plus closed source theorems.
3. Generalize the numerical jet representation to variable-length gradients
   and Hessians while retaining one reflected computation and one generic
   soundness theorem.
4. Check all 16 real certificate leaves with shared expression preparation,
   then compare complete recurring cost with the legacy verifier.
5. Extend the expression/domain language only when the next real target
   actually requires additional operators.

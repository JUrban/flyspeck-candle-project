# Direct Flyspeck: actions 126--143 compatibility

Status: DEVELOPMENT / NON-RELEASE. This report makes no S1, S2, S3,
qualification, promotion, or release claim.

## Headline Flyspeck progress

- Fresh cumulative boundary-04 execution: **131 actions reached / 131 passed**.
- Exact cumulative green prefix: actions **000--130 = 131/131**.
- Focused development frontier: **144 actions reached / 143 passed**.
- Current cumulative boundary: `04-geometry-through-151`.
- Latest completed focused raw failure after that prefix: action 143,
  `text_formalization/tame/ssreflect/sort-compiled.hl`, after focused actions
  131--142 pass from a clean cumulative predecessor copy. The independent raw
  action-144 probe is running from another clean copy.
- Focused corrected-source development passes complete actions 130, 131, and
  141--143 through terminal theorems `Ssrnat.sqrn_distn`,
  `Hypermap_iso.fan_hypermaps_iso2`, `Seq.allpairs_catr`, and
  `Seq2.prev_el_hd` plus `Sort.min_k0_le_EL`.
- Full direct S2: open.
- Nonlinear plus LP / S3: open.
- Current known compatibility failure classes: repaired action-126
  grouping/value-restriction issues plus the shared anonymous module-structure
  effect class in actions 128--131 and 141--143. Action-143 authority is now
  committed and action 144 is the active functional source.

The latest fresh action-zero replay is
`/project/flyspeck-candle-runs/v67-post-action130-cumulative-checkpoint-50d54fab2-5c35190-dev-001`.
It emitted exactly 131 nonce-bound ordered action markers for actions 000--130,
no action-131 load/evaluation, and one clean readiness marker before
checkpointing the waiting REPL. Its 14,536,615-byte log has SHA-256
`2a15fe334e98964b9ca8f68353d736a4a9b5401c9c1534f5c84de137119e1bee`.
The read-only 3,587,438,959-byte image has SHA-256
`3f69d01fc9ec2722f571ccc4ca1d83a7dcae6edfecc96c1e150269cce90eaad4`;
the snapshot receipt has SHA-256
`3080b3fcaec1a0d3689eadf738fd46fceba234d4d27cafc37e8053583781a8b8`.
This independently confirms actions **000--130 = 131/131** from action zero.
The adjacent fresh-probe harness verifies the immutable source-image receipt,
reflink-copy hash, input, log, and result image for every focused attempt.

The preceding fresh action-zero replay is
`/project/flyspeck-candle-runs/v66-post-action129-cumulative-checkpoint-50d54fab2-e11714c-dev-001`.
It emitted the exact nonce-bound ordered markers for actions 000--129, no
action-130 marker, and one clean readiness marker before checkpointing the
waiting REPL. Its 14,510,316-byte predecessor log has SHA-256
`8afde3bafc104431fe80fba2fdb56a4ae1423de7089a9a8abd0814a7bb6e3c38`.
The read-only 3,389,850,163-byte DMTCP image has SHA-256
`6ffa24d7b11308fa74664ff0e6fa26d2b207c6c8dc08f29b2cc0714fe3ca9949`;
the snapshot receipt has SHA-256
`a82b6214863f5a3b78cc4aedcb237aa1b4e032c678104f733d00fb75cbb1d104`.
This independently confirms actions **000--129 = 130/130**, including the
complete action-091 Marchal correction and all action-126/action-128/action-129
work, from a fresh action-zero state.

The preceding fresh action-zero run is
`/project/flyspeck-candle-runs/v65-post-action128-cumulative-checkpoint-50d54fab2-e0960fe-dev-001`.
It emitted the exact nonce-bound ordered markers for actions 000--128, no
action-129 marker, and one clean readiness marker before checkpointing the
waiting REPL. Its 14,493,265-byte log has SHA-256
`a2d48845b124494a37c8f5966a59604d1d8b33c50e885b5c8ea70ce16b2e09ca`.
The read-only 3,144,351,964-byte DMTCP image has SHA-256
`65a0c0c30a426c8ba9db48762316a904c50ea5d383d11fd251e3280a1bddc775`;
the snapshot receipt has SHA-256
`e28e2ea01338b25c482aa666d9b8aefcce3967c41c28f35d641c69f9057d5fbe`.
This independently confirmed actions **000--128 = 129/129**, including the
complete action-091 Marchal correction and all action-126/action-128 work.

The still earlier fresh action-zero run was
`/project/flyspeck-candle-runs/v57-dev-direct-boundary04-1de3868-attempt-001`.
It ran from `2026-09-14T22:02:51Z` through `2026-09-14T23:17:41Z`,
used one CPU for 1:14:50 wall time, and reached a maximum resident size of
17,086,720 KiB. Its 14,441,588-byte log has SHA-256
`c068460bff9fca3b35efda38a1265c2162ce2ccbbe97fb6f5ab046b6724f6de9`.
The ordered action ledger contains exactly actions 000--125. It independently
confirms the complete action-091 Marchal correction and all later compatibility
work through action 125 from a fresh action-zero state.

That earlier run selected and loaded action 126, then stopped before its success record
on the `spec_var_th` command-list expression. It emitted no boundary-success
record. It had previously superseded the 112/111 cumulative metric.

## Action 126: exact ssreflect language corrections

The raw failure is preserved in
`v57-focused-action126-ssreflect-post120-probe-001`, whose log has SHA-256
`b3080fc0e68ec5571518b2f65701915aaaa671c1b44fd7c7ab6b0570b218c585`.
The primary error was:

```text
Type mismatch between (_38 * term) list -> (_38 * term) list
and (string * term) list * thm -> _46
```

A typed decomposition localized it to the returned expression
`head1 @ tail, th0`. Parenthesizing the appended command list as
`((head1 @ tail), th0)` restores native OCaml grouping without changing
either component, evaluation order, or proof behavior. Once that expression
typed, CakeML exposed the independent top-level value restriction on
`cases_table` and `elim_table`. Exact
`(string, thm) Hashtbl.t` annotations state the types already fixed by their
immediately following inserts and change no table contents or lookups.

The normalized `ssreflect.hl` is 33,389 bytes, MD5
`620d9c7687482fd95245e71aae89a553`, and SHA-256
`c17436d7db382916371a31535853acb51ccd62efb72c56ce461b436c7fb5be37`.
An early concatenated actions-121--126 probe establishes those values but is
not clean acceptance evidence: a later uppercase-`EXCEPTION` audit found that
action 125's redundant dependency check did not see concatenated action 124 in
HOL Light's `loaded_files` bookkeeping. Its log SHA-256 is
`7261e908e187b560cce10d4afc7aac7dde9d39af97d98905ddcb3fb2fe6ebeb5`;
the unrelated harness exception is preserved rather than ignored.

Clean source-isolation acceptance comes from
`v64-focused-action128-all-effects-post120-probe-001`: it begins at action
126 from a fresh post-120 image, intentionally omits unrelated actions
121--125, and runs complete actions 126--128. It establishes
`spec_var_th`, both theorem tables, `Sections.finalize_theorem`, and the
action-128 terminal theorem with no `ERROR`, `EXCEPTION`, or parse-failure
text. The action-126 correction, regenerated authority, and authority fixed
point were committed as `97bfc33`, `f84b6d8`, and `80f2ae5`.

## Action 128: misleading theorem-line error resolved as module effects

The raw complete `jHOLLight/Examples/ssrfun-compiled.hl` source initially
reported:

```text
Type mismatch between thm and (string -> unit) -> _32 at line 68
```

The message points at the `eq_ext` theorem binding, but bounded probes showed
that its two branches, split tactic, composite tactic, and complete
`Sections.section_proof` all type and prove the intended theorem outside the
module. Adding type annotations around either branch or the composite tactic
did not change the complete-source failure. Factoring the same expressions
into globally typed tactic helpers also failed, ruling out the apparent
proof-expression explanation.

The next source expression is the anonymous effect
`Sections.begin_section "Injections";;`. Binding only that first effect moves
the error to the next anonymous expression and changes it to:

```text
Type mismatch between unit and unit -> _4 at line 75
```

That result is preserved in
`v64-focused-action128-first-effect-post120-probe-001`, log SHA-256
`ff8d18d1783a4ad3e3239032614fd2a518353dc8f539cce307f511639c8e6fd6`.
It confirms the same CakeML module-structure typing class seen in earlier
Flyspeck sources.

The complete source contains 31 anonymous structure expressions comprising
32 calls to `Sections.begin_section`, `end_section`, `add_section_var`,
and `add_section_hyp`. The accepted correction mechanically changes every
`expression;;` to `let _ = expression;;` at its original position. Every
call, argument, section name, mutation, result, exception, and evaluation order
is preserved. No theorem statement, hypothesis term, tactic, proof step,
SET_TAC/MESON implementation, search order, or axiom changes.

From a fresh post-action-120 copy, complete actions 126--128 with all 31
bindings establish the unchanged `Ssrfun.associative` theorem, emit one
completion marker, and contain no parse/error/exception text. The run is
`v64-focused-action128-all-effects-post120-probe-001`; its log SHA-256 is
`7e6ca45b343913aeca54ffd78b48878eab2d0eead58f2da526fc45d8a1d9235b`.
The normalized source is 21,756 bytes, MD5
`a8e5c90f3740bd1ab08645a1577bbf80`, and SHA-256
`c885eaaba8f2687edd27c363156832c659fb3ea3c3da8699a43597fab87b8556`.

The exact 31-expression normalization and its fail-closed contract assertions
are committed as `453527d`. Authority regeneration, the fixed point, and the
final 66-entry test expectation are committed as `e0d6c17`, `96daef5`,
and `e0960fe`. All 46 combined manifest/normalizer tests, the normalization
contract check, and the independent 297-root/400-source manifest fixed-point
check pass; the Candle worktree is clean.

The exact development overlay is
`v65-normalizations-ssrfun-e0960fe-dev-001`. Its contract SHA-256 is
`eb2280e0480c777d44c50ab8d9fb185643b2c7285fcdc202588acd0dcbeae49e`
and receipt SHA-256 is
`9c63296a7cd238a7128a41af4f3a4fb56c08a5ba046aa040741192b2491a5dd8`.
The corresponding 297-action plan is
`v65-stratum-plan-ssrfun-e0960fe-dev-001`, SHA-256
`d68cc2fdb492fce00beebb6b6a3ac5a85fa8c1da4157aad398788391c2fa8c01`.

## Action 129: the same module-effect class in ssrbool

The raw authenticated action-129 probe used a fresh copy of the immutable
post-action-128 cumulative image. It selected the exact unmodified
`jHOLLight/Examples/ssrbool-compiled.hl` source, but emitted neither the
action-success marker nor the terminal theorem marker. The genuine failure was:

```text
ERROR: Type mismatch between unit and unit -> _7 at line 10
```

The cited expression is `Sections.begin_section "ApplyIff"`. The run is
`v65-focused-action129-ssrbool-post128-cumulative-probe-001`; its input and log
SHA-256 values are respectively
`8254448e2a4b96f956b4e3afe0d86e2f9f1d015b1e0b3320d0a475c547aaf2b7`
and
`0344d024372fb60862afd9fd9447b0b2a34134e4e2bcc17f627415dc5fedeeef`.

Binding only the first anonymous effect did not repair the complete module: it
reported the same `unit` versus `unit -> _7` structure-group mismatch. The
complete source has exactly ten anonymous top-level structure expressions:
nine `Sections` expressions, comprising eleven calls, and one
`overload_interface` expression. The accepted candidate mechanically binds all
ten results with `let _ = ...` at their original positions. It preserves every
call, argument, state mutation, exception, result discard, and evaluation
order, and changes no theorem or tactic.

From another fresh image copy, the complete corrected source establishes
`Ssrbool.subrelUr`, emits exactly one terminal marker, and contains no
`ERROR`, `EXCEPTION`, or parsing-failure text. The run is
`v65-focused-action129-all-effects-post128-cumulative-probe-001`; its log
SHA-256 is
`fda635977a9cd8009610bb02a58826383916397e48403ea6f1431ed86e0166d8`.
The normalized source is 26,971 bytes, MD5
`ed5d8717c212edc3cfb7999131c8ecf7`, and SHA-256
`578d7fe281800b5fc5874ab6fa9cd499162034458be5ae75f5b3cf391023a99a`.

The exact action-129 normalization, authority regeneration, and fixed point
are committed as `689fd00`, `85bcc28`, and `e11714c`. All 46 combined
manifest/normalizer tests pass, as do the 67-entry normalization contract and
the independent 297-action/400-source manifest fixed-point check. The current
development overlay is
`v66-normalizations-ssrbool-e11714c-dev-001`, with contract SHA-256
`6c5e65799005f4aebef672ed73c55ce34ec25e5c60e75a47c3a45fcbfb1ef9c3`
and receipt SHA-256
`a0022357b72ff23ffbac1d956f603da84be7f58c9d3adcc95838519563c4471f`.
The corresponding exact plan is
`v66-stratum-plan-ssrbool-e11714c-dev-001`, SHA-256
`4c1894e85dbe514cea05ecaa444e2fb33325d082a39aa0fbcebf8bdbbd7d05d1`.

## Action 130: genuine Candle REPL parser failure

The raw action-130 probe used a fresh copy of the immutable post-action-129
cumulative image and selected the exact unmodified 148,083-byte
`jHOLLight/Examples/ssrnat-compiled.hl`, SHA-256
`66e533d777177cb4149e7f958af68cfa8e28e0be117537b8b3f81ba50071c188`.
Its already-loaded action-129 dependency resolved through the authenticated
normalization map, after which Candle reported:

```text
Expected to be at EOF
Parsing failed at line 6

module Ssrnat = struct
^^^^^^
```

The terminal check then independently found `Ssrnat.sqrn_distn` undefined.
There was no action-130 success marker. The run is
`v66-focused-action130-ssrnat-post129-cumulative-probe-001`; its input and log
SHA-256 values are respectively
`5327b5347177cdee0ff53fd019dce06f44b2bbcd8b0327fa048ffcf27bb698c7`
and
`f4cd23c750d969691bf4975eb5cec47d49457a48a8189f8340995b066d70375a`.
This is the first genuine failure after the fresh 130/130 prefix.

The earlier 400-source parser inventory does not contradict this result. That
diagnostic parsed a quotation-prepared derivative with the standalone
`parse_prog` path; direct execution loads the exact source incrementally via
the Candle REPL `use_file` path. A minimal ordinary named module parses in the
same post-action-129 environment, excluding the module declaration or module
name alone. Fresh-image binary probes localized the rejection from lines
2--1095 to lines 2--549, 2--276, 2--142, 2--71, 2--36, 2--22, and finally the
initial setup preceding `succnK`. Every complementary half parsed, and each
self-contained included lemma proved where dependencies permitted. `succnK`
itself also parsed and proved when separated from the setup.

The setup differential identifies the exact cause. `open Ssrbool`,
`overload_interface`, and `prioritize_num` each parse independently. The two
consecutive anonymous effect expressions are rejected as an enclosing-module
parser failure. Binding only `prioritize_num` retains that rejection; binding
only `overload_interface` advances to the known `unit` versus
`(unit -> unit) -> _2` structure-group type mismatch; binding both passes.

The complete source has eight anonymous module structure expressions: the two
initial effects, four `Sections` expressions comprising six calls, and the
final `parse_as_infix` plus `override_interface` expressions. The accepted
candidate mechanically prefixes each with `let _ =` at its original position.
Every call, argument, state mutation, exception, result discard, and evaluation
order is preserved. No theorem statement, tactic, proof step, SET_TAC/MESON
implementation, search order, or axiom changes.

From a fresh post-action-129 image copy, the complete 148,147-byte candidate,
MD5 `c50d0c69de3d905f10cfd2ba420ba9ce` and SHA-256
`acc6cbbfcf7ae49803cf236371c05332303b0bb49bb8a44cfb67d79295357d70`,
loads without parse/error/exception text and establishes the unchanged terminal
`Ssrnat.sqrn_distn` theorem. The run is
`v66-focused-action130-all-effects-post129-cumulative-probe-001`; its log
SHA-256 is
`8a34addffca14137ed39530019b3ce3a6cc1e4cf835af1ea97c7e199a345bcdc`.

The exact correction and regenerated authority are committed as `cbd8a38` and
`5c35190`. All 46 normalizer/manifest tests pass, as do the 68-entry
normalization contract and independent 297-action/400-source manifest fixed
point. The current development overlay is
`v67-normalizations-ssrnat-5c35190-dev-001`, with contract SHA-256
`1ac8207a07c0fdb35b66a426f6fa4fe1696570d622b1991ee687ad9bf93348ac`
and receipt SHA-256
`2f6f2f62689a46509da47f1dbfbbf347a7b31fd778c32795857febe8e1875c55`.
The corresponding exact plan is
`v67-stratum-plan-ssrnat-5c35190-dev-001`, SHA-256
`5f0c1d792dbe6700466b1f72bda6c7d2133a4b158d3d311cf8ee0eefde2c8097`.
This raw failure occurs before tactic execution and is not evidence of a
SET_TAC/MESON rule-order, node-rate, or pointer-equality problem.

## Action 131: hypermap_iso has the same module-effect incompatibility

The focused development frontier is now **132 actions reached / 131 passed**.
This does not yet award cumulative credit: the exact fresh action-zero replay
remains at 131 reached / 130 passed until it completes action 130 and captures
its clean successor state.

The raw action-131 probe used another fresh copy of the immutable post-action-129
image. A diagnostic overlay extension preserved the authenticated 67-entry
overlay, added only the already validated action-130 normalized identity, and
then loaded the exact unmodified 138,080-byte
`text_formalization/fan/hypermap_iso-compiled.hl`, SHA-256
`2da3701d76d46ae96406b71a5934611bb66b12d1bc0ac511e15ace0806523755`.
Action 130 established `Ssrnat.sqrn_distn`; raw action 131 then failed at the
module boundary with:

```text
Expected to be at EOF
Parsing failed at line 6
```

The run is `v67-focused-actions130-131-post129-cumulative-probe-001`.
Its input and log SHA-256 values are respectively
`e67db4accf1a85dc434b8425d08cc151575d483a9387f32d799d8f37b8099147`
and
`9198e69dbb4572c195b593cc6ed69d1c38b45ab2e47ab131293a0230c8fcdd3b`.
The later physical-loader-cache exception belongs to REPL recovery after the
module parse failure; it is not a separate source failure or a pass marker.

A complete static inventory found 46 anonymous module structure expressions:
one `parse_as_infix` registration and 45 `Sections` expressions executing 46
section calls. The candidate mechanically prefixes all 46 expressions with
`let _ =` at their original positions. It preserves all arguments, state
changes, exceptions, discarded results, and evaluation order, and changes no
definition, theorem statement, tactic, proof step, or axiom.

From a separate fresh post-action-129 image copy, the normalized action 130
followed by the complete corrected action 131 loads without parse/error text
and establishes the unchanged terminal theorem
`Hypermap_iso.fan_hypermaps_iso2`. The run is
`v67-focused-action131-all-effects-post129-cumulative-probe-001`; its combined
input and log SHA-256 values are respectively
`396935b1effc1f5499df8c76a445308560076350c11abf8e28ce489315c24dd0`
and
`2768e9b4b43e7d52cc4b82e89c8afc4ace89cf96b6fc258e87941955a61a4bc1`.
The corrected source is 138,448 bytes, MD5
`e8e2545fe119f5c727f6a67dbfb0c04e`, and SHA-256
`391c52e4d6ea15a8d1e540b1f3d1b170578adc768dc985c4777f74f9ae0e3bcc`.

No relevant source or patch exists on `fix-top100`; the historical Flyspeck
branches inspected share the same raw source blob. The exact 46-expression
contract and fail-closed assertions are committed as `40470ab`; authority
regeneration is committed as `d1b80a1`. All 46 combined normalizer/manifest
tests pass, as do the 69-entry normalization contract and the independent
297-action/400-source manifest fixed-point check. The immutable development
overlay is `v68-normalizations-hypermap-iso-d1b80a1-dev-001`, with contract
SHA-256
`fa221630bf8e47f8efd041a3e20961e5c2d5afca53b32dbcb97245d0c3b01c63`
and receipt SHA-256
`2dc67b92307a6ae1c17253a143201db7e819a050e80fbf9e80d80823cb03ff87`.
The corresponding exact plan is
`v68-stratum-plan-hypermap-iso-d1b80a1-dev-001`, SHA-256
`4c5a215b50b5dcdcd76f122bc0d815773e0d02a45d6730ca260631a3d7de6910`.
The fresh cumulative confirmation remains in progress.

## Actions 132--140: exact raw sources pass in order

After the corrected actions 130--131, the exact raw sources for actions
132--140 all pass in manifest order. Each frontier probe restores a distinct
fresh reflink copy of the immutable post-action-129 image and evaluates only
through its one new action. Each log contains every expected ordered loader
marker through its frontier, the frontier completion marker, exit status zero,
and no `ERROR`, `EXCEPTION`, or parsing-failure diagnostic.

| Action | Exact source | Focused log SHA-256 |
|---:|---|---|
| 132 | `tame/CKQOWSA_3.hl` | `c764e36f24420f9928d03fb43f0ca0edc64d1ad37695ee280575726c3c9fff3d` |
| 133 | `tame/CKQOWSA_4.hl` | `68b3bbdfa6177e9cad942f8dd0af241b88abdcecbf1c720f7c60be2de9e99748` |
| 134 | `tame/CKQOWSA.hl` | `2860bb40955f085b2fc8dbee87ae390c5307c53598b47e19b2d2a9705e99510a` |
| 135 | `tame/tame_general.hl` | `127445d89f4bcf291d81a67ffc5175b0ad201b2cad30e633e63620f727a492bb` |
| 136 | `tame/JGTDEBU.hl` | `eb5cdf7b35e3dbe134f2c13c8a4ab76b10c9c745fafaa11b77657fbd224723e9` |
| 137 | `tame/tame_opposite.hl` | `8bd3f702de54547434421e3e35db4f3e656f9140726b0bcd0c60a3163e1056ca` |
| 138 | `tame/FATUGPD.hl` | `b48c543501ea7bb5b57d47a852c88195891d9f206cd66595ee757a284d50ccc0` |
| 139 | `tame/CRTTXAT.hl` | `977674443a2823f663e722e37d8f559323ebba4276f680d2efb12b15c6f52fb1` |
| 140 | `tame/HRXEFDM.hl` | `a8f24411cb9bad57bd4a085a0cd24e86cc847ac72f21cfe04f50dcfb1f93abfd` |

The single anonymous `parse_as_infix` expression in action 136 passes raw.
That excludes the registration construct alone and further localizes the
earlier parser failures to module-expression grouping/interactions.

## Action 141: complete seq module-effect batch

From another fresh predecessor copy, actions 130--140 produced all eleven
ordered success markers. The exact unmodified 383,631-byte
`jHOLLight/Examples/seq-compiled.hl`, SHA-256
`ce054aa95c0bf0cb492ef11eb5291d085f7564eee98624564b75add785288b6a`,
was then selected but produced no action-141 marker:

```text
Expected to be at EOF
Parsing failed at line 7
module Seq = struct
^^^^^^
```

The later loader-cache exception and controller print occur during REPL
recovery and do not indicate success. The raw run is
`v68-focused-action141-post129-cumulative-probe-001`; its input and log
SHA-256 values are respectively
`a2834a7728bd5b7ebaab0148c65af46e7d104d297d482bc1eb7733959711e9b8`
and
`abfb1a45003bfe8b3741defc07a958232705e44f2859b0b0bca9751e11ce1e46`.

A complete static inventory found 119 anonymous structure expressions: 112
`Sections` expressions, two parser registrations, two interface overrides,
one overloadable declaration, one overload registration, and one
`prioritize_num` call. No relevant source or normalization exists on
`fix-top100` or the inspected historical Candle branches. The accepted
candidate prefixes each exact expression with `let _ =` in place. It preserves
all calls, arguments, effects, exceptions, discarded results, and evaluation
order, and changes no definition, theorem, tactic, proof step, search order, or
axiom.

The complete 384,583-byte candidate, MD5
`5bf90fc3f85e809c838cf9d9d03037dd` and SHA-256
`471600c15ac7fe30ae24a888e8690570639dcba136af9f95fc1388c65a3facdb`,
passes through the loader from a separate fresh state, emits all twelve ordered
markers for actions 130--141, and establishes terminal theorem
`Seq.allpairs_catr`. Its run is
`v68-focused-action141-all-effects-post129-cumulative-probe-001`; input and
log SHA-256 values are respectively
`2dbde7a0181828c08ee9e937f8c38c7922ca30e2bbfb4e3851f1abe231a99f45`
and
`37bf4f3e2cad5fd46adce2fce4b718a7408383b80f8183ff2cc2765cf3e83102`.
The 119-expression contract and its structural assertions are committed as
`26b39b2`; regenerated authority is committed as `7d40e94`. All 46 combined
normalizer/manifest tests pass, as do the 70-entry normalization contract and
the independent 297-action/400-source manifest fixed-point check. The
immutable overlay is `v69-normalizations-seq-7d40e94-dev-001`, with contract
SHA-256
`2b5f1efe9e1f756002e78d7558ccf8a1963774bf3c6c70e672b5bec99d605b5f`
and receipt SHA-256
`c740beb275e723cd7e9393121d0771e5d2c1153ef3d3ca5f8cf82150ac7b764e`.
The corresponding plan is `v69-stratum-plan-seq-7d40e94-dev-001`, SHA-256
`c62919c83d40beab5e4f96f378d10500705290f1a42a04de9264996c29953a4f`.

The exact committed authority was then confirmed from a fresh copy of the new
post-action-130 image. Actions 131--141 emitted all eleven ordered markers,
the terminal theorem exists, exit is zero, and there are no error diagnostics.
The run is `v69-focused-actions131-141-post130-cumulative-probe-001`; input and
log SHA-256 values are respectively
`5afc83c023230b781e2ea49cb8c029e547d826f481ca222e90283e59c15b479c`
and
`ba268f39af3ac338b680c325b674b9c8f0be5c876f3a897fff35f39e0e4c06d6`.

## Action 142: seq2 module effects

From another fresh post-action-130 copy, actions 131--141 emitted all expected
ordered markers. The exact unmodified 249,341-byte
`text_formalization/tame/ssreflect/seq2-compiled.hl`, SHA-256
`0a98ab2cb9847c23e0a78b4165d3ee7d911eb19a1392235c0263b6c7ac961bc6`,
was selected but produced no action-142 marker:

```text
Expected to be at EOF
Parsing failed at line 4
module Seq2 = struct
^^^^^^
```

The raw run is `v69-focused-action142-post130-cumulative-probe-001`;
its input and log SHA-256 values are respectively
`46b3981bcb3cf641a0b55994ffd4b3e2063a67c6c00d34748586531bceca9225`
and
`37ed64f9b9acc58142131895f21894391fec2792c41b31ffb83ba14c311d1d29`.
The later loader-cache exception and controller print are REPL recovery, not a
successful action.

The complete anonymous structure-expression inventory has 42 entries: 36
`Sections` expressions, two parser registrations, two interface overrides,
one overloadable declaration, and one overload registration. No relevant
`fix-top100` or historical Candle patch exists. The accepted candidate binds
all 42 results with `let _ =` at their exact positions, preserving every call,
argument, effect, exception, discarded result, and evaluation order without
changing mathematical content.

The complete 249,677-byte candidate, MD5
`5426c7e61a82a58b36fdf8f1b4ce31de` and SHA-256
`ecff65c2b053efb5a584bf42acf371544ec17ddfd29e069ea2ef591ee1a93de3`,
passes through the loader from a separate fresh post-action-130 copy. It emits
all twelve ordered markers for actions 131--142 and establishes terminal
theorem `Seq2.prev_el_hd`, with exit zero and no diagnostics. The run is
`v69-focused-action142-all-effects-post130-cumulative-probe-001`; input and
log SHA-256 values are respectively
`06804964bfb0fbc483e304ab32ac01545c6f57b433c3c50f73804cd7a5d044dc`
and
`972f2d928f52af4f664b27eac733eaa7a19623fd31282483354fc7e391c9100e`.
The exact 42-expression contract and its structural assertions are committed
as `22319dd`; regenerated authority is committed as `f8ddb80`. All 46 combined
tests pass, as do the 71-entry normalization contract and independent
297-action/400-source manifest fixed-point check. The immutable overlay is
`v70-normalizations-seq2-f8ddb80-dev-001`, with contract SHA-256
`c2267f5399036e522c08a2535c24e26ea0fdbd4f6d4ccc5e3545472869624c11`
and receipt SHA-256
`afb7cca0d91648bf30640c89b268dfedc5cf0ccfa4a2cfbccf77e4b77adfa9a5`.
The corresponding plan is `v70-stratum-plan-seq2-f8ddb80-dev-001`, SHA-256
`2b14126407e3cf1271718e744b17ad7c611f30fc602b461cc2fa24f8322d17ed`.

## Action 143: sort module grouping/type mismatch

From a fresh post-action-130 copy, corrected actions 131--142 emitted all
twelve ordered markers. The exact unmodified 98,705-byte
`text_formalization/tame/ssreflect/sort-compiled.hl`, SHA-256
`4276b638ad55dc99e9e61d729ae47fd8353d6867e589d5bfd52d6dd62d8ded93`,
was then selected but produced no action-143 marker. Its genuine error was:

```text
ERROR: Type mismatch between unit and (string * term -> unit) -> _1 at line 9
```

Line 9 is the anonymous `overload_interface ("++", ...)` registration in the
initial module-expression group. The raw run is
`v69-focused-action143-post130-cumulative-probe-001`; input and log SHA-256
values are respectively
`3388c2823ee9449eef007570c451572474a84748d007eb770a3fa50959f6fabf`
and
`c3d4414b047fd89027c9455669f1b10f8de53d87d164a6aaec6b97e6e26fdbd4`.

The complete inventory has 35 anonymous structure expressions: 28 `Sections`
expressions, two parser registrations, two interface overrides, one
overloadable declaration, one overload registration, and one
`prioritize_real` call. No relevant `fix-top100` or historical Candle patch
exists. The accepted candidate binds all 35 results with `let _ =` at their
exact positions, preserving all behavior and mathematical content.

The complete 98,985-byte candidate, MD5
`f2108691e64e667c9308b1ebdf3f8282` and SHA-256
`83207abe4b8b58219a51f230f51c59ec70983367d296280a01320eea98ea6cc9`,
passes from a separate fresh state. It emits all thirteen ordered markers for
actions 131--143 and establishes terminal theorem `Sort.min_k0_le_EL`, with
exit zero and no diagnostics. The run is
`v69-focused-action143-all-effects-post130-cumulative-probe-001`; input and
log SHA-256 values are respectively
`8fd2730a3365d6438d290c32ebac4aec96cb0318d52901ea76c761e31a3bcdfb`
and
`c22c266ed590a3614a802f713e97c49ab9acbfbfd58e813d7093699988910335`.
The exact 35-expression contract and its structural assertions are committed
as `2173758`; regenerated authority and its clean post-commit fixed point are
committed as `0a93e78`. All 46 combined normalizer/manifest tests pass, as do
the 72-entry normalization contract and independent 297-action/400-source
manifest check. The immutable overlay is
`v71-normalizations-sort-0a93e78-dev-001`, with contract SHA-256
`2c81530268fa1c1ca64c505887a106fdf9bb7fa310ac831ef7b4a46f7095562f`
and receipt SHA-256
`088b7152923a136801155fa7f4c7476005aaf28f08c804231afb772bccac2f4f`.
The corresponding plan is `v71-stratum-plan-sort-0a93e78-dev-001`, SHA-256
`fb0a0a3b9ea1cebe60053722f3a956548f95cd2c7cd0f2e1dab3523648c5778f`.

## Clean-state discipline and next step

Every action-126/action-128 candidate used a fresh reflink copy of the immutable
post-action-120 image. The post-action-128 state was then recreated by a fresh
action-zero replay and captured only after all 129 ordered records and clean
loader bookkeeping passed. Every action-129 candidate used a fresh copy of
that immutable image. No partially loaded or failed process was continued.

A new fresh action-zero replay on the exact `e11714c` authority completed
through action 129. It was hard-cut before action 130 and created the clean
pre-action-130 state only after actions 000--129 produced all 130 ordered
markers, the loader had no pending source identities, and the REPL was waiting
cleanly. The raw action-130 run and every minimization candidate use separate
fresh reflink copies of that immutable image. No partially loaded or failed
process is continued.

A fresh action-zero replay on the exact committed `5c35190` authority completed
through action 130. It was hard-cut before action 131 and awarded 131/131
cumulative credit only after actions 000--130 emitted all ordered markers,
loader bookkeeping was empty, and the REPL was waiting cleanly. The resulting
post-action-130 image is immutable and has its own hash-verifying fresh-probe
harness.

Every action-131--action-141 raw or corrected probe used a distinct fresh image
copy. The incremental controller inputs are hash-pinned, and each new raw
frontier is attempted only after all predecessor markers pass. No action after
the raw action-141 failure was evaluated in its failed process. The next step
was to confirm the exact action-141 authority from a fresh post-action-130
image; that confirmation is now green. Raw action 142 then failed in a new
copy, and its complete corrected candidate passed in another new copy. The
same discipline exposed and repaired raw action 143 without continuing either
failed process. Action-143 authority regeneration is now complete. Raw action
144 is running from a separate fresh copy; its mechanically enumerated module
effect candidate uses another independent copy. A later fresh action-zero
boundary replay remains required before cumulative credit can advance beyond
action 130.

The recent failures are grouping, value-restriction, and module-structure
compatibility defects rather than SET_TAC/MESON search failures. Native versus
Candle MESON rule ordering, early search choices/node counts, and pointer
equality remain the next cheap diagnostic fork only if similar MESON failures
begin to cluster.

# 2026-08-27 — exact local-open subset

## Outcome

The exact 400-node direct-source closure contains 79 parenthesized local opens
in 23 files and no `let open ... in` forms.  Inventory generator v2 now
classifies the body following each `M.(`.  All 79 selected occurrences are
operator references; none is a general expression.  The selected distribution
is 78 base-stratum occurrences plus `Lib.(--)` in LP support.

The generated closure artifact is
`compatibility/generated/direct-closure-summary.json`, SHA-256
`4c256fac8c953288e35a31773d50b12a7c3d918a072237743fff58b1a352e6eb`.
Its selected-site digest is
`3e9ba03631ab57e9fc0df6524630fe112222eb0f75e7a2df72b2b987e189a78f`.
It records 79 `operator_reference` bodies across 20 exact operator spellings
and zero `general_expression` bodies.

## Semantic boundary

`scripts/test-local-open-compatibility.sh` runs two bounded OCaml 4.14.1 versus
clean compiled Candle checks:

- `compatibility/oracles/open_local_parenthesized.ml` exercises simple and
  dotted module paths, custom symbolic operators, whitespace, and conflicting
  outer bindings.  Both frontends resolve the module operators and compute the
  same results.
- `compatibility/oracles/open_local_parenthesized_general.ml` demonstrates the
  broader boundary.  OCaml resolves `M.(value + 1)` as a local-open expression;
  Candle fails closed at `Undefined variable: Array.get`.

The broad repository inventory contains seven general-expression sites, but
all seven are outside the selected direct manifest.  This is not a claim of
general OCaml local-open compatibility.  The direct closure generator and
release lock require every selected occurrence to remain an operator reference;
introducing a general-expression body reopens the ledger entry.

## Gate effect

`PROJECT-LOCAL-OPEN-REVIEW-001` is resolved for the exact selected subset with
regressions `CANDLE-LOCAL-OPEN-OPERATOR-001` and
`CANDLE-LOCAL-OPEN-GENERAL-NEGATIVE-001`.  G2 as a whole remains in progress:
declaration `open`/Dopen and the other selected module/runtime categories are
separate obligations.

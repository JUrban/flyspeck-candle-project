# Pristine direct-reference semantic wire design (2026-08-30)

## Decision and evidence limit

The pristine reference output must use a new nonce-bound semantic subprotocol;
the existing bare `CANDLE_FINGERPRINT_V2` and
`CANDLE_STATE_FINGERPRINT_V2` lines are not sufficient as retained run
evidence.  They carry no run nonce or boundary, and the existing reusable
parsers do not require the four theorem records to occur in transcript order.

This design does not authenticate a process, source tree, serializer, parser,
or protocol module.  At this layer the parser establishes only internal wire
consistency and equality to claimed raw-plan/native records.  It creates no
candidate or comparison descriptor and does not approve S2, S3, PFT, or
release.  Parser integration remains prohibited until this design passes
exact-commit rereview and is rebased onto the independently accepted
exact-source authority repair at project commit `b0e24b9`.

## One exact marker namespace

The schema-v3 request owns the marker name
`CANDLE_PRISTINE_DIRECT_SEMANTIC_V3`.  Every line in the semantic session uses
that same name, is tab delimited, contains the plan's 64-lowercase-hex session
nonce, and has one of four exact record tags.  Metavariables below are shown in
angle brackets; angle brackets are not present on the wire.

```text
CANDLE_PRISTINE_DIRECT_SEMANTIC_V3\tTHEOREM\t<nonce>\t<index>\t<name-hex>\t<theorem-hex>\t<hypotheses-hex>\t<conclusion-hex>\t<global-axioms-hex>\t<hypothesis-count>\t<global-axiom-count>
CANDLE_PRISTINE_DIRECT_SEMANTIC_V3\tPOST_STATE\t<nonce>\t<kernel-state-hex>\t<type-constants-hex>\t<term-constants-hex>\t<definitions-hex>\t<global-axioms-hex>\t<type-count>\t<term-count>\t<definition-count>\t<global-axiom-count>
CANDLE_PRISTINE_DIRECT_SEMANTIC_V3\tDEPENDENCY\t<nonce>\t<index>\t<name-hex>\t<full-digest-md5>
CANDLE_PRISTINE_DIRECT_SEMANTIC_V3\tCOMPLETE\t<nonce>\t<boundary>\t<theorem-count>\t<dependency-count>
```

All hexadecimal fields are lowercase.  Serialized identity fields contain an
even, nonzero number of hexadecimal digits, names decode as strict ASCII,
SHA-256 is computed over the decoded serialized bytes, and
`full-digest-md5` is exactly 32 lowercase hexadecimal digits.  Decimals use
only `0|[1-9][0-9]*`, are at most 19 digits, and are converted only after that
lexical check.  The exact final boundary literal is
`07-final_assembly-through-296`.

The intended producer request emits the theorem and post-state payloads
directly from the functions in its claimed `candle/fingerprint.ml` source
rather than first emitting and then relabelling bare Great-100 marker lines.
The intended dependency values are returned by the claimed original
`Serialization.full_digest_thm` implementation.  The raw parser does not
establish either execution claim; the later held-process collector must do so.
The parser does not accept pre-hashed theorem or post-state identities: it
hashes the retained decoded payload bytes itself.

## Exact order and cardinality

There are exactly ten semantic lines, in this order:

1. four `THEOREM` records with indices 0 through 3 and the exact names
   `Linear_programming_results.linear_programming_results_th`,
   `Mk_all_ineq.the_nonlinear_inequalities`,
   `The_kepler_conjecture.tame_nonlinear_imp_kepler_conjecture`, and
   `Candle_flyspeck_l2.tame_imp_kepler_conjecture`;
2. one `POST_STATE` record;
3. four `DEPENDENCY` records with the same indices and theorem names; and
4. one `COMPLETE` record binding the exact final boundary and counts 4 and 4.

The semantic session occurs after all 297 action completions and after the
two terminal post-action native-load observations for the final target and
serializer.  It occurs before the existing reference-session completion
marker.  No native-load, action, LP, ordinary diagnostic, semantic, generic
success, or failure line may interrupt those ten lines.  No semantic marker
may occur elsewhere.  The reference-session completion marker remains the
last stdout line and is accepted only after the semantic terminal.

This positioning proves an observed ordering fact; it does not by itself
authenticate that the named sources were the executed sources.  The later
collector must independently authenticate the fixed parser, protocol,
request, serializer, Flyspeck source, runtime, and retained stdout bytes under
its held-lock execution protocol.

## Derived values

The immutable parser derives, rather than accepts:

- four theorem records by SHA-256 hashing the decoded theorem, hypothesis,
  conclusion, and global-axiom serializations;
- one structural post-state record by SHA-256 hashing its five decoded
  serializations;
- the invariant that every theorem and the post-state have exactly the same
  global-axiom identity and count three;
- four ordered dependency records;
- the ordered-request and ordered-dependency-record SHA-256 values using
  `direct_release_protocol.canonical_sha256`, namely UTF-8 JSON with sorted
  keys, separators `,` and `:`, and non-finite numbers forbidden;
- the common nonce-free semantic projection; and
- one exact raw semantic-completion observation bound to that projection.

The run-bound completion observation has this closed shape:

```text
{
  "schema": 1,
  "kind": "candle-flyspeck-pristine-direct-semantic-completion-observation-v1",
  "status": "complete-observed-unapproved",
  "role": "pristine-clean-reference",
  "reference_ordinal": <exact integer 1 or 2>,
  "nonce_kind": "reference-session-nonce-v1",
  "session_nonce": <the plan's 64-lowercase-hex nonce>,
  "boundary_id": "07-final_assembly-through-296",
  "plan": <canonical content record of the validated plan>,
  "request": <canonical content record of the validated request>,
  "transcript": <canonical content record of the rederived transcript>,
  "theorem_count": 4,
  "dependency_count": 4,
  "ordered_theorem_name_sha256": <direct_release_protocol.canonical_sha256(list(FINAL_THEOREM_NAMES))>,
  "semantic_projection": <canonical content record of the derived projection>,
  "approved_reference_present": false,
  "dependency_history_is_kernel_trace": false,
  "pft_used": false,
  "s2_s3_evidence": false
}
```

Here a canonical content record is exactly the existing
`pristine_direct_reference_protocol.content_record` result with fields
`bytes` and `sha256`.  The parser rederivation envelope carries both the
derived semantic projection and this run-bound observation.  The observation
is not a common projection and must never be consumed outside its exact
plan/request/transcript/rederivation closure.  It keeps
`cross_runtime_coverage = null`; the exact coverage status is
`not-derived-requires-authenticated-inventory-and-generated-inputs`.

This separation is necessary rather than merely conservative.  The five pure
parser inputs are stdout, stderr, the exact process result, the raw plan and
the request.  The plan binds the 400-source inventory and 43 generated inputs
only by named content records, so those payloads cannot be reconstructed from
their hashes.  Current cross-runtime coverage-v2 has no partial-coverage type
and requires the full inventory, generated inputs and inventory-backed logical
closure.  A held collector must open the separately retained canonical bytes,
match them to the plan records, validate their full public shapes, and only
then combine them with the parser result to construct and validate full
cross-runtime coverage-v2.  No parser-level source-closure or full-coverage
claim is permitted.

The parser implements the exact byte framing of `candle_s1_field`: a canonical
decimal byte length, one colon, and exactly that many payload bytes.  Before
integer conversion, every framed length and child-count token must match
`0|[1-9][0-9]*`, contain at most nine digits, denote a value no greater than
536,870,912, and, for a payload length, be no greater than the bytes remaining
in the already bounded field.  Conversion failures become protocol errors.
The parser never loops or allocates from an unchecked declared count.
Immediately after decoding a node's child count and before iteration or
allocation, it also requires
`child_count <= remaining_serialized_bytes // 2`, because even an empty
canonical child frame occupies two bytes (`0:`).  An outer `candle_s1_node` is
one framed tag, one framed canonical child count,
exactly that many framed children, and no trailing byte.  It must then enforce:

- each theorem composite is byte-for-byte
  `candle_s1_node "theorem" [hypotheses; conclusion]` reconstructed from the
  separately retained component fields;
- the kernel-state composite is byte-for-byte
  `candle_s1_node "kernel-state"
  [type_constants; term_constants; definitions; global_axioms]` reconstructed
  from the separately retained component fields;
- each hypotheses/type-constant/term-constant/definition/global-axiom
  component is an exact outer `candle_s1_node "list" [...]` whose declared
  child count equals both its actual number of framed children and the
  corresponding decimal count on the marker; and
- every theorem's global-axiom payload is raw-byte-identical to the post-state
  global-axiom payload, not merely equal after hashing.

Every theorem has zero hypotheses and the exact decoded empty-hypothesis wire
`4:list1:0`; its global-axiom count is three.  The post-state type, constant,
and definition counts are nonnegative canonical decimals and its global-axiom
count is three.  The serializer record is copied from the claimed raw-plan
authority only after the raw native closure has observed an equal content
record as its final post-action source.  This is internal raw-bundle equality,
not source or process authentication.

`Serialization.full_digest_thm` is an MD5 over sorted theorem-history MD5
strings.  It is neither collision-resistant evidence nor a kernel proof trace.
The derived dependency observation therefore keeps
`dependency_history_is_kernel_trace = false`, remains unauthenticated, and
cannot approve semantics without the later process authentication and
three-way comparison.

The parser result remains `authentication_status = not-authenticated`, has no
candidate, and keeps approval, promotion, PFT, S2, and S3 fields false.  A
later collector may promote these derived values into a raw candidate only
after replaying the complete retained bundle and reauthenticating every
authority record before and after the run.

## Rejection matrix

Hostile tests must reject at least:

- bare Great-100 fingerprint/state lines, old semantic versions, unknown
  semantic tags, extra fields, empty fields, spaces as delimiters, CRLF,
  missing final LF, non-ASCII bytes, and noncanonical decimals;
- missing, duplicate, reordered, interrupted, or post-terminal semantic
  records, wrong counts/boundary, and a semantic session before either
  terminal native-load observation;
- nonce drift in every record class, index gaps, theorem-name substitution,
  name aliases, non-ASCII name bytes, odd/uppercase/nonhex payloads, signed or
  padded counts, and overlong decimals;
- nonempty theorem hypotheses, a noncanonical empty-hypothesis serialization,
  a global-axiom count other than three, theorem-to-theorem axiom drift, and
  theorem-to-post-state axiom drift;
- theorem-only or kernel-composite-only splices, malformed/noncanonical length
  frames, list child-count mismatches, marker/list count mismatches, trailing
  serialized bytes, and shared-axiom payloads that differ before hashing;
- missing, duplicate, reordered, uppercase, or malformed dependency digests
  and a shared semantic `COMPLETE` record copied from another nonce or
  boundary;
- plan/serializer content drift, parser/protocol source drift, stale bytecode,
  `__file__` relabelling, PFT tokens, and any semantic/candidate/approval field
  accepted as producer input; and
- stdout larger than the exact plan/request cap 536,870,912 bytes before an
  in-memory semantic decode begins, nonempty stderr, a bool or drifted cap,
  and a nine-digit child count greater than `remaining_bytes // 2` before any
  child loop or allocation.

Collector tests in the later implementation must additionally reject graph
count/index/parent coercions, duplicate or unreferenced edges, role/capture
aliasing, wrong operation/file-type pairs, path aliases, symlinks, hard links,
swap-and-restore mutations, noncanonical or over-64-MiB control objects,
plan-relative input path drift, request-JSON/request-source substitution, a
surviving adopted descendant, a detached process-group writer,
supervisor/child PID or start-tick drift, descendant count/list drift, child or
descendant nonzero wait status, any writable file description outside the
child's exact stdout/stderr channels, public or wrongly owned attempt paths,
missing fsync or read-only seal facts, post-read append, rederivation replay
drift, and an envelope/candidate not content-bound by the terminal postflight
publication.

A same-shape substitution of one 32-hex dependency digest is intentionally not
claimed to be detectable by this passive grammar.  It changes the retained
stdout and derived projection.  The later held-process authenticator must
establish that the exact request computed the value, and the comparison must
require equality with the other authenticated runs.  Adding a producer-owned
terminal digest would provide only internal consistency because an attacker
able to rewrite the record can rewrite that digest too.

The semantic/output-cap extension is a disjoint raw-protocol migration.  The
five closed top-level plan, request, transcript, native-closure and candidate
artifacts move from schema 2 / `*-v2` kinds to schema 3 / `*-v3` kinds, the
source-rederivation envelope moves to `*-v3`, and the exact marker protocol and
its marker names move from `V2` to `V3`.  Schema-v2 values and V2 markers then
reject; embedded LP inventory/success subprotocols remain their separately
named schema 1.  The migration is required even though no v2 production bundle
exists, so the two exact shapes never share one durable schema identity.

The schema-v3 raw plan and request each add exactly three top-level fields:
`retained_stdout_max_bytes`, `retained_stderr_max_bytes`, and
`retained_input_artifact_root`.  The two cap values have exact Python type
`int`, not `bool`, and are respectively exactly `536870912` and `0`.  The root
is one exact canonical safe POSIX absolute path naming the authenticated input
artifact directory from which the two plan-relative control objects are
opened.  Request validation requires exact value equality with all three plan
fields.  They are not nested under authority, the marker contract or resource
limits.

The pure five-input parser has no publication descriptor and makes no file-
capture claim.  Before UTF-8 decoding, line splitting, hexadecimal decoding or
semantic framing, it requires exact immutable `bytes` and
`len(stdout) <= plan["retained_stdout_max_bytes"]`; it also requires the
already validated request caps to equal the plan caps.  Before hashing or
otherwise consuming stderr it likewise requires exact immutable `bytes` and
`len(stderr) <= plan["retained_stderr_max_bytes"]`, hence exact emptiness for a
successful run.  Its transcript, native closure and source-rederivation
envelope record content derived from those bytes but contain no `fresh_file`,
`nofollow`, path-identity or descriptor-stability fact.

The later collector anchors an absolute no-follow descriptor walk at `/`,
holds every directory descriptor and parent/child edge through validation, and
creates the plan, request JSON, generated request source, stdout, stderr and
source-rederivation basenames relative to their held parents with
`O_CREAT|O_EXCL|O_NOFOLLOW`.  All file descriptors must be ordinary by
`fstat`.  It performs bounded binary reads of at most 536,870,913 stdout bytes,
one stderr byte, and 67,108,865 bytes for each canonical control object and the
generated request source, rechecks the stable descriptors and every named
edge, and rejects oversize content before invoking a decoder.  Before any raw
candidate can exist it must publish a separate closed
`candle-flyspeck-pristine-direct-capture-envelope-v1` receipt.  That receipt
has exactly these top-level fields:

```text
{
  "schema": 1,
  "kind": "candle-flyspeck-pristine-direct-capture-envelope-v1",
  "status": "captured-unapproved",
  "plan_capture": <canonical-object capture record>,
  "request_capture": <canonical-object capture record>,
  "request_source_capture": <request-source byte capture record>,
  "process_result": {"exit_code": 0, "timed_out": false},
  "child_process_binding": <closed child/fd binding record>,
  "capture_namespace": <closed anchored namespace record>,
  "stdout_capture": <stdout capture record below>,
  "stderr_capture": <stderr capture record below>,
  "source_inventory_capture": <canonical-object capture record>,
  "generated_inputs_capture": <canonical-object capture record>,
  "source_rederivation_capture": <canonical-object capture record>,
  "envelope_publication": <closed anchored self-publication policy record>,
  "candidate_included": false,
  "approval_included": false,
  "pft_used": false,
  "s2_s3_evidence": false
}
```

The child-process binding has exactly `policy`, `supervisor_pid`,
`supervisor_proc_start_ticks`, `child_pid`, `child_proc_start_ticks`,
`child_process_group`, `child_session`, `child_wait_status`,
`subreaper_enabled`, `descendant_record_count`,
`ordered_descendant_record_sha256`, `descendant_records`,
`process_group_empty_after_wait`, `no_descendants_after_wait`,
`waited_child_pid`,
`request_source_child_fd`, `stdout_child_fd`, `stderr_child_fd`,
`request_source_child_fd_position`, `preexec_stop_wait_status`,
`preexec_handshake_complete`,
`request_source_namespace_edge_index`, `stdout_namespace_edge_index`,
`stderr_namespace_edge_index`, `writer_fd_closure_complete`, and
`fresh_exclusive_process`.  Its policy is
`fresh-exclusive-subreaper-child-held-stdio-descriptors-v1`; the supervisor
and child PIDs and Linux `/proc/<pid>/stat` start ticks are exact positive
integers and the two PIDs differ.  Every PID, process start tick, process group,
session, wait status, child fd, fd position, descendant index/count, and
namespace edge selector in the binding or any descendant record has exact
Python type `int`, never `bool`, before any range or equality comparison.
`waited_child_pid` equals `child_pid`; the process group and session equal that
PID; and `child_wait_status` is the raw value returned by
`waitpid(child_pid, ...)`, is zero, and derives the envelope process result.
The request-source, stdout and stderr child fd fields have exact values 0, 1
and 2; fd 0 position is zero.  `preexec_stop_wait_status` is the raw value
returned by `waitpid(child_pid, WUNTRACED)`, satisfies
`os.WIFSTOPPED`, and has `os.WSTOPSIG(...) == signal.SIGSTOP`.  The exact
non-bool edge selectors identify the corresponding terminal edges; and all six
boolean facts, including `preexec_handshake_complete`, are exact `true`.

The fresh exclusive collector enables Linux `PR_SET_CHILD_SUBREAPER` before it
has or launches any child.  Each adopted-descendant record, which excludes the
separately bound original child, has exact fields `index`, `pid`,
`proc_start_ticks`, and `wait_status`; the index equals list position,
PID/start ticks are exact positive integers, PIDs are unique, and `wait_status`
is the raw status returned when that identity is finally reaped with `waitpid`
and is zero.  Record count satisfies
`0 <= descendant_record_count == len(descendant_records) <= 4096`; every list
index equals its record's `index`; and the ordered digest is
`direct_release_protocol.canonical_sha256(descendant_records)`.  The collector
requires `PR_GET_CHILD_SUBREAPER == 1` after enabling the mode, uses
`waitid(..., WNOWAIT)` to retain start ticks before reaping, repeatedly checks
`/proc` ancestry, and cannot declare quiescence until the original process
group no longer exists, every adopted descendant has an exact successful wait
record, `/proc` contains no remaining descendant of the supervisor, and
nonblocking `waitid(P_ALL, ...)` reports no child.  A surviving, failed or
excess descendant fails the attempt.

Every supervisory request-source capture and revalidation uses bounded
position-independent `pread`, never `read` or `lseek` on an execution-channel
open-file description.  Independently of those capture descriptors, the
collector freshly opens a dedicated read-only request-source open-file
description for child stdin, verifies its descriptor identity against the
captured request source and its exact offset as zero, and neither reads, seeks,
nor duplicates it anywhere except to the identified child's fd 0.  The child
installs that descriptor as stdin and the exact publication write descriptors
as stdout/stderr, calls `setsid`, closes all other inheritable file descriptors,
and stops itself at a mandatory pre-exec handshake.  The supervisor accepts the
stop only from the same PID/start-tick identity through `waitpid(...,
WUNTRACED)`, verifies by `/proc/<child>/fd` identity and access mode that file
descriptors 0, 1 and 2 are the only intended channels, verifies from
`/proc/<child>/fdinfo/0` that fd 0's exact position is still zero, records the
exact raw stopped wait status, zero position and completed-handshake fact in the
child-process binding, and only then sends `SIGCONT`; the child immediately
executes the pinned
runtime without another user-space read or seek.  The supervisor does not
access the shared stdin open-file description until the identified child has
exited and refuses success until this descendant-drain fixed point and all
collector write descriptors are fsynced and closed.
`writer_fd_closure_complete` also requires a same-UID `/proc/*/fd` and
`fdinfo` scan to find no remaining writable description whose exact
`(st_dev, st_ino)` identifies either publication.  Inability to inspect a
same-UID descriptor fails closed.  Failure to establish quiescence fails
closed; a detached surviving writer cannot yield an envelope.

The namespace record has exactly `policy`, `anchor_path`, `anchor_identity`,
`anchor_opened_metadata`, `anchor_baseline_metadata`, `anchor_post_metadata`,
`collector_euid`, `collector_egid`, `attempt_root_edge_index`,
`creation_count`, `ordered_creation_sha256`, `creation_events`, `edge_count`,
`ordered_edge_sha256`, and `edges`.  Its policy is
`root-anchored-nofollow-openat-exclusive-publications-v1`; `anchor_path` is
`/`; and its identity and metadata are taken from the held root descriptor.
A descriptor identity has exactly integer `st_dev`, `st_ino`, and
`st_file_type`, where the last value is `stat.S_IFMT(st_mode)`; a descriptor
metadata record has exactly integer `st_mode`, `st_nlink`, `st_size`,
`st_mtime_ns`, `st_ctime_ns`, `st_uid`, and `st_gid`.  Every edge
record has exactly `index`, `parent_index`, `basename`, `object_kind`,
`open_operation`, `opened_descriptor_identity`, `opened_descriptor_metadata`,
`baseline_descriptor_metadata`, `initial_named_identity`,
`final_named_identity`, `final_descriptor_identity`,
`final_descriptor_metadata`, and `terminal_role`.
Directory edges walk every canonical absolute component.  Terminal ordinary-
file edges cover the exclusively created plan, request JSON, generated request
source, stdout, stderr and source-rederivation basenames and the no-follow-
opened source-inventory and generated-input files.

All graph and stat integers use exact type `int`, never `bool`; counts, capture
selectors, device numbers, modes, link counts, sizes, timestamps, user IDs and
group IDs are nonnegative, inode numbers are positive, parent selectors may
additionally be `-1`, and `1 <= edge_count <= 256`.  Collector effective user
and group IDs are exact non-bool integers and equal the corresponding IDs of
every fresh attempt/publication directory and collector-created file.
`edge_count` equals `len(edges)`; every edge `index` equals its zero-based list
position; parent index `-1` selects the anchor and every nonnegative parent
index is smaller than the child index and selects a directory edge.  Each
basename is one nonempty path component other than `.` or `..`, and every
`(parent_index, basename)` pair is unique.  Directory `terminal_role` is exact
`null`; the eight ordinary-file roles are exactly one each of `plan`,
`request`, `request_source`, `stdout`, `stderr`, `source_inventory`,
`generated_inputs`, and `source_rederivation`.  The eight capture selectors
are exact, distinct, point to the matching roles, and exhaust all ordinary-
file edges.  Every directory edge is an ancestor of at least one of those
eight terminals, so duplicate and unreferenced edges reject.

Aside from the exact anchor path `/`, every recorded path uses the exact
canonical POSIX absolute grammar: it is at most 4096 printable-ASCII
characters, starts with one `/`, has components of 1--255 printable-ASCII
characters, has no empty, `.`, or `..` component, contains no backslash or
alternate separator, and equals the `/`-joined basename chain obtained by
following its terminal's parent indices to the anchor.  The eight terminal
paths are distinct.  No `resolve`, symlink following, lexical alias, or
unrecorded path lookup participates in capture or validation.

`attempt_root_edge_index` is an exact non-bool selector for the unique held
directory edge naming the fresh attempt root.  That edge and every directory
at or below it in the six collector-publication paths have exact integer
`st_mode == 16832` (`stat.S_IFDIR | 0o700`), collector euid/egid ownership and
stable link count.  The collector sets umask `0o077` before creating the fresh
attempt tree and rejects any pre-existing or group/other-searchable
publication directory.  All six collector-created terminals are descendants
of this attempt-root edge.  The two separately retained input terminals may
have ancestors outside the attempt root, but the input files themselves are
exact read-only ordinary files as specified below.

All four opened/initial/final identities on an edge are exactly equal.
`object_kind` is exactly `directory` or `ordinary-file` and `st_file_type`
must equal `stat.S_IFDIR` or `stat.S_IFREG` respectively.  `open_operation` is
exactly `open-existing-nofollow-directory`,
`open-existing-nofollow-ordinary`, or
`create-exclusive-nofollow-ordinary`; its mapping is exact: directories use
the first, the two pre-existing input roles use the second, and the six
collector publications use the third.
The ordered digest is
`direct_release_protocol.canonical_sha256(edges)`.  Re-stat uses
`follow_symlinks=False` relative to the still-held parent.
`initial_named_identity` is the first no-follow named stat after the descriptor
is opened.  For an exclusive-create edge it follows successful creation and
descriptor `fstat`; no pre-creation identity is claimed, and `O_EXCL` fails the
attempt if the name existed.

There are exactly six creation events, in plan, request, request-source,
stdout, stderr, then source-rederivation order.  Each has
exactly `index`, `terminal_role`, `parent_index`, `basename`, `edge_index`,
`parent_metadata_before`, and `parent_metadata_after`; its exact typed
selectors and name identify a distinct matching exclusive-create edge;
creation count equals list length six, each event index equals list position,
and the roles equal the stated order.  For each affected parent, the first
event's before metadata equals the directory's opened metadata, every later
event's before metadata equals the prior event's after metadata, and the last
event's after metadata equals the directory's baseline metadata.  An
unaffected directory has equal opened and baseline metadata.  Only these six
retained transitions may change directory metadata.  After the
source-rederivation publication exists, the collector records every held
directory's `baseline_descriptor_metadata` (and the anchor baseline).  Final
directory and anchor metadata must equal that baseline;
existing-input opened, baseline, pre/post-read and final metadata must all be
equal.  Every publication has initial exact integer
`st_mode == 33152` (`stat.S_IFREG | 0o600`), collector euid/egid ownership,
link count one and size zero before any descriptor is exposed outside the
collector.  Plan, request and request source are written, fsynced, changed to
exact mode 0444, fsynced, reopened read-only and write-closed before child
launch; stdout/stderr are sealed after descendant drain; and the source
rederivation is sealed after parser replay.  Thus authorized creation
does not contradict stability, while any unrecorded mutation or swap-and-
restore changes the retained metadata chain and rejects.  Creation count is
exact non-bool integer 6 and the creation digest is
`direct_release_protocol.canonical_sha256(creation_events)`.

The closed stdout and stderr capture records each have exactly `path`,
`namespace_edge_index`, `bytes`, `sha256`, `retained_max_bytes`,
`bounded_read_max_bytes`, `descriptor_metadata_before_read`,
`descriptor_metadata_after_read`, `bound_check_performed_before_decode`, and
`within_bound`.
Paths are exact safe absolute publication paths and resolve lexically through
the recorded edges; each edge index selects the corresponding terminal
ordinary-file edge; pre/post-read metadata are exactly equal and equal the
edge's final descriptor metadata; byte count equals both the length read and
descriptor `st_size`; SHA-256 is computed from those same bytes; and two
honest exact `true` facts state that the bound check preceded all decode and
that the successful capture is within it.  Final metadata has exact integer
`st_mode == 33060` (`stat.S_IFREG | 0o444`) and link count one.  Stdout limits
are 536870912 and 536870913.  Stderr limits are 0 and 1, so any successful
nonempty stderr capture rejects without reading more than one byte.

After the exact child and supervised descendant tree are quiescent, the
collector fsyncs both publication write descriptors, changes each to exact
regular-file mode 0444,
requires `st_nlink == 1`, fsyncs again, closes every writable copy, fsyncs each
held parent directory, and retains only the already identity-matched read-only
descriptors.  It then performs the bounded reads and hashes above.  Immediately
before and after envelope and raw-candidate publication it re-stats every named
edge and held descriptor, rechecks mode/link/metadata, and boundedly re-reads
and hashes all eight terminal files from those held read-only descriptors.  It
also reruns the canonical validators and pure parser after each reread.  The
authority lock, anchored directory descriptors, input descriptors and
read-only publication descriptors remain held until the candidate is fsynced,
fchmoded to 0444, parent-fsynced, revalidated and content-bound to the
envelope, and the terminal capture-completion receipt is written, sealed and
validated in its current state.  The terminal does not self-attest its own
durable publication; that limitation and the required future outer postcondition
are specified below.
A failure at any checkpoint leaves only a failed-attempt artifact and no
consumable candidate.  This is a cooperative provenance boundary, not an atomic
defense against a hostile same-UID actor able to chmod or mutate arbitrary
files outside the supervised process tree; that stronger threat model remains
explicitly out of scope.

Each canonical-object capture record has exactly `role`, `path`,
`namespace_edge_index`, `bytes`, `sha256`, `retained_max_bytes`,
`bounded_read_max_bytes`, `descriptor_metadata_before_read`,
`descriptor_metadata_after_read`, `bound_check_performed_before_decode`,
`within_bound`, and `canonical_json_verified`.  Its exact role and non-bool
edge selector identify the matching terminal.  All five control objects have
the independent absolute maximum 67108864 bytes and a bounded-read maximum of
67108865; exact integer size is rejected against that constant before decode,
regardless of any producer-claimed size.  Pre/post-read metadata are exactly
equal and equal the final edge descriptor metadata; byte count equals
descriptor size; SHA-256 is computed from the same bytes; and all three
boolean facts are exact `true`.  Retained bytes must equal
`pristine_direct_reference_protocol.canonical_json_bytes(decoded_value)`
byte-for-byte.  The plan, request, source inventory, generated inputs and
schema-v3 source rederivation each pass their corresponding public exact
canonical-byte validator.

The request-source byte capture has the same exact field set and independent
67108864/67108865 byte limits as a canonical-object capture except that its
last field is `request_content_record_verified`, not
`canonical_json_verified`.  That fact is exact `true`; the bytes are not JSON
decoded.  Its role is `request_source`, its path is the exact request-source
publication path, and its bytes and SHA-256 equal the validated raw request's
`request_source` content record.  All capture and postflight bytes are read by
bounded `pread` from capture-only descriptors.  The independently opened,
held, read-only execution descriptor has the same exact file identity, has
position zero before `dup2`, is observed at exact child fd 0 position zero at
the stopped-child pre-exec handshake, and is retained without supervisor
read/seek access through candidate postflight.

The three collector-owned canonical publications have exact integer
`st_mode == 33060` (`stat.S_IFREG | 0o444`) and link count one under the same
fsync/read-only sealing protocol as stdout/stderr.  The plan publication's
bytes equal the exact validated object held by the authenticated plan
authority; the request publication's bytes equal the validated request
generated under that plan.  The two pre-existing input captures also require
exact mode 33060, link count one, and equality to their independently
authenticated plan authority; they are never rewritten by this collector.

The six collector-owned terminal basenames are exactly `raw-plan-v3.json`,
`raw-request-v3.json`, `request.ml`, `stdout.bin`, `stderr.bin`, and
`raw-source-rederivation-v3.json`, in creation order, under the exact fresh
attempt capture directory.  The validated raw request's `request_source.path`
is exactly `request.ml`.  Plan, request and source-rederivation capture paths
must equal the corresponding literal basenames; no producer-selected alias is
accepted.

For the two pre-existing input roles, `path` must be exact lexical POSIX
joining of the validated plan's `retained_input_artifact_root` with the
corresponding safe named plan-relative input path; no resolution or alternate
root is accepted.  Their byte count and SHA-256 also equal the named plan
record.  Before collector implementation, `direct_release_protocol.py` must
add public canonical-byte validators for the exact 400-record source inventory
and exact 43-binding generated inputs; the collector must use those public
entrypoints rather than private helpers.

The anchored self-publication policy record has exactly `policy`, `path`,
`anchor_path`, `anchor_identity`, `parent_edge_count`,
`ordered_parent_edge_sha256`, `parent_edges`, `parent_path`, `parent_identity`,
`parent_metadata_before_create`, `parent_metadata_after_create`, `st_dev`,
`st_ino`, `st_uid`, `st_gid`, `initial_mode`, `initial_nlink`, `final_mode`,
`final_nlink`, `retained_max_bytes`, `bounded_read_max_bytes`,
`canonical_json_required`, `parent_fsync_required`,
`hold_required_through_candidate_publication`, and
`candidate_content_binding_required`.  Its policy is
`root-anchored-exclusive-canonical-fsync-readonly-publication-v1`; the exact
path is `<attempt-root>/envelope-receipt/capture-envelope-v1.json`.
`parent_edges` is a nonempty list of the same exact directory-edge shape,
typing, unique parent/name, canonical path and held descriptor/name identity
rules used by the capture namespace, beginning at `/` and ending at the exact
parent path.  Its exact non-bool count equals list length at most 256 and its
digest is `direct_release_protocol.canonical_sha256(parent_edges)`.  Every
nonfinal parent edge has equal opened/baseline/final metadata; the final edge
has opened and baseline metadata equal to `parent_metadata_before_create` and
final metadata equal to `parent_metadata_after_create`.  This one recorded
exclusive creation is its only allowed transition.

The `envelope-receipt` parent is pre-created before capture, disjoint from the
exact `candidate-receipt` and `completion-receipt` parents, and all three have
collector euid/egid ownership and exact mode 0700.  Therefore each later file
creation mutates only its own separately held parent.  The envelope file is
created with exact initial mode 0600/link count one and collector ownership;
device and inode come from its exclusive no-follow descriptor before
serialization.  Final mode is exact 0444/link count one.  Its independent read
cap and cap-plus-one bound are 67108864 and 67108865 bytes; all four policy
booleans are exact `true`.  The collector writes exactly the canonical JSON
bytes through that descriptor, fsyncs, fchmods, fsyncs and parent-fsyncs,
retains an identity-matched read-only descriptor, and boundedly rereads and
canonical-validates the exact bytes before and after raw-candidate
publication.  The hold field is an obligation, not a preclaim that the future
candidate step completed; the envelope is not consumable by itself and does
not attempt an impossible self-hash.

The schema-v3 source-rederivation object has exactly `schema`, `kind`, `role`,
`reference_ordinal`, `nonce_kind`, `session_nonce`, `boundary_id`, `plan`,
`request`, `stdout`, `stderr`, `process_result`, `transcript`,
`native_execution_closure`, `semantic_projection`,
`semantic_completion_observation`, `cross_runtime_coverage`,
`semantic_status`, `coverage_status`, `authentication_status`,
`candidate_included`, `approval_included`, `promotion_allowed`, `pft_used`,
`s2_eligible`, `s3_eligible`, and `s2_s3_evidence`.  Its schema is 3; kind is
`candle-flyspeck-pristine-direct-raw-source-rederivation-v3`; the role/run
identity equals the plan; plan/request/stdout/stderr are their canonical
content records; process result is the exact successful two-field object; and
the transcript and native closure are the freshly rederived schema-v3 objects.
The semantic projection and run-bound completion are the freshly derived
objects above.  Cross-runtime coverage is exact `null`; semantic status is
`complete-observed-unapproved`; coverage status is
`not-derived-requires-authenticated-inventory-and-generated-inputs`;
authentication status is `not-authenticated`; and all candidate, approval,
promotion, PFT and S2/S3 facts remain false.  A public
`validate_canonical_source_rederivation_bytes` entrypoint enforces this exact
key set, values, canonical encoding and all nested validators.

The schema-v3 raw candidate has exactly `schema`, `kind`, `role`,
`reference_ordinal`, `nonce_kind`, `session_nonce`, `boundary_id`, `status`,
`authentication_status`, `authority_sha256`, `artifacts`, `action_count`,
`loader_event_count`, `lp_success_count`, `exit_code`, `timed_out`,
`validation_error`, `fresh_process_replay_from_action_zero`,
`process_state_checkpoint`, `serialization_environment`,
`approved_reference_present`, `promotion_allowed`, `pft_used`, `s2_eligible`,
`s3_eligible`, and `s2_s3_evidence`.  It retains the current exact values,
with schema 3, including `status = pending-postflight-unapproved` and
`authentication_status = not-authenticated`, but its exact `artifacts` map has
the canonical content records named `plan`, `request`,
`request_source`, `stdout`, `stderr`, `transcript`,
`native_execution_closure`, `semantic_projection`,
`semantic_completion_observation`, `cross_runtime_coverage`,
`source_rederivation`, and `capture_envelope`.  Every value must equal the
freshly validated object or held bytes from this one capture; the semantic and
coverage projections are never accepted as producer inputs.  Kind is
`candle-flyspeck-pristine-direct-reference-raw-candidate-v3`.  The exact path
is `<attempt-root>/candidate-receipt/raw-candidate-v3.json`; it is created
0600, canonical-written, fsynced, changed to 0444, fsynced, parent-fsynced,
reopened read-only, and boundedly reread with the independent 67108864/
67108865 limits.  Its complete root-anchored parent-edge and file observation
is retained by the terminal receipt.  Its exact expanded map and validated
capture envelope are checked only by a private structural helper invoked inside
the public terminal-postflight and bundle validators.  No standalone public
candidate validator returns success, and this pending artifact is not a
consumable raw reference before terminal postflight.

The terminal postflight object has exactly `schema`, `kind`, `status`,
`authentication_status`, `promotion_allowed`, `s2_eligible`, `s3_eligible`,
`role`, `reference_ordinal`, `nonce_kind`, `session_nonce`, `boundary_id`,
`capture_envelope`, `raw_candidate`, `candidate_publication`,
`envelope_held_and_revalidated`, `candidate_held_and_revalidated`,
`all_terminal_captures_held_and_revalidated`, `authority_lock_held`,
`approval_included`, `pft_used`, and `s2_s3_evidence`.  Its schema is 1, kind
is `candle-flyspeck-pristine-direct-capture-completion-v1`, status is
`capture-complete-unapproved`, `authentication_status` is `not-authenticated`,
and `promotion_allowed`, `s2_eligible`, and `s3_eligible` are exact `false`.
Run identity equals the plan, and the two content records equal the exact
canonical envelope and candidate files.
`candidate_publication` is the exact completed anchored publication
observation: it repeats the self-publication record's root-to-parent edge
schema plus exact 0600-to-0444 creation identity/metadata, bounded canonical
read, fsync and parent-fsync completion facts.  It has exactly the
self-publication fields through `bounded_read_max_bytes`, followed by
`canonical_json_verified`, `parent_fsync_complete`, and
`held_and_revalidated_before_completion`; the three policy-obligation fields
are absent and these three observed facts are exact `true`.  The four
top-level completion facts are exact `true`; approval, PFT and S2/S3 remain
false.

This terminal object is written last at the literal path
`<attempt-root>/completion-receipt/capture-completion-v1.json` with the same
0600-to-0444 construction procedure, including file fsync, parent fsync,
canonical encoding and 67108864/67108865 bounded read.  The terminal object
does not and cannot self-attest that its own final file fsync or parent fsync
completed.  A fresh fixed-source public postflight validator performs a new
root-anchored no-follow walk to all three disjoint receipt parents, opens the
envelope, candidate and completion by held descriptors, and checks their exact
current identities, modes, canonical bytes and content bindings.  It makes no
durable-publication claim and accepts no candidate without this terminal
object.  A later promotion would require a separate authenticated outer
collector-success/exit and durability postcondition, which is intentionally
absent from this unauthenticated raw-capture phase.  No field in the envelope
preclaims that candidate publication completed.

The schema-v3 bundle has exactly `plan`, `request`, `request_source_capture`,
`transcript`, `native_execution_closure`, `semantic_projection`,
`semantic_completion_observation`, `cross_runtime_coverage`,
`source_rederivation`, `capture_envelope`, `candidate`, and
`capture_completion`.  There is no public value-only bundle validator.  The
only successful raw-reference entrypoint is a file-aware
`validate_capture_bundle_from_attempt(attempt_dir_fd)` API.  Before trusting any
recorded anchor identity or graph value, the entrypoint itself opens the actual
named `/` with exactly `O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC` and
requires its descriptor identity to equal the graph's `/` anchor identity.  It
then repeats the root-anchored no-follow namespace walk to the recorded attempt
path and requires the resulting descriptor identity to equal the caller's held
`attempt_dir_fd` before opening any terminal through it.  From those two
independently bound descriptors it
reopens all eight terminal captures and all three disjoint receipt files
through the validated edge graph, boundedly obtains stdout/stderr and every
other retained byte string from those descriptors, and decodes no caller-
supplied object.  It then replays the exact parser from those actual bytes,
reconstructs the coverage adapter, validates the terminal postflight closure,
and only then returns this still-unapproved, unauthenticated bundle.

The process result has exactly fields `exit_code` and `timed_out`, with exact
types `int` and `bool` (excluding bool-as-int), and exact values `0` and
`false`; it is one of the five parser inputs.  An
independent envelope validator must rerun the exact trusted parser from the
retained stdout/stderr bytes, process result, plan and request, then require
canonical byte equality between that result and the retained schema-v3
source-rederivation object named by `source_rederivation_capture`; candidate
assembly may consume only the freshly rerun result.  The envelope is validated
before coverage assembly.  Authenticating the collector source and process
remains a separate prerequisite; neither this receipt nor the pure parser
pre-claims that authentication.

The later public pristine adapter is a pure function of the validated plan,
freshly replayed schema-v3 rederivation, validated 400-record inventory,
validated 43-binding generated inputs, and run-bound semantic completion.  It
must construct every coverage-v2 member rather than accept one.  Actions are
the 297 plan records joined positionally to all 297 exact transcript
completions.  The selected logical closure is the sorted unique native event
set excluding the serializer, with every identity matched to the authenticated
inventory and every normalization copied from that inventory.  LP consumption
is the canonical-relative-path reorder of all 39 exact successful transcript
records and must equal the LP subset of generated inputs.  Inventory and
generated objects are copied only after their public validators pass.

The adapter derives each `mathematical_coverage` fact separately:
`source_closure_observed` requires the complete validated native closure and
the inventory join above; `lp_completed_observed` requires all 39 distinct
inputs exactly once and run-bound semantic theorem record 0;
`nonlinear_completed_observed` requires every plan action whose stratum is
`nonlinear_support` to have its matching completion and run-bound semantic
theorem record 1;
`final_premises_completed_observed` requires semantic theorem records 0--2 at
the final boundary; and `final_implication_completed_observed` requires record
3, action 296, and the exact final-target/serializer post-action suffix.
Structural-fingerprint and dependency-request lists are derived from the four
run-bound semantic names, not copied from a claimed coverage object, and
`approved_reference_present` remains false.  If any predicate is false the
adapter fails rather than hard-coding the validator's required `true`.  Its
result must finally pass the existing public full coverage-v2 validator before
it can enter a candidate.

Positive tests must build canonical length-framed theorem/list/kernel payloads
from independently constructed component bytes, rederive all hashes, require
exact ten-line order, and compare the result with the existing strict direct
semantic projection validator.  They must not seed the parser with the
expected semantic projection.  A separate limitation test substitutes one
well-formed dependency digest and confirms that only the unauthenticated
derived projection changes; it must not mislabel lexical parsing as content
authentication.

## First hostile design review

Independent review of commit `55d66f5` failed the first design with no P0,
two P1 findings, and three P2 claim/precision findings.  The P1 findings were
the missing composite/list reconstruction above and the impossible claim that
a passive grammar could reject an otherwise well-formed substituted
dependency digest.  The P2 findings required the explicit non-kernel/non-
collision-resistant dependency limitation, exact canonical-JSON encoding and
output cap, exact boundary literal, and weaker raw-parser authority wording.

This revision incorporates every finding.  It is still a design prerequisite,
not implementation evidence, and requires a fresh exact-commit rereview.

Rereview of `ad3d0e8` found no P0/P1 and confirmed all original findings
closed.  Its remaining P2 precision requests were a pre-conversion bound on
inner frame decimals, a disjoint migration for the new closed shapes, the
exact shared semantic `COMPLETE` name, and an ordinary fresh-publication file
requirement.  The present revision incorporates those requests and again
requires exact-commit rereview before implementation.

Final hostile rereview of exact design commit `0608b10` returned PASS with no
P0, P1, or P2 finding.  It verified the inner-frame pre-conversion/allocation
bounds, composite/list/axiom checks, complete v3 migration, exact four-form
semantic marker, cap-plus-one fresh-file read protocol, dependency limitation,
and nonpromotion boundary.  Implementation must include the reviewer's
explicit regression for a nine-digit child count larger than the bytes
remaining in its frame, proving that the parser rejects before an unchecked
loop.

An implementation-sufficiency audit then found that the five-input parser
cannot derive full coverage-v2 from plan content hashes alone.  Commit
`8c0f858` correctly separated the nonce-free semantic projection from later
coverage assembly, but its fresh hostile review failed with two P1 and three
P2 capture-contract findings: the capture receipt omitted the process result
and replayed parser result; pathname facts were unanchored booleans rather than
retained namespace identities; the completion observation was not run-bound;
the child count lacked its explicit remaining-byte pre-loop bound; and stderr
had no collector bound.  The present design incorporates all five findings
with the run-bound observation, zero-byte stderr policy, anchored namespace
records, exact process/rederivation closure, mandatory parser replay, and
pre-loop `remaining_bytes // 2` check.  It requires another exact-commit
rereview before implementation.

Two independent reviews of exact `4860ec9` confirmed those five repairs but
found the capture closure still insufficient.  One P1 was an ambiguous graph
schema without exact indices, role closure or operation/type mappings.  The
other review added two P1s: a descendant could retain a writable stdout file
description after the successful parent exited, and plan/request/rederivation
bytes were not namespace-captured.  Their P2 findings required honest bound
facts, absolute control-object caps, exact path/canonical-byte bindings,
public inventory/generated validators, authorized-creation chronology, and an
explicit coverage adapter.  The current revision incorporates all of these
findings with exact terminal roles, a fresh subreaper/descendant drain
and writer scan, fsync/read-only sealing, capture of every replay input/output,
fresh envelope/candidate publication, 64-MiB control caps, and predicate-by-
predicate coverage construction.  It again requires exact-commit rereview.

Two independent reviews of exact `c8e03b6` confirmed that closure but rejected
the writable and final-publication phases.  Their P1 findings were permissive
initial publication ownership/modes, omission of the generated ML request
source and its exact child execution channel, missing descendant count/list
equality, unanchored receipt/candidate parent paths, and absent exact v3
rederivation/candidate key sets.  P2 findings rejected a future hold fact
preclaimed by the envelope, unbounded/nonzero descendant wait statuses,
unnamed publication basenames, unbounded final objects, ambiguous subreaper
verification/parent chronology, and mathematical facts that did not cite the
corresponding semantic records.  The present revision answers them with eight
terminal roles, six private 0600 creations under owned 0700 directories,
request-source-to-child-stdin identity, zero-status count-complete descendant
records, root-anchored disjoint receipt paths, exact schemas and public
validators, and a last postflight publication that binds the held envelope and
candidate only after both exist.  It requires a new exact-commit hostile
rereview.

Two independent reviews of exact `17192de` confirmed all of those repairs but
found one remaining P1 in the execution channel: descriptor identity and access
mode did not bind the shared open-file-description offset, so an implementation
could capture the full request yet execute EOF or a suffix.  A follow-up found
that wait statuses and fd numbers did not all require exact integer types, and
that a public `complete-unapproved` standalone candidate contradicted the later
mandatory postflight.  They also found that a value-only bundle could not
replay retained output bytes and that the terminal could not self-attest its
own final fsync.  Further P2 requests added closure fields on the terminal
postflight and construction of coverage before the candidate that binds it.
The current revision requires `pread`-only supervisory capture, a separate
untouched stdin open-file description, and a stopped-child pre-exec handshake
that records and validates fd 0 identity, mode and exact position zero.  It
requires exact non-bool integer fd/wait fields, makes the candidate
`pending-postflight-unapproved` behind a private structural helper, adds the
four explicit nonpromotion fields, exposes only a descriptor-rooted file-aware
bundle validator that independently opens actual `/`, explicitly withholds
terminal durability, and corrects the publication order.
Implementation tests must include bool/float fd and wait fields plus fd 0
initially at EOF, offset one, and a valid-looking suffix.

Two independent reviews of exact `57f885b` confirmed those repairs but found
that several adjacent PID/group/session/index/selectors still lacked one global
exact-integer rule, the file-aware API compared a caller-provided root only to
the self-declared graph, and the implementation sequence disagreed with the
normative envelope-before-coverage order.  The present revision makes every
child-binding scalar exact `int`, opens actual `/` inside the validator before
trusting the graph, identity-binds the supplied attempt descriptor to the
resulting walk, and fixes the order below.  Hostile implementation tests must
also include float/bool substitutions for every integer class and a
self-consistent positive-content tree supplied through a fake root fd.  This
revision again requires exact-commit hostile rereview.

## Implementation sequence

1. Rebase onto the independently accepted parser/protocol source-authority,
   exact-JSON-type and immutable-bytes repair at `b0e24b9`, preserving its
   exact hostile-test evidence.
2. Apply the disjoint schema/kind/marker v3 migration, including the exact
   top-level plan/request stdout/stderr caps and retained-input root, and add
   pure semantic-line decoding plus hostile unit fixtures without enabling
   raw-candidate production.
3. Integrate the ten-line state machine into the immutable stdout parser and
   rederive the common semantic projection plus the exact raw semantic-
   completion observation from bytes.  Keep full cross-runtime coverage null.
4. Extend the request generator so pristine OCaml emits the exact wrapper
   lines from the loaded serializer and `Serialization.full_digest_thm`.
5. Specify and implement the held collector capture envelope, authenticate
   the generated request source and the plan-bound source-inventory/generated-
   input payload bytes through new public canonical validators, drain/seal the
   child output closure, replay the held captures, publish and validate the
   envelope, construct and validate full cross-runtime coverage-v2 from those
   freshly replayed objects, then publish the pending candidate and terminal
   postflight in that order.  Obtain an independent hostile rereview before any
   live pilot or raw candidate.

No current run or artifact is made promotable by this design.

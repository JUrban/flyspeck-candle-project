# v1.3 direct-source parallel gates — 2026-08-27

The governing release target remains S3 direct source execution.  The PFT
lane is P1 validation only and is reported separately.

## Compatibility ledger

Project commits `225f3fa` and `5d620bc` classify all 217 pointer-like tokens
and both custom FFI calls against the selected S3 source route.  Fifteen
pointer findings are selected: twelve built-in physical-identity uses over
non-reference values, one immediate-integer `==`, and two tokens resolving to
the local theorem-producing `eq_eq` operator.  The other 202 findings are
source-backed route exclusions, not claims that those files are globally
dead.

Pinned differential oracles confirm two P0 compatibility defects.  Candle
exposes built-in `==` as reference-only, rejecting the selected list and
integer uses, while built-in `!=` fails in the `nRelOp` parser path.  Local
`(==)` rebinding succeeds, supporting exclusion of the two `eq_eq` tokens.
No remedy or proof is implemented yet, so G3 remains in progress.

The required FFI sites are boot-time `chdir` in
`candle/chdir_to_root.ml:14` and `system` in `candle/ocaml.ml:294`; the latter
is reached by the full LP route's tracked compressed-certificate handling.
A minimized `Sys.command "exit 7"` success path passes in Candle.  G4 remains
open because the ABI, total validation/error behavior, path and environment
policy, sandbox, fuzzing, and refinement/trust obligations are not discharged.

The compatibility inventory is now regenerated from the dedicated clean
upstream worktree at exact source pin `1ce0353`; PFT head `2ea440e` is retained
only as a separately labelled comparison.  Direct minus PFT is eight files,
14,760 bytes, and five PFT-only syntax findings, with no change to any of the
217 pointer or two FFI review classifications.

Validation is green for 8 inventory tests, 4 triage tests, 3 ledger tests, 3
pin-delta tests, and the source-backed validator: 6,044 findings, 217 pointer
reviews, 2 FFI reviews, 8 project ledger entries, and 1 authoritative imported
entry.

## Decimal floats

CakeML branch commit `30e014bd9cfd80b356f4718124cdbeed0409c10e`
restores the decimal-float lexer, grammar, and parse-tree conversion.  A
one-core targeted HOL build proved `caml_lex`, `camlPEG`,
`camlPtreeConversion`, and `camlTests`.  Ten OCaml-4.14-normalized positive
forms and six malformed negatives are covered at this parser/proof-script
gate.

Candle source-evidence commit `91efd3795b7ca9cdd7ba72be8268338422670f00`
pins the result and benchmark harness.  This is not closure: the rebuilt
compiled Candle must still match the ten exact IEEE words, reject all six
negative cases end to end, and carry measured parser cost through the verified
bootstrap chain.  The chain is being built sequentially at one core.

## Declaration open

The current CakeML Dopen branch has completed the foundational namespace, AST,
grammar, conversion, semantic-primitives, evaluator, and type-system gate.
Commit `56d2ed995` repaired the first exact proof failure by proving parsed
module paths non-empty.  Commit `6a3a2a595` adds the relational
`env_rel_open` preservation candidate after the separate existence/alignment
lemma.  That candidate is not yet built or connected to generalized
`infer_d_sound`; inference soundness/completeness, CV, semantic equivalence,
bootstrap, and compiled-source gates remain open.

## Manifest-rooted loader inventory

Candle loader branch commit `21e93da` generates a relocatable-path-independent
full-source DAG from a dedicated clean Flyspeck worktree at the governing
upstream source pin `1ce0353`; the PFT development head is excluded.  It
records:

- 297 ordered full-build entries, 287 unique;
- 398 hashed source nodes and 417 selected dependency edges;
- 42 individually hashed LP/archive/nonlinear inputs;
- 15 source-and-line-reviewed non-literal load sites; and
- zero unreviewed dynamic loads, unresolved roots, missing ordinary sources,
  ambiguities, path escapes, or detected cycles.

Two generated-runtime contracts remain explicit: compiler-derived
`candle/build/insulate.ml`, and Flyspeck's temporary theorem-digest module.
The manifest also pins direct, PFT-free Candle source for
`Candle_flyspeck_l2.tame_imp_kepler_conjecture` and its exact imported premise.
The enforcing loader fails closed on any build mode other than `full`; a
compiled-Candle negative test reaches that guard before filesystem/source
work.  In a clean full-mode process it now loads the pinned Candle/HOL source
stack itself, installs only manifest-supplied `HOLLIGHT_DIR` and `FLYSPECK_DIR`,
checks ordinary marker files through CakeML TextIO, preserves the verified
`Filename` operations, and aliases HOL Light's `load_path` to the single
`Cakeml.loadPath` reference.  The old `Sys.file_exists`, `Filename.concat`, and
`load_path` frontiers are therefore passed.  The next exact frontier is the
original `build/strictbuild.hl:21` directive `#load "unix.cma";;`; unknown
library loads must fail rather than be silently ignored.  The manifest now
freezes the five reachable directives (two `unix.cma`, three `str.cma`), 39
qualified member uses, two `open Str` sites, and three separately labelled
open-attributed candidates, for 42 capability occurrences over six `Unix`
and five `Str` members.  There is no reachable `open Unix`.  Open attribution
is explicitly lexical/reviewed rather than compiler-proved.  Unknown libraries
and members are promotion blockers.  The repository's two `nums.cma`
directives are outside the recursively reached build graph.  The repository
convenience entry `load_flyspeck.ml` is also outside the selected route: the
enforcing loader enters `strictbuild.hl` directly.  Removing that incorrectly
added root removes one directive and two placeholder `Unix.putenv` sites from
execution claims.

The five selected `Str` members now have a pure Candle source implementation,
with no dynamic loader or FFI.  Its compiled gate agrees with OCaml 4.14.1 on
all selected literal pattern forms, adjacent-delimiter split behavior,
replacement, and bounds-sensitive prefix extraction.  Advanced escaped regex
syntax and empty-match replacement fail explicitly, so this is partial binding
evidence, not activation.  All six `Unix` members remain unimplemented and
the direct loader still stops at the original `strictbuild.hl:21` directive.
Directory existence semantics remain explicitly open.  Known generated Candle
outputs are recipe contracts regardless of whether an ignored local build file
exists, so the source graph no longer depends on worktree build state.
The manifest is a G6 inventory slice only.  Positive full loading, digest and
generated-input enforcement, relocation, corruption/reorder rejection,
versioned atomic checkpoints, and clean/resume execution evidence remain open.

## Running evidence lanes

The first Great-100 matrix has completed ten of 65 clean sessions: eight
passes and two failures.  Target 9, `cayley_hamilton`, passed in 1,438.2
seconds.  Target 10, `ceva`, failed after 1,428.4 seconds because
`Examples/sos.ml` requires the missing `round_num`; this is distinct from the
minimized float-literal failure.  Its live runner has known
incomplete RSS sampling, and its historical timeout is inactivity-only.  The
isolated repair at `bb5fb495c8e850d525f58f25a13a51ebbc974a10` separates
inactivity and total wall deadlines and passes 15 tests; it does not
retroactively repair the live report.

The PFT resume smoke is green at project commit `4ac1bdf`: a 5,556,011,008-byte
process image restored to a byte-identical 305-command trace accepted by
compiled Candle.  A fresh full PFT validation run is active under the repaired
producer heads and new locked paths.  Neither result advances S2 or S3.

The active workload stays below four heavy cores and well below the authorized
60--80 GiB RAM range.  Long HOL/bootstrap targets and source sessions remain
sequential within their lanes.

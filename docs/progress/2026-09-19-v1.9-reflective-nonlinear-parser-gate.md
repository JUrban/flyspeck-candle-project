# Direct Flyspeck and reflective nonlinear parser milestone

Date: 2026-09-19 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`.
- The uninterrupted cumulative action-184 run remains healthy in the real
  `hard_1.dat` proof. At this report sample it had emitted terminal 1,571 of
  2,899; action 184 is not yet passed.
- The separate focused action-296 process remains active with a zero-byte
  buffered log and therefore receives no functional, cumulative, S2, S3,
  qualification, promotion, or release credit.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Reflective lane milestones

The staged plan now has three independently rerun proof-producing gates:

1. Stage 0's corrected `Kernel.compute` wrapper contract passes its complete
   arithmetic, conditional, equality, nested-pair, ordering, and negative
   checks on `codex/cv-stage0-contract-v1` at `4a72ddc`.
2. The `compute_all` representation/core and Flyspeck adapter gates both pass
   on `codex/cv-compute-all-v1` at `9c1a38e`. The measured reflected
   `list_of_faces` call is 7.373 ms versus 0.667 ms for the existing specialized
   conversion, or **11.06x slower**. This establishes the machinery but rejects
   that small call as a performance target.
3. The generic LP core and complete fresh Flyspeck-interface wrapper both pass
   on `codex/cv-lp-linear-combination-v1` at `365f0b3`. The retained real
   `hard_2` selected slices remain 9.87x, 4.15x, and 3.66x faster than legacy
   theorem construction/combination for terminals 15, 5, and 17 respectively.
   These are local proof-producing gains, not inclusive terminal claims; the
   corrected checkpoint old/new comparison remains deferred while swap space
   is constrained.

These findings materially narrow the plan: do not generalize from the
`compute_all` integration example, but continue the coarse LP checker where the
3x continuation threshold is met and use the nonlinear verifier's real phase
profile to identify the next coarse reflected predicate.

## Complete nonlinear parser gate

The authenticated recursive closure for
`formal_ineqs/verifier/m_verifier_main.hl` remains exactly 90 source nodes and
193 literal source edges. It contains 34 nodes already authorized by the direct
400-source manifest and a bounded extension of 56 exact Flyspeck identities.

The retained v1 parser inventory completed all 56 extension sources in 52m54s,
with 1,053,696 KiB peak RSS and no swapping. It passed 52 and found four real
frontend failures:

- `taylor/m_taylor.hl`;
- `taylor/m_taylor_arith2.hl`;
- `verifier/m_verifier.hl`; and
- `verifier/m_verifier_main.hl`.

The first three share one chained-array grouping defect. The fourth contains
three module-qualified record constructions rejected by the CakeML frontend
and one mis-grouped `ref := if ...` expression. Exact compatibility lowering
now performs 45 bounded operations over those four authenticated sources:

- 41 chained array reads/updates become explicit `Array.get`/`Array.set` calls
  with both indices parenthesized;
- three records retain the same fields and values under an explicit original
  record type; and
- one conditional assignment gains explicit RHS parentheses.

The initial array prototype briefly omitted those index parentheses, which
would have changed `a.(n - 1).(i - 1)` semantics. Review caught it before any
closure publication or proof run. Native OCaml differential tests now cover
nontrivial indices, updates, typed record equivalence, and conditional
assignment. Each of the four complete normalized files also passes a fresh
exact Candle parser process.

The retained v2 result is **56/56 parse-pass**. It reuses only the 52 exact
successful v1 prepared inputs and reruns all four normalized/formerly failing
sources freshly. Those four fresh parses consumed 9.188s total; the complete
authenticated controller finished in 17.53s, again at 1,053,696 KiB peak RSS
with no swapping. The result is
`/project/flyspeck-candle-runs/cv-nonlinear-verifier-parser-v2/result.json`
(SHA-256
`8fc0910a953bce92da92b00a8d28f8bb900dd43eb1988d5043e7f641a9ac1292`).

The normalization, closure, parser reuse contract, proof gate, and exact
first-leaf runner are committed on `codex/cv-nonlinear-verifier-v1` at
`cf32d97`. Twenty-three closure/parser/target tests plus the absolute-driver
regression pass. The Big_int/Num and nonlinear runtime compatibility gates also
match OCaml 4.14.1 on the exact development executable.

## Substantial nonlinear proof run

The first substantial target remains global case 7,853 / local case 0 of
`prep-4680581274 delta issue-cayleyR`: native precision 6,
`eps = 1e-10`, native verifier time 365.672506s, and historical theorem digest
`f7ff5f16bf03b6885aa4d46bb1490001`. The historical result is only an oracle.

The new controller authenticates the closure, the target, and the exact
`Prove_by_refinement`, `Definitions`, and `Break_case` reconstruction sources.
One fresh process must then:

1. load all authenticated verifier sources;
2. produce a small ordinary verifier theorem;
3. reconstruct the exact first leaf through `Break_case.ineqm_conv`;
4. run `M_verifier_main.verify_ineq`; and
5. return the exact target conclusion with no hypotheses and an unchanged
   global axiom set.

It records separate closure-load, smoke-proof, reconstruction, formal-check,
wall-time, CPU, and peak-RSS measurements. The first two launches are retained
as rejected harness evidence: v1 omitted the unprefixed resolver entry needed
for an absolute driver `#use`; v2 paired the current source with the older
runtime-build insulation file, which lacks the now-required `Word64.word` type
aliases. Neither reached a verifier source or theorem. V3 uses the current
source's authenticated generated insulation file, already validated by the
focused runtime gates, and is active at this report point.

## Next gates

1. Preserve and classify the first dependent failure from the combined
   nonlinear proof run, or retain its proof/timing result if it passes.
2. Use the measured reconstruction/formal-verification split to select the
   first nonlinear computation to move behind a coarse correctness theorem and
   `Kernel.compute`.
3. When host swap pressure permits, run the corrected inclusive LP old/new
   checkpoint comparison without disturbing the cumulative or PFT oracles.
4. Keep the uninterrupted cumulative action-184 run moving; reserve another
   action-zero replay for a coherent successful compatibility batch.

# v1.54: Nonlinear division-exception compatibility

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T00:25:12Z`, the protected cumulative action-184 run had
  freshly passed `hard_7.dat` terminal **5,552 / 9,080**.
- The reverse LP diagnostic has passed every attempted terminal down through
  **6,322**. The forward diagnostic has passed every attempted terminal
  through **6,015**, with no terminal failure reported by either shard.
- The nonlinear first-leaf predecessor has restarted from a clean state on the
  repaired source identity. No partial failed state was reused.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## Root cause and bounded correction

The v20 predecessor reached the genuine nonlinear verifier sources and then
failed while evaluating `formal_ineqs/informal/informal_verifier.hl` because
native OCaml's predefined `Division_by_zero` constructor is not a Candle
constructor. A focused restored-state probe established that arbitrary-
precision integer division by zero in Candle raises CakeML's `Div` constructor.

Candle commit `9a5eafb` therefore maps exactly four authenticated native catch
alternatives from `Division_by_zero` to `Div`: one in `informal_verifier.hl`
and three in `informal_search.hl`. It does not introduce a wildcard catch or
change any protected expression. The mapping preserves the intended failure
class under Candle's corresponding integer-division primitive.

The runtime probe completed with `CANDLE_DIV_EXCEPTION_PROBE_OK` at:

```text
/project/flyspeck-candle-runs/cv-division-exception-probe-v1-run-001
```

Both newly normalized source files then passed fresh parser processes. The
updated closure and downstream target identities passed all **58** focused
closure, parser-controller, smoke-controller, checkpoint, and target tests.

## Clean predecessor restart

The new authenticated input bundle is:

```text
/project/flyspeck-candle-runs/cv-first-leaf-predecessor-checkpoint-v21
```

It records:

- Candle commit `9a5eafb703f7457c9b0f850dcedbfcc888497e09`;
- closure SHA-256
  `39bef210abcec446927bc1799a549ccf1dda790c5e6b7f532f12bfbbfff537c7`;
- 93 authenticated source nodes, including three target-support nodes;
- 32 normalized sources; and
- driver SHA-256
  `aadc65cc0370a77f9220ec5bb4ce8f4e74230cd94f4b59eed7d22c6690a32ffd`.

The v21 process acknowledged execution and began a fresh HOL/closure load. It
will checkpoint only after the unique
`CANDLE_NONLINEAR_FIRST_LEAF_SUPPORT_READY` marker and will fail closed on any
source error. Once ready, genuine-leaf capture and the reflected-versus-legacy
timing comparison can run in disposable restores.

# v1.3 resource-envelope amendment — 2026-08-28

The user has explicitly amended the interactive resource envelope for the
parallel direct-Candle/PFT project:

- keep aggregate CPU use below approximately ten CPUs;
- keep ordinary work well below the machine limit;
- memory-heavy stages may use up to approximately 120 GiB when the machine has
  sufficient available RAM; and
- check availability before starting or enlarging a heavy stage.

The development host reports 251 GiB physical RAM. At the amendment checkpoint
it had approximately 178 GiB available. The restartable PFT oracle producer
used one CPU and about 23 GiB RSS; the verified CakeML bootstrap used one CPU
and about 8 GiB RSS. Neither job was enlarged, because both were progressing
inside the earlier envelope.

The 120 GiB value is a ceiling, not a target or a new default. The compiled
stratum runner retains its 48 GiB address-space default. An attempt may be
given a larger explicit `--max-address-space-gib` only for a measured stage
that needs it, after checking machine-wide memory and the other live jobs. The
receipt records the selected limit and observed child maximum RSS. Final lock
metadata must record the amended heavy-stage ceiling and the actual per-gate
measurements; it must not imply that host preparation or PFT replay is direct
S2/S3 evidence.

# Float completeness provenance closure — 2026-08-28

This is an interim assurance report for the v1.3 direct-source route. It does
not claim a compiled Candle pass, a complete Flyspeck load, S2, or S3. The
verified CakeML bootstrap is still active, and the independent PFT export
remains a separate validation lane.

## Reviewed implementation

The clean Candle integration branch
`codex/flyspeck-v13-candle-integration` is at
`a9ba565f424b66aa4b00a7a081fde1203feabd95`. The
float-completeness-specific checkpoints
are
`a9ea921d6dd099e6bb079c26dadbc72d7bc7976e` (`test: close float oracle
provenance gaps`) and `1e5ebb97a1d04c5c01f8fd1d8e3c637a96aa258d` (`test: bind
float runner execution sources`). The later controller checkpoint
`0788eaaaed32cd7967ce4eab3dc8ab551f4ea13e` binds those gates into the exact
direct-stratum runner; subsequent performance-provenance commits do not alter
the completeness implementation. Together they close the independent rereview
findings against the direct Flyspeck decimal-float completeness gate:

- both the Python inventory and OCaml lexer consume one private, read-only
  snapshot of the authenticated runtime bytes, which is rehashed after both
  observations;
- the OCaml oracle records every excluded paired-backtick span by exact source,
  line, column, and byte endpoints; the selected-source dialect contract pins
  318,855 spans and canonical digest
  `c77a3aca5d71590637fd624b04e51f3ea4250591a4f693d338611647c9a606dd`;
- the dialect contract explicitly excludes polymorphic variants and is
  documented as a reviewed selected-graph assumption, not a general syntactic
  proof about arbitrary paired backticks;
- all 21 observed external regular-file inputs to OCaml oracle compilation and
  execution are pinned, including the compiler-libs and standard-library
  inputs, `ocamlrun`, loader configuration/cache, exact `/lib` objects, and the
  `/lib64` interpreter alias;
- `/etc/ld.so.preload` must be absent, and the toolchain and generated oracle
  artifacts are reauthenticated after execution;
- the deterministic oracle bytecode, CMI, and CMO are pinned and retained;
- the four imported local Python modules execute from captured source byte
  strings that are retained directly, bypassing local bytecode caches; the
  top-level runner archives its startup source capture; and
- the running `/proc/self/exe` Python image and base ELF closure are pinned,
  while the ten `pexpect`/`ptyprocess` sources are copied into evidence before
  import and loaded only from that retained snapshot; and
- retained inputs are rehashed during the compiled gate's postflight.

Two independent read-only reviews found no material false-green at the final
commit under the documented threat model. Initial top-level Python source
compilation, removed pre-start loader injection, hostile same-UID mutation, the
broader Python standard library, kernel, vDSO, and built-in/frozen modules
remain documented host trust boundaries.

## Exact validation

The final host suites passed from the clean commit:

- 60 compatibility tests;
- 136 broader Candle tests;
- 11 focused completeness tests;
- 12 focused compiled-corpus runner tests; and
- 15 artifact-provenance tests in the independent rereview.

The exact shared-snapshot authentication passed with:

- 400 authenticated source nodes;
- 15,775 code float occurrences;
- 1,741 exact decimal spellings;
- nine runtime files containing code floats;
- 318,855 authenticated paired-backtick spans; and
- 21 pinned OCaml external regular-file inputs.

The generated oracle executable is 2,911,680 bytes with SHA-256
`b66c3433c012bf87bbce86c350aa60d31b3d8321900e6e1f557a4952e95261b9`.
The exact current-corpus check reports:

```text
Independent OCaml lexer completeness PASS: 15775 occurrences, 1741 exact spellings, 9 runtime files
```

## Commit-bound host materialization

After the final controller and performance reviews passed, the host inputs were
regenerated against Candle `a9ba565f424b66aa4b00a7a081fde1203feabd95`
and Flyspeck
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`:

- normalization overlay:
  `flyspeck-candle-runs/v13-normalized-overlay-a9ba565-ac925270`, with 18
  entries and contract SHA-256
  `ac925270aa6a8605a8f70ab170ff965c3e4a4d6410623e3d3a6d51976ff1da08`;
- generated LP input:
  `flyspeck-candle-runs/v13-generated-lp-a9ba565-0ca1b5b6`, containing the
  119,855,733-byte `hard_7.dat` with SHA-256
  `0ca1b5b6ceba53537ac5d95ffddd883bb297e7d48c30ac241de4f3ec71ab5526`;
  and
- stratum plan: `flyspeck-candle-runs/v13-stratum-plan-a9ba565`, containing
  297 actions, eight cumulative boundaries, two diagnostic cutpoints, and
  plan SHA-256
  `c2ccfdaa536386733d3e0406098e5e77a1e166d46d5819210723bd917e932aab`.

The plan's host-materialization receipt has SHA-256
`228a72783d8b9835f4081fd9bc688b5675e03e85c5997e5d723c1c7bc0bfe40b`
and binds both its Candle integration base and materialization head to
`a9ba565f424b66aa4b00a7a081fde1203feabd95`.

The exact independent completeness check passed again on the regenerated
overlay. These materializations are host scheduling and source-authentication
artifacts only.

## Next promotion gates

1. Finish the verified CakeML `cake.S` bootstrap and issue its successful,
   commit-bound provenance record.
2. Link the clean Candle commit to that exact compiler output and validate the
   linked record.
3. Run the focused compiled float/Dopen/Num/multiline gates and the complete
   1,741-spelling corpus gate, retaining the new provenance closure.
4. Run the bounded full-session performance gate and then the cumulative direct
   Flyspeck strata in fresh Candle processes.
5. Approve theorem fingerprints only after direct-source execution; do not
   promote the independent PFT lane to S2/S3 evidence.

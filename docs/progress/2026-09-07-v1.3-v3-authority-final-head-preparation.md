# V3 authority and final-head preparation

Date: 2026-09-07 UTC

## Result

The executable candidate-V3 wire is now the official Candle fingerprint wire,
and the final S1 runner consumes its complete kernel state through a bounded,
sequenced stream.  The candidate final runtime head is Candle
`867525fdd4cd44e0872d2d2b1be76531517251d0`.

The repository-wide test result is deliberately one gate short of green:
359 tests ran, 358 passed, and only
`test_top100_manifest.Top100ManifestTest.test_generated_manifest_is_current`
failed.  It failed because the committed identity approval pins the old V2
serializer.  That failure is retained until the fresh V3 native-reference
collection closes and receives independent review; rebinding or weakening the
old approval would be unsound.

## Runtime and consumer closure

Candle commit `d377f36`:

- makes `candle/fingerprint.ml` byte-identical to the reviewed V3 serializer;
- adds a bounded incremental protocol for the complete state stream;
- makes the official Great-100 runner reconstruct and validate that stream;
- binds the serializer, stream helper, and protocol into the execution
  contract.

The principal identities are:

| Input | SHA-256 |
| --- | --- |
| `candle/fingerprint.ml` | `444edaba460b2e9ab01c535fbfb18e9fe9932dcf6fe7f8996e2008b47991b110` |
| `candle/fingerprint_v3.ml` | `444edaba460b2e9ab01c535fbfb18e9fe9932dcf6fe7f8996e2008b47991b110` |
| `candle/runtime_fingerprint_protocol.py` | `147f552e1acb09e244c487f12cf981492672db1a696499b9594c4ce6f6459df8` |
| `candle/fingerprint_v3_state_stream.ml` | `4a658cb6e00508d66128f2a3f9dba4fbbe7bf0a2d36d53f826dd2a8b28d65946` |

The focused runner/reference suites passed 56 tests.  The project-side S1
finalizer at commit `36d4957` independently parses and reconstructs the same
wire, hard-pins the schema-5 collection contract and producer/reviewer
identities, and passed all 69 adversarial tests.

## Flyspeck closure at the final HOL base

The exact source graph was regenerated from Flyspeck commit
`1ce0353008eba83d3c76ae9a25c3c242e4802d53`.  It contains 297 roots, 400
source nodes, and 43 generated inputs.  The final HOL dependency change removes
`compute.ml` from the selected closure and admits `thecops.ml`; the manifest,
loader pin, source-digest program, parser descriptors, and all-inventory
authority were regenerated as one exact cohort.

The reconciliation is committed in Candle as:

- `d6f9571` — regenerated manifest and derived controls;
- `1b6ace8` — rebound exact all-inventory authority and corpus hashes;
- `867525f` — refreshed the generated-control test fixture.

The parser diagnostic suite passes 56/56, the source-only all-inventory suite
passes 17/17, and the stratum runtime suite passes 45/45.  Direct-source strata
continue to use the aggregate V3 fingerprint, not the Great-100 chunked
transport; they verify that the official serializer bytes have the same V3
identity.

## Native V3 reference authority in progress

The immutable collection root is:

```text
/project/flyspeck-candle-runs/
  candidate-v3-reference-v9-two-sweep-afc95fb-c00408a-001
```

It runs two independent sweeps over all 65 targets under schema 5, with seven
bounded workers and no sweep overlap.  At the 2026-09-07T09:39:52Z checkpoint,
44 of 130 target runs had succeeded and none had failed.

An independent incremental audit at 30/130 is retained at:

```text
/project/flyspeck-candle-runs/
  v3-reference-authority-review-d377f36-prefix-030/report.json
```

Its SHA-256 is
`7c22a569bdbaea52385fdf8f4b1eec1180e6dd30e16a63b90be6fb0cd64de021`.
It cross-matched all 30 available reference identities to the completed
65-target Candle diagnostic report, but correctly reports
`incremental_review_incomplete`, `promotion_allowed: false`, and emits no
authority approval.

## Next gate

No further runtime or source-closure changes are planned before authority
closure.  Once all 130 native runs are present, the next steps are:

1. run the independent audit without the incomplete-review allowance;
2. import the closed, audited collection and issue a new exact V3 identity
   approval;
3. regenerate the Great-100 manifest and require the full Candle suite to pass;
4. freeze that evidence-bearing Candle head;
5. perform one fresh cache-disabled canonical bootstrap, followed by two clean
   full Candle Great-100 executions and independent S1 finalization.

This report is an interim engineering checkpoint.  It is not schema-6,
schema-7, S1, S2, S3, or release evidence.

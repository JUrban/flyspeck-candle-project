# v1.3 source integration and Dopen bootstrap — 2026-08-27

The direct Candle release-candidate sources now have one integration branch:
`codex/flyspeck-v13-candle-integration` at
`273cd9c25e16963e2068e6230ee4dbe54ae486f9`. Its merge commit
`b2cf35876ad54888cbcec58d7cd10a55ff467359` joins the authenticated direct
loader head `ba90c9151` with the independently tested Great-100 and SOS
compatibility head `02faa155`.

The merged source graph initially failed its intended stale-manifest gate.
Regeneration changed only the selected `candle/nums.ml` and `lib.ml` source
hashes and their generated digest-program authentication. Because the loader
is deliberately excluded from that self-hashed program, its outer MD5 pin was
updated explicitly and the graph regenerated a second time. Evidence at the
committed head is:

- 64 host integration tests pass;
- the full generator reports 297 roots, 400 source nodes, and 43 generated
  inputs;
- direct manifest SHA-256 is
  `aa681c20c232bc5b5116704044d3e50a8753fe685d36d699f9d71297aef7a5e2`;
- source-digest program SHA-256 is
  `76bb4583e0050d54e7d37ab9360b212088e829a8b2b529d31329a552037987a4`;
- build ordering and dependency topology did not change.

This is an integrated and authenticated source candidate, not yet a compiled
S2 or S3 run. Activation waits for the verified CakeML compiler bootstrap.
The generated Dopen corpus contract now names the repaired compiler head and
all five soundness, translation, and permission targets rather than the
pre-repair integration merge.

The integration head also adds `build-local-cakeml.sh`. This is a fail-closed
handoff from proof bootstrap to runtime testing: it reads the required CakeML
commit from the generated Dopen contract, rejects a different or dirty
checkout, requires all five generated x64-64 inputs, applies Candle's existing
memory-layout patch, builds the compiler, regenerates the type insulation, and
prints SHA-256 hashes for the installed artifacts. It therefore cannot silently
fall back to Candle's historical downloaded compiler while testing Dopen.

The same branch has a staged `candle/test_dopen_runtime.sh` gate. It checks
opened values and constructors, nested module paths, qualified lookup after an
open, and precedence on both sides of the open. This synthetic runtime smoke is
only the first post-bootstrap gate; success on the authenticated
`general/debug.hl` source action is still required before claiming the corpus
contract has been exercised.

A separate `candle/test_multiline_string_runtime.sh` gate loads the exact
two-line compatibility oracle and compares its runtime value with an escaped
newline construction. It distinguishes parser acceptance from correct string
contents and is staged after the Dopen smoke in the post-bootstrap sequence.
The two runtime-gate commits are `f8772c2e59378f0d53e5dc7869b682e25326b125`
and `273cd9c25e16963e2068e6230ee4dbe54ae486f9`, respectively.

That bootstrap exposed two additional Dopen proof obligations after the main
type-soundness theorem:

1. `ed49b28052c79abf612642921bff808c16f2332b` proves that constructor-
   environment equivalence is preserved by aligned module lookup and
   `nsOpen`, closing `evaluate_dec_list_eq_evaluate_decs`. The targeted
   `evaluate_decTheory.uo` build passed in 23.23 seconds at 509,768 KiB peak
   RSS.
2. `fec48e7d76b7d3b132ef2d420279a5f0655c76f7` proves that `open_dec_env`
   preserves Candle's unrestricted value permission invariant, closing
   `evaluate_perms_ok`. The targeted `permsTheory.uo` build passed in 1 minute
   32.95 seconds at 1,225,064 KiB peak RSS.

Both proofs contain no cheat, admit, sorry, or replacement axiom. The full x64
bootstrap has resumed from `fec48e7d7`; its live transcript is
`flyspeck-candle-runs/v13-dopen-bootstrap-fec48e7d7.log`. This report does not
claim that the bootstrap or direct Flyspeck corpus gate has completed.

## Bootstrap observation — 2026-08-28T02:07Z

The still-running clean x64-64 bootstrap has passed the basis heap, Candle
kernel and prover, backend lowering and encoding, REPL, declaration compiler,
and source-to-flat compiler layers. In particular, the generated declarations
and incremental source-compile paths were proved after the Dopen repairs, and
`to_flatProg` exported `namespace_nsopen_v_thm` and
`source_to_flat_open_compile_env_v_thm`. The active target is now
`to_closProg`; it has already exported the flat-to-closure whole-program,
declaration-list, and incremental-compile translation theorems and is
translating the closure optimizer.

This is useful end-to-end proof-stack progress, but it is a timestamped live
observation only. `cake.S` and `config_enc_str.txt` have not yet been emitted,
the native compiler has not yet been linked, and none of the staged compiled
Dopen, multiline-string, or authenticated `general/debug.hl` gates has run.
Consequently it adds no S2/S3 acceptance claim.

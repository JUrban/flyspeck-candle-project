# Pristine semantic wire v3 pure-slice hostile rereview (2026-08-30)

## Outcome

The final hostile rereview accepts exact clean project commit
`15f92e566489dfe154b0d2bd0bab4b617d8910fe` for the bounded schema-v3 and
pure-decoder slice.  No P0, P1 or P2 finding remains at that boundary.

The rereview confirmed that every public parser/decoder operation rebinds the
supplied plan and request to the trusted loader's exact project root and exact
parser, pristine-protocol and direct-protocol source bytes; semantic projection
construction requires the matching validated transcript/native closure and
copies serializer identity only from its exact final native event; incomplete
source rederivation has a diagnostic kind disjoint from the reserved final
kind; and all candidate/bundle surfaces remain immediate fail-closed.

It also confirmed both pre-conversion bounds.  A framed decimal greater than
536870912 rejects lexically before `int`, and a canonical declared payload
length greater than the bytes remaining rejects by lexical comparison before
`int` or payload extraction.  The exact `b"2:x"` regression patches
`builtins.int` to fail if conversion occurs and passes only through the intended
protocol rejection.

## Evidence and limit

The five-suite regression passed 96 of 96 tests, the compatibility suite passed
3 of 3, Python byte compilation passed, and `git diff --check` plus worktree
cleanliness passed.  The accepted semantic-wire design blob remained
`74d911a1d7cc8ab9c90740f2fb63a7a3faacf59a`.

This PASS covers only the disjoint v3 schema and pure semantic decoder.  At the
reviewed commit semantic lines still deliberately reject in the full stdout
state machine, source rederivation is diagnostic/incomplete, coverage is null,
and no collector, candidate, comparison descriptor, approval or live evidence
exists.

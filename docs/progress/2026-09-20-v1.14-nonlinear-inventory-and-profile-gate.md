# Direct Flyspeck and reflective checker milestone

Date: 2026-09-20 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`. Action 184 is still running and has not earned
  its success marker.
- The cumulative process has completed LP inputs `hard_1.dat` and
  `hard_10.dat` through `hard_14.dat`. At the 2026-09-20T09:23Z sample it was
  healthy at terminal 120/2,033 in `hard_15.dat`.
- The independent focused action-296 nonlinear process remains CPU-active
  after 20 hours but has emitted no result, so it receives no action, S2, S3,
  or release credit.
- PFT remains alive as the independent oracle. None of these computations was
  interrupted by the reflective work.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Preventive nonlinear runtime-surface audit

The v15 nonlinear first-leaf replay exposed missing `atan` only after
4,595.625 seconds. To avoid repeating that discovery pattern one identifier at
a time, commit `071df9a35614de061c6dc1f3257b6f276ee6fea2` adds a mechanical
inventory of the complete floating-runtime identifier surface in the
authenticated 56-source verifier closure.

The inventory records 94 lexical uses of nine relevant identifiers:

- 13 `abs_float`;
- 4 `atan`;
- 42 `float_of_int`;
- 2 `float_of_string`;
- 4 `floor`;
- 2 `infinity`;
- 19 `int_of_float`;
- 6 `log`; and
- 2 `nan`.

`log` is a verified runtime primitive. `abs_float`, the conversion functions,
`floor`, `infinity`, and `nan` are supplied by Candle's central compatibility
layer. The four `atan` sites are exactly the closed
`2.0 *. atan 1.0` expressions covered by the already validated bit-identical
literal normalization. The scan deliberately includes qualified and
unqualified occurrences and records the resolution class for every name.

The final closure JSON is 426,253 bytes with SHA-256
`0e214376fd7afb1ac23f322a39b0b525ca6533d5ca5140e45cf89d2247013b32`.
All 39 nonlinear closure/controller tests pass. This commit changes closure
authority, target identity, tests, and documentation only; it does not change
the verifier's normalized source bytes.

## Exact-baseline nonlinear profiling gate

The granular profiler has now been integrated onto the exact v16 source and
runtime-compatibility baseline rather than run from its older development
ancestor. The isolated branch head is
`3153f6e5e11f5a8b23acae486d9abcbb96ca6fc3`.

It retains the outer setup/search/reconstruction phases and adds 46 formal
reconstruction-node phases: 16 leaf checks plus separate domain-split and
theorem-glue phases for each of 15 glue nodes. Acceptance requires every
expected phase to close, requires the ordinary theorem gate to pass, and now
rejects any phase record whose lane is not exactly `nonlinear-leaf`.

Focused validation on the combined branch is green:

- 44 nonlinear closure/controller tests pass in 47.645 seconds;
- 3 independent observer tests pass in 0.701 seconds;
- the exact instrumented formal verifier prepares 78,238 bytes, masks five
  loader actions, and passes the real Candle parser in 2.360 seconds with
  normalized SHA-256
  `9c4ae1c590888d4fb1e592a040a4b382ef177b18ead23d2b1073fe66bb3f5a28`;
  and
- the exact instrumented main verifier prepares 33,673 bytes, masks seven
  loader actions, and passes in 1.207 seconds with normalized SHA-256
  `48c122ceb552264c9f126254e0a009f9ae42d4256fd52c5dcd69c6a51dfc87b3`.

Both parser runs returned status zero, empty stderr, and `parse-ok`. These are
syntax and controller gates, not proof or speedup results. The profiler will
run only if the fresh uninstrumented v16 baseline first produces the required
ordinary theorem with the exact conclusion, no hypotheses, and unchanged
axioms. At the 09:25Z sample v16 remained healthy and CPU-active after 50
minutes with a growing log and no error.

## LP reflected-checker gate

The proof-producing Flyspeck linear-combination adapter at
`52e0c5af9ab7d5c06228b31db6a580b8d9c7bef4` has been rerun from a fresh HOL
state. Its exact terminal-scaling, cancellation, hypothesis preservation,
global-axiom, legacy auxiliary-row reset, and fail-closed malformed-input tests
all pass with marker `CANDLE_CV_LINEAR_COMBINATION_FLYSPECK_OK`. The worktree
is clean.

The accepted non-inclusive real-certificate construction gains remain 9.87x,
4.15x, and 3.66x on `hard_2` terminals 15, 5, and 17. No new inclusive benefit
is claimed here. The complete old/new terminal harness remains queued because
another DMTCP restore would add about 18 GiB while swap is already 6.8/8.0
GiB used. Current memory-pressure telemetry shows no ongoing swap-out, so the
useful long-running proof states are being preserved rather than displaced for
that benchmark.

## Plan impact

The preventive inventory closes the known verifier floating-name surface, and
the granular observer is now validated on the exact uninstrumented baseline it
will measure. The next nonlinear decision remains evidence driven:

1. require v16 to pass uninstrumented;
2. run the fresh granular profile on the same source/runtime baseline;
3. use the measured split between certificate search, formal leaf checks,
   domain splitting, and theorem glue to choose the first substantial
   reflected nonlinear boundary; and
4. retain the existing theorem interface and kernel checks, with no raw Boolean
   or externally trusted result accepted as a proof.

For LP, the next evidence remains the inclusive complete-adapter comparison on
the selected real terminals when a fresh restore is memory-safe. No action-zero
replay or expensive relink is triggered by either side lane.

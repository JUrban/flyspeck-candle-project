# Pristine semantic wire design rereview and rebase (2026-08-30)

## Outcome

Two independent hostile rereviews PASS the exact semantic-wire design with no
P0, P1, or P2 finding.  The reviewed design was then rebased onto the accepted
source-authority repair without changing its Git blob.  This is design and
regression evidence only: it does not authenticate a collector, produce a raw
reference, approve promotion, or make PFT evidence eligible for S2/S3.

## Exact reviewed object

- Pre-rebase commit: `2e0949353cb0ec65380d215593a0d434a50be1f1`.
- Design Git blob: `74d911a1d7cc8ab9c90740f2fb63a7a3faacf59a`.
- Design file SHA-256:
  `e029cbf4b07e65865097a837fa7ec34dd297506548a3142583d4420b7be7a0e0`.
- Both reviewers observed a clean worktree and a clean `git diff --check`.
- Both returned PASS with no P0/P1/P2 finding.

The final rereview specifically confirmed exact non-boolean integer typing for
the child/descendant binding; independent opening of the actual filesystem root
before graph trust; identity binding of the supplied attempt descriptor;
position-independent supervisory reads; a dedicated stdin open-file
description and stopped-child offset-zero handshake; descriptor-rooted replay
of all eight captures and three receipts; pending/private candidate status;
the single envelope-before-coverage publication chronology; honest terminal
durability limitations; and the unauthenticated/nonpromotable boundary.

## Rebase preservation

- Accepted authority commit: `b0e24b9a90bd98c09b5b01ed2466853082e6d91c`.
- Old merge base: `fd9be934b727ba87a2ac00b30080fa77068a118b`.
- Post-rebase semantic branch head before this report:
  `9d8d967cd477f22c7de34babd419f5a18f6f947b`.
- Post-rebase design Git blob:
  `74d911a1d7cc8ab9c90740f2fb63a7a3faacf59a`.

The pre- and post-rebase design blob IDs are identical.  The rebase completed
without conflict, and the worktree and `git diff --check` were clean.

## Regression evidence

The authority regression set passed 87 of 87 tests after the rebase:

- `test-pristine-direct-reference-protocol.py`: 10 passed;
- `test-parse-pristine-direct-reference-output.py`: 22 passed;
- `test-direct-release-protocol.py`: 30 passed;
- `test-check-published-direct-result.py`: 18 passed;
- `test-check-published-parser-result.py`: 7 passed.

The separate direct-compatibility-closure suite also passed 3 of 3.  Python
byte-compilation of the authority, parser, loader, direct-release, published-
result, and compatibility modules passed.  Final `git diff --check` and
worktree-clean checks passed.

## Implementation gate

Implementation may now begin with the design's disjoint schema-v3 migration
and pure semantic-line parser slice.  Raw-candidate generation, live pristine
pilots, promotion, and any S2/S3 claim remain disabled until their later gates
and independent hostile reviews pass.

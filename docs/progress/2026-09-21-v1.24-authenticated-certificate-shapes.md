# Direct Flyspeck and authenticated nonlinear certificate shapes

Date: 2026-09-21 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still executing and has not earned
  its success marker.
- The protected direct process remains CPU-active in `hard_5.dat`. At the
  latest sample it had reached terminal **2,004/3,257**.
- PFT remains alive as the independent oracle and is not counted as direct S2
  or S3 evidence.
- No qualified Great100 runtime, source, report, transcript, semantic evidence,
  or other S1 byte was changed.

## The two real nonlinear targets now have complete authenticated shapes

Candle commit `0146586` replaces handwritten partial certificate counts with a
mechanical extraction from each already-authenticated native stdout record. The
extractor binds the `Verifying` record to the exact target label and its theorem
record, rejects ambiguity or crossing into another case, and records every
shape field published by the historical verifier.

The authenticated shapes are:

- `prep-4680581274 delta issue-cayleyR`: 16 pass leaves, 0 raw leaves,
  15 glue nodes, 0 convex glue nodes, 0 monotonicity nodes, and 0
  pass-monotonicity nodes;
- `prep-8657368829` (action 296): 1,061 pass leaves, 0 raw leaves,
  1,060 glue nodes, 0 convex glue nodes, 0 monotonicity nodes, and 0
  pass-monotonicity nodes.

Both target builders regenerate byte-identical current authority JSON after the
new fields are included. Seven target-specific tests and the broader 15-test
target/controller set pass. This is historical target and performance-oracle
authentication only; the native results are never imported as Candle proofs.

The result materially narrows the next implementation step: both selected real
workloads lie entirely inside the current ordinary-pass/ordinary-glue adapter.
Raw, convex, monotonicity, and reference support should remain fail-closed until
a different real checker actually requires them.

## Reconstruction source gate tightened

The uncommitted Flyspeck analytic adapter now additionally rejects:

- nonzero function indices in a single-inequality certificate; and
- any supplied leaf theorem with hypotheses, in both the single and
  disjunctive composition paths.

Negative tests exercise both checks. The complete set of six implementation
modules and seven HOL test/profile drivers passes Candle's fresh-process parser
protocol; all shell drivers pass syntax checks. The two real profile drivers
also require the complete authenticated shapes and exact 16/15 and 1,061/1,060
plan cardinalities before any timing is credited.

This source remains deliberately uncommitted until the actual HOL proofs pass
from a fresh reusable checkpoint restore. Parser acceptance is not proof
evidence.

## Reusable analytic state

`cv-analytic-closure-checkpoint-v4` remains CPU-active and has not emitted an
error or a success marker. It uses the exact dual-segmented analytic overlay and
named input/output FIFOs so every proof experiment can start from a fresh copy
of one clean predecessor state. The older unsegmented v19 attempt is still
preserved while it remains ahead; it has not emitted a semantic result.

## Next gates

1. Finish and authenticate the v4 post-`M_verifier_main` checkpoint.
2. Run the combined single/disjunctive reconstruction proof test from a fresh
   restore and require assumption-free, theorem-interface-equivalent results.
3. Commit the Flyspeck adapter only after that proof gate passes.
4. Profile the real 16-leaf disjunction and the 1,061-leaf action-296 target,
   separating search, adaptive checking, leaf proof production, and exact
   composition from the legacy path.
5. Integrate only a measured theorem-equivalent improvement.

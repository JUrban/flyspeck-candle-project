# 2026-08-27 — selected module subset and `Set.Make` boundary

## Outcome

The exact 400-node direct closure's module queue is classified and gated.  Its
389 structure declarations, nine module-type declarations, one include, and
the ordinary alias/constraint forms match pinned OCaml 4.14.1 in the clean
compiled Candle frontend.  The 15 findings conservatively labelled
`module.declaration_other` are scanner limitations: source review shows that
all are multiline ordinary or signature-constrained structures.

There is one real selected incompatibility:

```ocaml
module St = Set.Make(String_compare);;
```

in `text_formalization/general/serialization.hl`.  OCaml accepts the minimized
functor application.  Clean compiled Candle rejects it at parse time
(`Parsing failed at line 3`).  This is a selected-source blocker, despite the
broad inventory separately reporting zero *functor declarations*: the right
side of this module alias is a functor application.

## Bounded remedy

Candle branch `codex/flyspeck-v13-loader` commit
`f4c95109430266c3a734e4836263e9efe4659d9c` adds normalization
`PROJECT-MODULE-S3-SET-MAKE-001`.  It authenticates serialization source
SHA-256
`fe7f414d6e72a2c838a81b59af15494a1e9f6a5118f9ec0cd1f6ade11a939eed`
and replaces only the exact `String_compare`/`Set.Make` block with a private
string-list set.  The normalized output is authenticated as SHA-256
`062139c28e400951aaab8436298fecdcdb1c5a76b19ec3e19fa65f6fce3915ea`.

The selected source uses `St` only through `empty`, `add`, and `mem`.  It never
observes ordering, iteration, cardinality, or representation.  For strings,
`Set.Make(String_compare).mem` and the replacement's structural-equality
membership agree; duplicate-insensitive `add` therefore preserves every
selected observable result.  Any extra `St` operation, source drift, or new
selected functor application fails the contract and reopens the ledger entry.
This is deliberately not a claim of general OCaml functor or `Set.Make`
support.

## Gates and evidence

- `scripts/test-module-compatibility.sh` compares ordinary structures, module
  types, constraints, aliases, and includes; pins the functor failure; and
  compares the explicit replacement under both frontends.
- `candle/test_flyspeck_set_make_normalization.sh` checks the exact source
  transform, OCaml reference membership across duplicates/empty strings, and
  the compiled Candle replacement.
- The normalization contract now has ten entries and 20 ordered operations,
  SHA-256
  `57da80d5591830bf6d2a56a5275c26a8684db7b0d37e1adf29c357b4adf327d3`.
- The current exact manifest is
  `dd4707140892dd531b55b3d5ae5e05a13bdbcd474aba060c37502ede58497db8`;
  the ten-output overlay receipt is
  `0d14b1b19cb74aabd2cb0b48efdbc84918fa73c92c618f6f6bf2644aae480896`.
- The clean executable remains byte-identical at SHA-256
  `c20b3ec65fc01b6f50a0101c706e18271920530030aa3c381a36ff0fdbd3b23f`.
  The rebuilt source changes generated manifest data only, not boot or binary
  bytes.
- The current direct replay log (after the independent telemetry advance) is
  `/project/flyspeck-candle-runs/v13-direct-clean-noffi-frontier-telemetry.log`,
  SHA-256
  `ddd78faff4adb56cece0c3bb76e0048dba287a26ad9192a42af3a442bc3668c0`.
  It authenticates the ten-output overlay and reaches the same independent
  `open Parser_verbose`/Dopen frontier.

An initial replay against the older clean worktree correctly failed its
generated-source digest preflight.  Fast-forwarding that clean worktree to the
same source commit, while preserving identical compiled artifacts, removed the
provenance mismatch.  No semantic failure was hidden or retried away.

## Claim boundary

The selected module compatibility queue and this one source-normalization
proof are resolved.  The direct run still stops before executing serialization
because verified declaration-open integration remains incomplete.  These
results close part of G2; they are not S1, S2, or S3 evidence.

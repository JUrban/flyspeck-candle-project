# Great100 source-compatibility baseline — 2026-08-28

## Final result

The clean sequential Great100 regression completed all 65 grouped targets:

- 55 `PASS`;
- 9 `FAIL`; and
- 1 inactivity `TIMEOUT` (`100/pascal`).

The run took 105,538.83 seconds wall time.  Its machine-readable report is
`flyspeck-candle-runs/top100-source/report.json`, SHA-256
`cc75cf81f699b5ac6b909731c09781f8cc78dbc7ef2147fad5812e29e0bdff4a`.
The ten non-pass logs were copied out of `/tmp` to
`flyspeck-candle-runs/top100-source/failure-logs/` and are bound by
`failure-logs.SHA256SUMS`.

This is a baseline for clean Candle commit
`110a18d485557ae877d0cb47bb9172e6558ddf61`, executable SHA-256
`d361be3839f31811328d5a0da1ecea15a8a73f369c77e34a288355f16bb930d3`.
It predates the v1.3 CakeML/Candle integration and therefore does not close G3
even for the 55 passing targets.  It is also not Flyspeck S2/S3 evidence.

## Non-pass classification

| Target | Final result | First actionable class |
|---|---:|---|
| `bertrand-primerecip` | fail | decimal-float parsing at `num_of_float`; the rebuilt v1.3 binary must also validate `float_of_num` and `frexp` |
| `ceva` | fail | `Examples/sos.ml` requires `round_num` |
| `constructible` | fail | multiline string lexing |
| `cubic` | fail | term comparator and one-argument `setify` compatibility |
| `e_is_transcendental` | fail | trailing sequence semicolon parsing |
| `heron` | fail | one-argument `setify` requires an explicit term comparator |
| `pascal` | timeout | no output for the 1,800-second inactivity window after `projective_conic`; requires a bounded diagnostic rerun before semantic classification |
| `piseries` | fail | value restriction at `BOUND_SUMPROD_RULE`, with later `round_num` use |
| `ramsey` | fail | unavailable local let-polymorphism, followed by overload-priority risk |
| `thales` | fail | same `Examples/sos.ml` / `round_num` blocker as `ceva` |

The decimal-float and multiline-string parser repairs, typed comparator fixes,
selected `float_of_num`/`frexp` runtime slice, and a Num oracle are already on
the integration lines.  That is not retrospective credit: each must pass
again through the freshly bootstrapped and provenance-bound executable.
Heron, Piseries, Ramsey, the remaining SOS frontier, and the Pascal performance
case remain explicit Great100 closure work.

## Next gate

After the Dopen CakeML bootstrap and Candle link succeed, run the focused
parser/runtime oracles first, then the ten non-pass grouped targets.  Fix
compatibility classes rather than accepting per-test silent behavior.  Only
then rerun all 65 groups from clean state with exact theorem/assumption
fingerprints and resource receipts.  A green complete rerun is the relevant G3
evidence; the present baseline is a diagnosis and regression inventory.

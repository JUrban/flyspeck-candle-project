# Current-head direct stratum plan — 2026-08-29

## Result

The direct-source host inputs have been rematerialized from the exact runtime
pin that is awaiting canonical bootstrap:

- Candle `6f4345057185214016dd7f051a0f3b503950480e`;
- CakeML `964406486a52e1a53a94eade4cf86a666dc8055a` (source pin; not yet linked);
- HOL4 `a390cbabd3a4521bab4ee20281e3e42933a8a3ae`; and
- Flyspeck `1ce0353008eba83d3c76ae9a25c3c242e4802d53`.

This closes a scheduling gap between the already retained parser plans and the
later direct-source run.  It does not record compiled execution and is not S1,
S2, or S3 evidence.

## Published inputs

The fresh normalization overlay is
`/project/flyspeck-candle-runs/v13-normalized-overlay-6f43450`.  It contains 18
exact normalized sources.  Its contract SHA-256 is
`491ca5a204e15fde5454faf63a42343f5cf4a281e03b8b212c937013b992de7e`,
and its receipt SHA-256 is
`c669cea834f67ac1b671edcc00e1187cf5003eebce72ab7885b68b8e91d376ca`.

The fresh generated-input tree is
`/project/flyspeck-candle-runs/v13-generated-lp-6f43450`.  It retains the
119,855,733-byte `formal_lp/glpk/binary/hard_7.dat` with SHA-256
`0ca1b5b6ceba53537ac5d95ffddd883bb297e7d48c30ac241de4f3ec71ab5526`.
The contract SHA-256 is
`dc9f9e5d60b3b43a1275a450e643bb1d8ce4843d517cdf4d84a6dc5c318a8816`,
and the receipt SHA-256 is
`6979641bc74cbaeafbcce2e5f90b1086601651b4d58dc3840ccec2a252deb9a6`.
The receipt deliberately declares the prepared input as mode 0644; read-only
directory modes are not represented as same-UID immutability.

The fresh cumulative plan is
`/project/flyspeck-candle-runs/v13-stratum-plan-6f43450`.  Its exact retained
records are:

- `plan.json`: SHA-256
  `00a305aec30e9461d842be1f2dc936bb97ec39e3c7f3061a1dae1fc6e09c426f`;
- `host-materialization.json`: SHA-256
  `263391828e19e736f90e521b5e39445c103b41741a42f70b7235dd6f331db875`;
- `host-schedule-template.json`: SHA-256
  `ce1e931e1128a5d9c6dadd5113eba2a06fc08138ce918dd71d773560f6d65769`.

The plan independently validated 297 ordered actions, 400 source nodes, 43
generated inputs, 18 normalization outputs, eight cumulative boundaries, and
two diagnostic cutpoints.  The cumulative boundary action counts are 30, 38,
50, 61, 152, 185, 291, and 297.  The two diagnostic prefixes end after 3 and
19 actions and remain explicitly nonpromotable.

All overlay and plan directories are mode 0555 and their retained files are
mode 0444.  The generated tree follows its declared schema-2 mode contract.
All three publications use fresh-root, no-replace publication and retain failed
staging rather than deleting an exchanged pathname.

## Runtime gate

No direct stratum process was launched.  The cold four-stage CakeML proof
replay is still active, so no current schema-6 linked runtime exists.  After
that replay completes, the required order is:

1. pass the independently reviewed canonical prelaunch gate;
2. complete and publish the canonical same-head bootstrap;
3. link and validate ordinary schema-6 runtime provenance;
4. pass the retained 20-file parser pilot and its independent consumer;
5. pass the 400-file parser inventory and its independent consumer;
6. run the compiled compatibility gates and diagnostic direct prefixes; and
7. execute the eight cumulative direct boundaries from fresh authenticated
   processes, retaining and independently validating every receipt.

Any committed change to Candle, the normalization or generated-input
contracts, Flyspeck, or the linked runtime authority invalidates this plan and
requires fresh materialization under a new output root.

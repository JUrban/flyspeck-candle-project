# v1.76: Nested analytic expressions compile and execute as one reflected program

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T21:55:24Z`, the protected cumulative action-184 run had
  freshly passed `hard_7.dat` terminal **8,329 / 9,080** and remained live at
  99.9% CPU with about 17.2 GiB RSS.
- The current cumulative boundary is still action 184. No action-184 pass,
  complete S2/S3 result, qualification, promotion, or release claim is made.

## One typed program now covers nested analytic source

The reflected source language now retains maximal polynomial regions as compact
shared-jet leaves and composes them with negation, addition, multiplication,
squaring, reciprocal, and guarded square root. A structural theorem proves the
value, gradient, and Hessian enclosure semantics for every expression in this
language; no expression-specific arithmetic or derivative theorem is used.

The compiler emits a typed postfix program. Each stack entry carries both a
domain-validity bit and its entire dimension-generic jet. The cval interpreter
executes the full nested program through `Kernel.compute`, failing closed on an
unknown tag, a malformed stack, a reciprocal whose child interval includes
zero, or an invalid square-root enclosure certificate.

For the first nested behavioral fixture,

```text
sqrt (inv (1 + x) + 3), at x = 0
```

one five-instruction analytic program with two compact polynomial leaves
produced and authenticated:

```text
domain = true
value = 2
gradient = -1/4
hessian = 15/32
```

The clean computed-program evidence is:

```text
/project/flyspeck-candle-runs/cv-analytic-expr-program-compute-v15-run-001
```

## The source formula is authenticated mechanically

A theorem-producing source reifier now recognizes polynomial regions plus
negation, addition, subtraction, multiplication, division, reciprocal, square,
and square root. The caller supplies rational square-root enclosure data, but
the reflected domain check authenticates that data before acceptance. The
reifier returns a kernel theorem equating the AST value to the original HOL
source expression.

The source-to-computation regression reifies the fixture above, checks the
expected AST, compiles it, executes it, derives the accepted domain theorem,
and confirms that the global axiom set is unchanged. Its clean fresh-restore
evidence is:

```text
/project/flyspeck-candle-runs/cv-analytic-expr-reify-v3-run-001
```

The success marker is:

```text
CANDLE_CV_ANALYTIC_EXPR_REIFY_RESULT source_nodes=8 analytic_instructions=5 domain=1 value=2 gradient=-1/4 hessian=15/32 correspondence=1
```

The result log SHA-256 is
`76154031b8834e5f99a4e0b77e533c978bc45d99b6161c303f3828fdab6aa502`.

## Checkpoints now follow the active editing boundary

Two independently sealed checkpoints preserve expensive completed stages:

1. Universal analytic expression support:
   `/project/flyspeck-candle-runs/cv-nonlinear-analytic-expr-checkpoint-v2`,
   checkpoint SHA-256
   `cf4af86ece56e7aebda886b3303aebffadf3b99264213fd12da9e955d7030497`.
2. Typed compiler plus proved cval interpreter:
   `/project/flyspeck-candle-runs/cv-nonlinear-analytic-program-checkpoint-v1`,
   checkpoint SHA-256
   `0177fd2833e13864b6e2adace8eb7af292995cd074d12d0b70805db777cfa24e`.

Both seals record a fresh-input acknowledgement before the readiness marker and
authenticate the base checkpoint plus every loaded overlay. A disposable smoke
from the first checkpoint reduced the prior approximately 2.5-minute support
reload to about 9 seconds. A disposable smoke from the newer checkpoint ran the
computed behavioral proof in about 14 seconds. The complete changing source
adapter test now runs in about 17 seconds from a fresh state.

This is the intended moving boundary: stable expensive proof stages are sealed;
thin candidate adapters remain ordinary fragments until another substantial
integrated stage becomes reusable. Dependency changes can fall back to the
nearest compatible earlier seal.

## Source record and next boundary

Candle commits through `ef8e25f` record the universal AST theorem, layered
checkpoint tooling, typed compiler, cval interpreter, and source reifier. The
principal commits are:

- `d06bba6`: universal nested analytic jet soundness;
- `8635e75`: typed nested analytic compiler;
- `23053c3`: proved computed execution;
- `2b19fa6`: post-program checkpoint;
- `ef8e25f`: source-expression reifier.

The next proof boundary is the analytic Taylor handoff: connect the universal
AST derivatives to Flyspeck's Taylor contract, then expose the same prepared
source / repeated-box interface already used by the live nonlinear driver. Once
that integrated certificate stage is clean and materially reusable, move the
checkpoint boundary forward and run a matched legacy/reflected timing from
fresh disposable copies.

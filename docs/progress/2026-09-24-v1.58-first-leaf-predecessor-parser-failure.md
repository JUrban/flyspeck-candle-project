# v1.58: First-leaf predecessor reaches the verifier frontend

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T05:00:42Z`, the protected cumulative action-184 run was
  alive in `hard_5.dat`. Action 184 has not completed and receives no new
  cumulative credit.
- The reverse LP diagnostic was alive and had freshly passed `hard_7.dat`
  terminal **5,876** while descending.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## The long nonlinear predecessor stopped at one frontend construct

The authenticated first-leaf predecessor run

```text
/project/flyspeck-candle-runs/cv-first-leaf-predecessor-checkpoint-v21
```

completed all seven segmented Taylor chunks and all ten segmented
multivariate-Taylor chunks. It then loaded the informal evaluator/verifier
and `m_verifier_build.hl`. The first failure was the following source phrase
in the normalized `formal_ineqs/verifier/m_verifier.hl`:

```ocaml
let mk_real_vars n name =
  map (fun i -> mk_var (name ^ string_of_int i, real_ty)) (1--n);;
```

Candle reported:

```text
ERROR: Type mismatch between string -> string and string * hol_type -> _7 at line 22
```

The run emitted `CHECKPOINT-FAILED` and did **not** emit
`CANDLE_NONLINEAR_FIRST_LEAF_SUPPORT_READY`. Therefore it supplies no real
leaf, certificate, nonlinear-pass, or S3 evidence. This is the already-known
CakeML tuple/application grouping class, not a Taylor theorem or numerical
checker failure.

Stable failure evidence:

- `base.log` SHA-256:
  `5ae480f908ccc1ea6aeebd9e4af19344c118aec244dbf2fe457a8177113960f8`;
- `input-receipt.json` SHA-256:
  `0a905fa101f03eb919c1806f3bd06045d84b29e3a0b774b59fc12e3214a2e81d`;
- `CHECKPOINT-FAILED` SHA-256:
  `2ff898382cb8887e7d75bbb2c7bf03f20c3768b58facc50be3a9b233c55eb8d3`.

## Bounded central repair

Candle commit `618e13c` advances the versioned nonlinear normalization
contract from v15 to v16. At the four exact generated-variable constructor
sites it makes native OCaml grouping explicit:

```ocaml
let variable_name = name ^ string_of_int i in
mk_var (variable_name, real_ty)
```

The transformation preserves the generated name, `real_ty`, returned HOL
term, evaluation order, and theorem construction. It does not authorize a
basename alias or a general parser rewrite. Closure tests assert the exact
source, operation kind, and replacement count, and all dependent first-leaf
target hashes were regenerated from the updated closure.

Validation on the committed bytes comprised:

- 58 Python closure/parser/smoke/target/profile tests, all passing;
- the existing native-OCaml/Candle tuple-constructor normalization oracle,
  passing;
- a fresh parser-only process on the exact normalized `m_verifier.hl`,
  passing in **2.280 seconds** with five source actions and zero stderr.

The focused parser input was 77,506 bytes with SHA-256
`74bf071f7f247cfc34a130827da8f3932d23a63d37442cb491c3321656a7e0ab`;
the normalized source SHA-256 was
`3df1c9a04c169ba81252cd1c8f260fc19b45c492635711521a67fa3409d7be56`.
The parser runtime SHA-256 remained
`3aaa248c0d5d6c96fa8e36445d1a945a751f0cf11a8b734a6142e9fab1088b2b`.

## Replay strategy

The broad 56-source parser inventory was deliberately stopped after 25
sources once it had reached unchanged, previously passing long analytic
inputs. Rechecking only the changed `m_verifier.hl` provided the relevant
fresh parser observation without another roughly 50-minute inventory pass.

The reusable Taylor analytic-base checkpoint remains ready at

```text
/project/flyspeck-candle-runs/cv-flyspeck-taylor-analytic-base-checkpoint-v1
```

It is preserved unchanged. It ends after the stable analytic libraries and
before verifier/certificate support, so it is the preferred seed for the
next bounded suffix experiment. The failed v21 process itself produced no
checkpoint and cannot be resumed. A new multi-hour from-scratch predecessor
should not be launched until the post-analytic suffix has passed from a
fresh copy of that base.


# Direct Flyspeck and reflective checker milestone

Date: 2026-09-20 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`. Action 184 is still running and has not earned
  its success marker.
- The cumulative action-184 process has completed `hard_1.dat` (2,899/2,899),
  `hard_10.dat` (58/58), `hard_11.dat` (291/291), and `hard_12.dat`
  (182/182). At the 2026-09-20T07:40Z sample it had reached terminal 141/197
  in `hard_13.dat`.
- The independent focused action-296 nonlinear process remains alive but still
  has no usable result and receives no action, S2, S3, or release credit.
- PFT remains alive as the independent oracle. None of these computations was
  interrupted by the reflective work.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Complete proof-producing LP terminal shape

The isolated LP branch now reaches the input shape used by a real Flyspeck LP
terminal at Candle commit
`ed4df1e31976d1f33333c202d254ad988af04053`. The new adapter:

1. applies the positive decimal precision scale only to ordinary constraint
   weights;
2. leaves target, variable-bound, and global-linear-sum rows at their supplied
   certificate weights;
3. performs the exact bulk linear combination and canonicalization through
   `Kernel.compute`; and
4. requires exact coefficient cancellation and a strictly negative right-hand
   side before deriving `F` as an ordinary HOL theorem.

Negative certificate weights and nonpositive precision constants fail closed.
The focused interface test scales `x <= 0` by 10 and combines it with
`--x <= --1` at weight 10. It computes `([(0,0)],(0,10))`, derives `F`,
preserves the exact two source hypotheses, and leaves the global axiom set
unchanged. The focused test is green.

A fresh complete real comparison harness is prepared at
`/project/flyspeck-candle-runs/cv-lp-full-hard2-v4`. It independently runs the
complete legacy and reflected public-interface paths for `hard_2` terminals
15, 5, and 17 and compares conclusion, hypotheses, canonical theorem digest,
and axioms while profiling both paths. It is deliberately **not launched**:
the required fresh checkpoint restore adds roughly 18 GiB of resident state,
while three useful long-running computations are active and swap is already
6.8/8.0 GiB used.

The real comparison will also decide one important interface question. The
legacy `var_table` drops auxiliary variable-bound groups whose aggregate
coefficient cancels to zero; the bulk prototype currently retains every
supplied row. Retention is sound, but it may retain extra hypotheses or change
the aggregate right-hand side. If so, the next bounded repair is to reproduce
the legacy's safe group selection before bulk proof construction, not to
weaken theorem equality checks.

## Nonlinear formal-verifier profiling machinery

Fresh nonlinear first-leaf attempt v15 remains active on the committed
12-site IEEE comparison repair. At this sample it had run for about 53 minutes
with approximately 1.17 GiB RSS and had not produced an error or final result.
No nonlinear proof claim is made before this uninstrumented baseline
terminates.

On a separate worktree, commit
`ac8bdbd` prepares fail-closed profiling of that same proof-producing leaf
verifier. The instrumenter authenticates the exact already-normalized
`m_verifier_main.hl` source hash and brackets these existing phases without
authorizing any theorem:

- standardization;
- problem reification;
- evaluator construction;
- informal certificate search;
- adaptive informal verification;
- formal verification; and
- final normalization.

An external read-only `/proc` observer records wall time, CPU, RSS, and
high-water RSS for every phase. Acceptance requires the target-total marker to
close, no phase to remain open, and exact expected marker counts. The default
unprofiled path is unchanged. All 41 nonlinear unit/regression tests and all 3
observer tests pass.

The exact instrumented source also passed a real Candle parser-only gate:
33,673 prepared bytes, seven masked loader actions, return code 0, no stderr,
and 1.255713 seconds elapsed. Its SHA-256 is
`48c122ceb552264c9f126254e0a009f9ae42d4256fd52c5dcd69c6a51dfc87b3`.
This establishes that the instrumentation is runnable source, not yet a
performance measurement.

The profiled fresh replay will start only if the uninstrumented v15 baseline
passes. If v15 instead exposes another bounded compatibility failure, that
failure will be repaired and independently tested before profiling. This
prevents instrumentation results from obscuring a failing baseline.

## Measured benefit and plan impact

The accepted real `hard_2` construction/combination slices remain 9.87x,
4.15x, and 3.66x faster for terminals 15, 5, and 17. They are still explicitly
non-inclusive measurements. The next evidence-bearing LP milestone is the
queued full-terminal comparison, including variable bounds, cancellation,
final contradiction, and theorem-interface equality.

For nonlinear checking, the native target leaf took 365.672506 seconds and
contains 16 `pass`, 15 `glue`, and 0 `mono` certificate nodes. The new phase
split will determine whether the first substantial gain should come from
external/native certificate construction followed by Candle checking, or from
reflecting more of the formal reconstruction arithmetic itself. Either path
keeps Candle's kernel theorem production and existing theorem interface as
the acceptance boundary.

No action-zero replay or expensive relink is being triggered by this side
lane. The current cumulative action-184 verification continues unchanged, and
fresh cumulative confirmation remains reserved for a coherent direct
integration batch.

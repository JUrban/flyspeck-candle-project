# Direct-result consumer handoff — 2026-08-30

## Pinned authority

The independently reviewed consumer is retained in the clean worktree

`/project/worktrees/flyspeck-project-direct-result-gate-9a79224`

at exact project commit
`9a7922443a8bcfd01d1df657884ad2227cf422fd` on branch
`codex/flyspeck-v13-direct-result-gate-authority`.  Its ordinary Git porcelain
status is empty.  Exact source identities are:

- `scripts/check-published-direct-result.py`:
  `2855a6dc49add07dbce01c36505e80a10294ba8c3f1c97b94fa9b5c3f04524bc`;
- `scripts/test-check-published-direct-result.py`:
  `082b5428edc656138942af8122430c04e3c0716e94b94d8d5d3f59a96c678505`.

Do not develop in or advance this worktree.  Any consumer change requires a
new reviewed commit and a separate clean authority root.

## Exact producer invocation

Immediately before every attempt, require the exact schema-6-only linked gate
to pass:

```sh
/usr/bin/env -i PATH=/usr/bin:/bin LC_ALL=C \
  /usr/bin/python3 -I -S \
  /project/worktrees/candle-runtime-pin-964406486/candle/cakeml_artifact_provenance.py \
  check-linked \
  --candle-root /project/worktrees/candle-runtime-pin-964406486
```

Do not use the transition dispatcher's generic `check-linked` here: that
dispatcher also accepts diagnostic schema 7.  The ordinary direct sequence
requires schema 6.  Also require the selected result root and its intended
producer-output siblings to be absent, and run no build, parser controller,
other direct controller, or compiled compatibility workload concurrently in
the Candle worktree.

For the first diagnostic cutpoint, use the exact producer invocation below.
The `C.UTF-8` controller locale is intentional: it produces the authenticated
Python startup with `utf8_mode=0`; the child runtime then receives its separate
fixed `LC_ALL=C` environment from the controller.

```sh
(
set -euo pipefail
result_root=/project/flyspeck-candle-runs/v13-stratum-d0-688d9d1-attempt-001
producer_stdout=$result_root.producer.stdout
producer_stderr=$result_root.producer.stderr
test ! -e "$result_root"
test ! -e "$producer_stdout"
test ! -e "$producer_stderr"
set -o noclobber
exec 3>"$producer_stdout"
exec 4>"$producer_stderr"
set +o noclobber
producer_status=0
/usr/bin/env -i PATH=/usr/bin:/bin LC_ALL=C.UTF-8 \
  /usr/bin/python3 -I -S \
  /project/worktrees/candle-runtime-pin-964406486/candle/flyspeck_stratum_runtime.py \
  --candle-script /project/worktrees/candle-runtime-pin-964406486/candle.sh \
  --plan-root /project/flyspeck-candle-runs/v13-stratum-plan-688d9d1 \
  --boundary d0-diagnostic-through-002 \
  --write "$result_root" \
  --timeout 86400 \
  --max-cpu-seconds 86400 \
  --max-address-space-gib 48 \
  --max-output-file-gib 8 \
  >&3 2>&4 || producer_status=$?
exec 3>&-
exec 4>&-
seal_status=0
/usr/bin/chmod 0444 "$producer_stdout" "$producer_stderr" || seal_status=$?
if (( producer_status != 0 )); then
  exit "$producer_status"
fi
exit "$seal_status"
)
```

These are the complete current-plan substitutions.  Run one row at a time;
the result consumer below must pass before any ordinary row can schedule the
next ordinary row.  The two diagnostic rows are separately authorized by the
completed parser/compatibility handoffs and never schedule an ordinary row.
Run and consume `d0`, then run and consume `d1`; exact consumer passes for both
are the operator gate for starting `00`, despite their correctly false
`scheduling_authority` fields.

| Label | Exact boundary identifier | Actions | Fresh attempt-001 result root | Scheduling authority |
| --- | --- | ---: | --- | --- |
| `d0` | `d0-diagnostic-through-002` | 3 | `v13-stratum-d0-688d9d1-attempt-001` | false |
| `d1` | `d1-diagnostic-through-018` | 19 | `v13-stratum-d1-688d9d1-attempt-001` | false |
| `00` | `00-base-through-029` | 30 | `v13-stratum-00-688d9d1-attempt-001` | true |
| `01` | `01-arithmetic-through-037` | 38 | `v13-stratum-01-688d9d1-attempt-001` | true |
| `02` | `02-nonlinear_support-through-049` | 50 | `v13-stratum-02-688d9d1-attempt-001` | true |
| `03` | `03-analysis-through-060` | 61 | `v13-stratum-03-688d9d1-attempt-001` | true |
| `04` | `04-geometry-through-151` | 152 | `v13-stratum-04-688d9d1-attempt-001` | true |
| `05` | `05-lp_support-through-184` | 185 | `v13-stratum-05-688d9d1-attempt-001` | true |
| `06` | `06-text_formalization-through-290` | 291 | `v13-stratum-06-688d9d1-attempt-001` | true |
| `07` | `07-final_assembly-through-296` | 297 | `v13-stratum-07-688d9d1-attempt-001` | true |

Every row is cumulative and starts a new process from action zero; it does not
resume the previous row's process state.  On failure after `attempt.json` is
committed, preserve the result root and use a new attempt number after
diagnosis.  A safe failure before that commit may cause the controller to
remove its provably owned pre-attempt tree; in that case preserve the sealed
producer stdout/stderr siblings.  Do not reuse or overwrite a failed or
completed attempt.

## Exact consumer invocation

Run the consumer immediately after a direct controller has atomically
published a completed result.  The limits and optional CML sizes must be
identical to those used for that attempt.  For the default controller limits
and the first diagnostic cutpoint, the exact shape is:

```sh
(
set -euo pipefail
result_root=/project/flyspeck-candle-runs/v13-stratum-d0-688d9d1-attempt-001
consumer_json=$result_root.consumer.json
consumer_stderr=$result_root.consumer.stderr
test ! -e "$consumer_json"
test ! -e "$consumer_stderr"
set -o noclobber
exec 3>"$consumer_json"
exec 4>"$consumer_stderr"
set +o noclobber
consumer_status=0
/usr/bin/env -i PATH=/usr/bin:/bin LC_ALL=C.UTF-8 \
  /usr/bin/python3 -I -S \
  /project/worktrees/flyspeck-project-direct-result-gate-9a79224/scripts/check-published-direct-result.py \
  --project-root /project/worktrees/flyspeck-project-direct-result-gate-9a79224 \
  --project-head 9a7922443a8bcfd01d1df657884ad2227cf422fd \
  --plan-root /project/flyspeck-candle-runs/v13-stratum-plan-688d9d1 \
  --plan-sha256 cbe4c7ba55363dabc08eb329bbd7e61a8d7b449cef4401fc647006b2781f5467 \
  --result-root "$result_root" \
  --boundary d0-diagnostic-through-002 \
  --candle-root /project/worktrees/candle-runtime-pin-964406486 \
  --candle-head 688d9d1738a7021501f95b6f0ed788e014fa726f \
  --cakeml-head 964406486a52e1a53a94eade4cf86a666dc8055a \
  --hol4-head a390cbabd3a4521bab4ee20281e3e42933a8a3ae \
  --flyspeck-root /project/worktrees/flyspeck-v13-source \
  --flyspeck-head 1ce0353008eba83d3c76ae9a25c3c242e4802d53 \
  --timeout-seconds 86400 \
  --max-cpu-seconds 86400 \
  --max-address-space-gib 48 \
  --max-output-file-gib 8 \
  >&3 2>&4 || consumer_status=$?
exec 3>&-
exec 4>&-
seal_status=0
/usr/bin/chmod 0444 "$consumer_json" "$consumer_stderr" || seal_status=$?
if (( consumer_status != 0 )); then
  exit "$consumer_status"
fi
exit "$seal_status"
)
```

If the producer was launched with `CML_HEAP_SIZE` or `CML_STACK_SIZE`, append
the exact values as `--cml-heap-size` and `--cml-stack-size`.  Do not add them
otherwise.  A different boundary or attempt number requires the matching
fresh result root and exact boundary identifier; the plan/root/head arguments
remain unchanged while their authorities remain current.

For `d0-diagnostic-through-002` and `d1-diagnostic-through-018`, require exact
`gate=published-direct-result-pass` but also exact
`scheduling_authority=false`.  A diagnostic pass cannot schedule an ordinary
boundary.  For the eight ordinary boundaries, a result is accepted for
sequential scheduling only when the consumer returns all of:

- `gate=published-direct-result-pass`;
- the exact requested `boundary_id` and action count;
- `state=completed`;
- `scheduling_authority=true`;
- `promotion=false`; and
- `s2_s3_evidence=false`.

Each cumulative boundary still starts in a fresh process from action zero.
Preserve every producer result plus the consumer stdout and stderr siblings.
A gate rejection never authorizes rewriting or repairing the published tree;
diagnose it in place and use a new attempt root for any rerun.

## Remaining launch gate

This handoff cannot yet be exercised end-to-end.  It requires the cold proof
replay to finish, the exact-head canonical bootstrap to publish, the ordinary
schema-6 link to validate, and both parser profiles to pass their independent
consumers.  Only then may compiled compatibility diagnostics and direct
cumulative attempts start.  Consumer PASS is scheduling integrity, not
semantic S2/S3 approval or finalization.

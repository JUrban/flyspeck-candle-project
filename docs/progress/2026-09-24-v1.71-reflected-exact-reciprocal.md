# v1.71: Reflected exact reciprocal from the support checkpoint

Date: 2026-09-24 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Direct actions reached / passed remain **185 / 184**.
- At `2026-09-24T18:31:40Z`, the protected cumulative action-184 run had
  freshly passed `hard_7.dat` terminal **7,841 / 9,080** and remained healthy
  at one computation core with approximately 17.2 GiB RSS.
- No action-184 pass, complete S2/S3 result, qualification, promotion, or
  release claim is made.

## First action-296 numeric primitive

The first bounded extension toward the action-296 analytic language is a
generic exact rational reciprocal. The data operation is total: it computes
the exact reciprocal for positive and negative nonzero rationals and returns a
fixed zero representation for zero. Its mathematical theorem remains guarded:

```text
|- !q. candle_q_nonzero q
       ==> candle_q_real (candle_q_inv q) =
           inv (candle_q_real q)
```

The reflected `cval` operation has a representation theorem for every encoded
rational:

```text
|- !q. candle_cv_q_inv (candle_cv_q q) =
       candle_cv_q (candle_q_inv q)
```

This removes the exact numeric reciprocal operation as a primitive blocker.
It does **not** yet provide an interval reciprocal constructor, discharge its
nonzero-domain side condition for a box, or extend the expression reifier with
the source-language `inv` node. Those are the next connections needed before
the authenticated action-296 formula can use it.

## Fresh disposable validation

The production form of the source and test was loaded from a fresh disposable
copy of the reflected-support checkpoint. The restored process acknowledged
nonce
`446a19898b10794a3ce87bd6db19e9c467cb1ed80af790c8378a4a3712c8fcee`
at log line 1 before the fragments ran. It then proved:

```text
CANDLE_CV_EXACT_RATIONAL_INV_RESULT cases=4 sound=2 theorem_producing=true
CANDLE_CV_EXACT_RATIONAL_INV_OK
```

The four computed cases cover positive, negative, noncanonical, and zero
representations. Two nonzero cases were separately handed through the guarded
real-semantics theorem. Every resulting theorem is assumption-free, and the
global axiom set before and after the test is identical.

The complete restore, source proof, computation, and theorem-handoff test took
**16.29 seconds** wall time and **2,252,544 KiB** peak RSS. This is the first
new proof-development measurement from the support-only checkpoint and
confirms that the checkpoint is at the useful editing boundary: the stable
analytic libraries are reused, while the changing primitive and test are the
only overlays.

Evidence root:

```text
/project/flyspeck-candle-runs/cv-exact-rational-inv-v24-run-001
```

Evidence identities:

- source SHA-256:
  `44eb18371c20b92bf000d6ee8dc6f541b73d7e7720e333a8e553eaf353f91914`;
- test SHA-256:
  `e7dc0c7dde02e0171ea60d71d8606ab39767ddfb22290c0a1d7fe11b3aadf938`;
- fragment-set SHA-256:
  `b9c7600a80f3517e17d9fc7d5640038891cb9f05322527b899abc77085bce3a8`;
- generated-input SHA-256:
  `928ebd6d9a64e632ccd83d60d37a1270dd388d3cca8bcd89f98997d38e22dc2a`;
- result-log SHA-256:
  `3c013891667479b467735863b90a7ae4354ea7f7ddd7b77c9904a1bdd333c0fb`;
- fresh-input receipt SHA-256:
  `a1927370191463bb187a0d5fcc02213c3dbd6ed406c818500e232aa796c09dbe`.

## Source record and next boundary

Candle commit `1893e3175a2f27d4fc0698d384a9aa3dd355f29a` records the
operation, its ordinary and reflected correctness proofs, and the focused
checkpoint-backed regression.

The next coherent stage is the interval-level reciprocal constructor and its
domain proof, followed by the corresponding generic analytic AST/reifier node.
The same pattern can then be applied to square root; arctangent and a checked
interval enclosure of `pi` remain required for the full action-296 source.
Only after a small composed analytic term passes end-to-end should this stable
support boundary be advanced again.

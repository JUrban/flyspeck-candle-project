# Coarse reflection, LP late-state safety, and Taylor localization

Date: 2026-09-20 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still executing.
- The uninterrupted cumulative run has completed `hard_1.dat` and
  `hard_10.dat` through `hard_14.dat`. At this report sample it had proved
  terminal **931/2,033** in `hard_15.dat`.
- The independent focused action-296 nonlinear process remains CPU-active and
  silent. It has no action, S2, S3, qualification, promotion, or release
  credit.
- PFT remains alive as the independent oracle. None of these processes was
  interrupted or used as proof evidence for reflective development.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## Coarse proof-producing multiplication and rounding

Candle commit `72bab2b` on `codex/cv-native-float-v1` proves one reflected
program covering exact mantissa multiplication, exponent addition, and
directed lower/upper radix rounding behind one verified `Kernel.compute` call.
It supplies ordinary and cval correctness/soundness theorems. The clean suite
passed in 2:06.10 with 1,090,432 KiB peak RSS.

Flyspeck commit `1715679c` on `codex/flyspeck-cv-native-float-v1` adds the
proof-producing adapter for wrapped and raw radix spines and a selectable
integration into the existing `Arith_float` multiplication interface. Legacy
behavior remains the default. Clean direct and integration suites passed in
3:58.68 and 4:19.08, with 1,089,536 and 1,093,120 KiB peak RSS. They require
exact legacy/reflected conclusions and empty hypothesis lists.

On two 60-decimal-digit mantissas at precision 8, measured from one warm Candle
state using process CPU ticks:

| Complete multiplication path | Repetitions | CPU time | Per call |
|---|---:|---:|---:|
| legacy | 10,000 | 6.29 s | 0.629 ms |
| coarse reflected | 10 | 0.59 s | 59.0 ms |
| fine reflected operations | 100 | 11.17 s | 111.7 ms |

The coarse boundary is **1.89x faster** than fine-grained reflection but still
approximately **94x slower** than the specialized legacy conversion. This is
a plan-changing negative result: further per-operation replacement is not a
credible speed path. The proved arithmetic remains useful machinery, while
performance work moves outward to interval/Taylor or certificate batches that
amortize representation and theorem handoff.

## Complete LP-terminal path

Candle commit `d163f1f` on `codex/cv-lp-linear-combination-v1` makes the
prototype safe to load after real Flyspeck action 183:

- the wrapper's subtraction lemma uses an assumption-order-independent
  arithmetic proof instead of a late-state-fragile `CHOOSE_TAC` sequence; and
- HOL binders named `row` are renamed `lcrow`, preventing capture by
  Flyspeck's already defined `row` constant.

The unchanged theorem contracts passed the clean complete-compute, core, and
full Flyspeck-adapter gates. The full adapter gate took 4:00.22 with 1,089,536
KiB peak RSS.

Real `hard_2.dat` attempt 5 authenticated those exact bytes and restored a
fresh clean post-action-164 checkpoint. It was deliberately rejected by the
resource guard after 203.07 seconds, during the action-166 source closure and
before certificate decoding or a selected terminal call. Aggregate DMTCP proof
RSS was 93,093,596 KiB and new swap-out reached 129,605 pages (about 506 MiB),
past the 65,536-page/256-MiB limit. Only the comparison process was terminated.
The driver returned status 143 and no terminal pass marker was emitted.

This is a resource result, not a theorem-path failure. The late-state fix has
not yet been exercised at the real terminal boundary. Retrying while swap is
full would conceal pressure rather than answer the proof question; the next
restore waits for substantially more headroom or completion of one protected
18-GiB proof process.

## Nonlinear reconstruction localization

The prior first-leaf replay consumed about 1.2 GiB but exited after 11,108
seconds while entering the 3,701-line single-module phrase
`taylor_interval-compiled.hl`. Its dependencies closed, but it emitted no
verifier or reconstruction marker. That left frontend elaboration and first
proof execution indistinguishable.

Candle commit `8383f33` on `codex/cv-nonlinear-profile-v3` adds an authenticated
development-only localizer. It pins the original Taylor source SHA-256, emits
one marker immediately before the module phrase, one at the first body
structure item, markers at all 28 section boundaries, and final body/module
markers. All marker expressions discard unit and do not construct a theorem;
the public module interface, theorem statements, proof tactics, and first-leaf
checks remain unchanged. Ten focused authority/profile tests pass in 25.56
seconds.

The fresh localized replay is active in
`cv-nonlinear-first-leaf-v18`. The diagnostic interpretation is bounded:

- frontend-begin without execution-begin localizes the cost to whole-module
  parsing/inference/elaboration;
- execution-begin plus section markers localizes proof execution; and
- a complete closure then enables the already prepared verifier/reconstruction
  phase profiler.

## Next decisions

1. Preserve the direct cumulative, focused nonlinear, and PFT processes.
2. Let the Taylor localizer answer the frontend-versus-proof boundary before
   another nonlinear architecture change.
3. Retry the full real LP terminal comparison only with safe host headroom.
4. If the LP terminal path passes, measure the complete reflected boundary;
   extend reflection only where the real comparison shows a net benefit.
5. Continue toward a coarse nonlinear certificate/Taylor batch rather than
   replacing already efficient individual arithmetic conversions.

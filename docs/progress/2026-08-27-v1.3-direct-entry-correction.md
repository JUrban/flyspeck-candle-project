# 2026-08-27 — direct loader entry-route correction

## Result

Candle loader commit `21e93dad706842a2c5143e1bd811fc9b1fb4181d`
corrects the generated source graph to match what the enforcing loader actually
executes. Its explicit entry roots are now exactly:

1. `candle:hol.ml`;
2. `flyspeck:text_formalization/build/strictbuild.hl`.

`strictbuild.hl` itself reaches `build/build.hl`, and the loader maps the
authoritative `Build.build_sequence_full`. The convenience file
`load_flyspeck.ml` is not executed: it hard-codes `/home/user` paths and is not
referenced by `flyspeck_loader.ml`. The preceding manifest nevertheless added
it as an extra root, mixing repository inventory into an execution-route claim.

## Corrected evidence

Removing the false root changes the exact graph as follows:

| Measure | Previous | Corrected |
|---|---:|---:|
| Source nodes | 399 | 398 |
| Selected edges | 419 | 417 |
| Reachable `#load` directives | 6 | 5 |
| Qualified runtime uses | 41 | 39 |
| Open-attributed uses | 3 | 3 |
| Total capability occurrences | 44 | 42 |

The removed runtime records are `load_flyspeck.ml:1` `unix.cma` and its two
placeholder `Unix.putenv` calls at lines 7–8. They remain facts about the
repository but are no longer claimed as observations of this loader route.
The current contract has two reachable `unix.cma`, three `str.cma`, 25 `Str`
occurrences over five members, and 17 `Unix` occurrences over six members.

Regression tests freeze the two-entry bootstrap list, absence of
`load_flyspeck.ml` from `source_nodes`, 398/417 graph sizes, five directive
coordinates, and 39 + 3 capability counts. All 15 manifest tests and the
compiled `Str` differential gate remain green. The direct runtime frontier is
unchanged at the original `strictbuild.hl:21` `#load "unix.cma";;`.

The corrected manifest SHA-256 is
`ff7c6d79eb751566ddd55c60892fee2087a04bf32b61c659baa0d104de1c155c`.
This is still G6 inventory/policy evidence, not S1/S2/S3 acceptance evidence.

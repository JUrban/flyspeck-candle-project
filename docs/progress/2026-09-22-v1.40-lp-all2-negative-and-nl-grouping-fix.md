# v1.40: LP aggregate-relation negative result and NL grouping fix

Date: 2026-09-22 (UTC)

Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Actions reached / passed remain **185 / 184**.
- The protected cumulative action-184 worker remains healthy in
  `hard_7.dat`; its valid output prefix reached terminal **2,117 / 9,080**
  during this work.
- The replacement nonlinear predecessor load is running from a fresh state
  with no source/proof error marker. The failed predecessor is not reused.
- No action-184 pass, full S2/S3 result, qualification, promotion, or release
  claim is made. PFT remains an independent oracle only.

## Aggregate `ALL2` source correspondence is rejected

The next bounded LP prototype replaced the concrete decoded-list equality
chain with one `ALL2` relation. For each sparse/source entry pair it proved
the exact decoder equality from the cached coefficient and selector theorems;
one generic theorem converted the completed relation to exact source-list
equality, after which the existing generic `lin_f` denotation theorem supplied
the usual result.

The final run used the same deterministic 416 rows spread over the exact
1,662-row five-terminal `hard_2.dat` master and retained the complete
610-variable basis. It passed all of the intended gates:

- exact structural source-list match;
- hypothesis-free final linear-denotation theorem shape;
- identical coefficient-cache population (859 hits, 70 misses per measured
  path);
- unchanged global axiom set.

The candidate is slower:

| 416-row stage | wall time (s) |
|---|---:|
| cached numeral standardization | 0.05 |
| current decoded-entry construction, same run | 39.56 |
| aggregate `ALL2` through `lin_f` denotation | 50.58 |

The aggregate relation costs 27.9% more than the same-run entry constructor.
It is also 38.6% slower than the prior 36.49-second complete-denotation
measurement. Selector-context preparation varied between the two stages
(26.77 versus 21.00 seconds), but those costs are outside the reported proof
times and favor the rejected candidate. The result therefore cannot be
explained by a more expensive candidate context.

This rejects a second local HOL list architecture, after the earlier
per-entry rewrite path. The current list/pair congruence constructor remains
the best measured local method. Any later attack on the remaining cold master
cost should move to a genuinely coarser authenticated-source/checker boundary
or reuse the exact checked master across a sufficiently large workload; it
should not add another relation proof around every entry.

Evidence:

- log SHA-256:
  `dec1c1f27252d3fd6dfbb2c6d016d68f004990dfffe45ef63f99025ad59261`;
- external profile SHA-256:
  `94987ca3360131d9698ed5bc44514a9d4b8bd9bdff12da867269629eb15e30f0`;
- exact generated input stream SHA-256:
  `49aacb52749cc9dafe059ff9fa6c8bc450fb794529baa6a5128c905be7d9b2e8`;
- sample driver SHA-256:
  `875784e378edcd43e5c4d5cdeaeff7ed4e4cc675671cfa8a217aab7094f41b4f`.

## Nonlinear predecessor failure and durable correction

The previous genuine-leaf predecessor finished all segmented Taylor sources
and then failed at the first nested-array expression in `m_taylor.hl`.
Normalization had changed

```ocaml
(rand o lhand o concl) array.(n).(i)
```

to an unparenthesized `Array.get` application. Candle consequently grouped
the accessor as additional arguments to the composed function.

The closure normalizer now emits every lowered nested-array read as one
parenthesized expression. A native OCaml equivalence test specifically covers
the function-application boundary, all 21 closure tests pass on the focused
branch, and the full integrated 48-test nonlinear suite passes. The commits
are:

- focused verifier: `2401471`;
- integrated predecessor: `3ed10e4`.

An authenticated v14 predecessor was regenerated from the integrated commit
and started from a clean runtime. Its success path is already chained to a
v15 experiment-support checkpoint, then to the unchanged first genuine-leaf
capture, then to one fresh restore that captures leaves 4, 8, and 15. The
varied capture shares reconstruction/search/plan preparation once and records
the exact Taylor inputs plus native/reflected phase timings. Every handoff
requires its predecessor's ready marker and recorded hashes; partial or failed
states cannot enter the fixture collection.

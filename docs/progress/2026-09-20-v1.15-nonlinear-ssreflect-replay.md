# Direct Flyspeck and reflective checker milestone

Date: 2026-09-20 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`; action 184 is still executing and has not earned
  its success marker.
- The cumulative process has completed `hard_1.dat` and `hard_10.dat` through
  `hard_14.dat`. At the 2026-09-20T10:03Z sample it was healthy at terminal
  223/2,033 in `hard_15.dat`.
- The independent focused action-296 nonlinear process remains CPU-active
  after 20.7 hours but has emitted no result, so it receives no action, S2,
  S3, or release credit.
- PFT remains alive as the independent oracle. None of these computations was
  interrupted by the reflective work.
- No qualified Great100 runtime, source, report, transcript, or semantic
  evidence byte was changed.

## First-leaf replay v16 result

The sixth fresh nonlinear replay passed `trig/cos_bounds_eval.hl`, so the four
authenticated `2.0 *. atan 1.0` normalizations are now validated on the real
closure. It then stopped before reconstruction at
`formal_ineqs/lib/ssreflect/ssreflect.hl:64`: the copied `THENL_FIRST` /
`THENL_LAST` helper still used the old tactic-state shape and did not propagate
the second sequence's instantiation into the first sequence's pending goals
and justification.

This is a source-compatibility failure, not a failed nonlinear theorem or
certificate check. The preserved run reports:

- outcome `proof-failure`, exit status zero, and no forbidden log fragments;
- no reconstruction, smoke-theorem, target-theorem, or pass marker;
- 4,394.047 elapsed seconds, 4,382.414 child-user seconds, and 4.207
  child-system seconds;
- 1,197,056 KiB peak RSS; and
- a 9,664,504-byte log with SHA-256
  `23d8c3eb33c41950bbde85b72a2e8063850b7ee0aaac1c5145831a3dd7b79f2a`.

## Canonical `ssreflect` compatibility repair

Commit `88edf6ba56c054e1679aa30a7d84325e6c78c002` repairs the formal-verifier
copy without creating another handwritten tactic normalization. Compatibility
closure v9 selects the six applicable exact-byte operation objects from the
versioned direct-Flyspeck normalization for
`jHOLLight/caml/ssreflect.hl` and applies them to the authenticated
module-wrapped sibling. The obsolete dynamic-lookup operation is deliberately
excluded because the sibling already comments that block out.

The derivation fails closed if the canonical source or operation set changes,
if an operation kind changes, if an anchor is absent or duplicated, if a
second normalization lane overlaps it, or if the resulting identity differs.
Tests compare the actual `before` and `after` bytes of every derived operation
with the canonical contract objects.

The resulting closure has 27 normalized sources and 146 operations. Its
431,963-byte JSON has SHA-256
`3ce968d6b6dec45c05800527a5f3dfddea2fa5e44b49a5aac8da1895db1d1ef2`;
the regenerated first-leaf target has SHA-256
`6d84cd963c3bf4eead49ea87ea777827cc6885d782742c6a0591c7854f17cfbe`.

Validation is green:

- all 24 closure/target authority tests pass;
- the complete nonlinear regression suite passes all 40 tests in 39.381
  seconds; and
- a fresh runtime gate reproduces the original CakeML type mismatch, while
  the normalized helper constructs both `T /\ T` test theorems in fresh HOL
  states.

## Fresh exact replay

Uninstrumented first-leaf replay v17 is running from the clean exact commit
`88edf6ba56c054e1679aa30a7d84325e6c78c002` in
`cv-nonlinear-first-leaf-v17`. It uses the unchanged development runtime and
authenticated Flyspeck source. The prepared phase profiler remains gated: it
will be rebased onto this exact v9 closure and run only after the ordinary
unmodified proof reaches the required theorem with the exact conclusion, no
hypotheses, and unchanged axioms.

## Plan impact

The v16 failure does not change the reflective design. It did improve the
compatibility process: duplicated Flyspeck source siblings now consume one
canonical versioned repair rather than drifting independently. The next
decision remains evidence-driven:

1. allow v17 to establish the uninstrumented nonlinear theorem baseline;
2. if it succeeds, integrate the already-tested per-node profiler onto this
   exact closure and measure search, leaf checking, domain splitting, and
   theorem glue;
3. choose the first substantial nonlinear reflected boundary from those
   measurements while preserving the existing theorem interface and kernel
   checks; and
4. run the complete inclusive LP comparison when another large restore is
   memory-safe. The accepted non-inclusive LP construction gains remain
   9.87x, 4.15x, and 3.66x on the selected real `hard_2` terminals.

No action-zero replay, qualified-evidence mutation, or expensive relink was
triggered by this batch.

# Direct Flyspeck: actions 145--153 and `List.flatten`

Status: DEVELOPMENT / NON-RELEASE. This report makes no S1, S2, S3,
qualification, promotion, or release claim.

## Headline Flyspeck progress

- Highest exact clean-predecessor prefix is now actions **000--154 =
  155/155**. The independent action-zero `f958692` replay has reconfirmed
  actions **000--144 = 145/145** and is evaluating action 145.
- The exact `ca3e129` candidate replay has passed actions **000--144 =
  145/145** and is evaluating raw action 145.
- Focused clean-predecessor green prefix: actions **000--154 = 155/155**.
- Current cumulative boundary: `05-lp_support-through-184`.
- First repaired failure in this batch: action 153,
  `formal_lp/glpk/glpk_link.ml`, `Undefined variable: flatten at line 78`.
- First current failure beyond action 153: observed at action 155,
  `formal_lp/glpk/lpproc.ml`, at the fixed `Printf.fprintf outs "%s" j`
  construct already repaired in the `ca3e129` candidate overlay.
- Full direct S2: open.
- Nonlinear plus LP / S3: open.

The fresh action-zero run is
`/project/flyspeck-candle-runs/v124-dev-direct-boundary05-afb59ae-cc36ecadd-heap16-attempt-001`.
Its exact nonce-bound ledger contains actions 000--152 and no action-153
success marker. The 15,613,411-byte log has SHA-256
`06fa8b9d74e9e8fccb6c6a7065c4f2517eeddf4f96dd4cd9c1b28856396cb9a6`.
It ran for 4:44:45 wall time with a 17,106,432 KiB maximum resident set.

This run is also the first fresh cumulative confirmation that the central
nested-module separator repair accepts unchanged
`formal_lp/hypermap/ssreflect/add_triangle-compiled.hl` at action 145. Actions
146--152 then pass, including `arith_link.hl`. The raw action-145 acceptance is
functionally important but substantially slower than the superseded
source-wrapper development probe; performance investigation remains secondary
to the current functional lane.

## Action 153 diagnosis and correction

`glpk_link.ml` opens OCaml's standard `List` module and calls unqualified
`find_all` and `flatten`. Candle's compatibility module already provided
`concat`; `afb59ae` added the standard `find_all` alias. The cumulative run
passes the `find_all` use and stops at `flatten`, proving that the remaining
defect is the omitted standard `List.flatten` alias rather than a Flyspeck
algorithm or proof failure.

Native OCaml 4.14 specifies `List.flatten` with the same behavior as
`List.concat`. Candle now supplies the central compatibility binding
`let flatten xss = concat xss`. A native/Candle oracle checks both qualified
`List.flatten` and unqualified `flatten` after `open List`, including empty
sub-lists and element order. The oracle and the existing GLPK fail-closed
formatter/`find_all` checks pass. The base-prefix compatibility test was
updated so raw `List.flatten` must now type and its independent intentional
failure count remains exact.

The compatibility binding is commit `8851b8f`; focused runtime-root test
repairs are `14e812f` and `d2d3964`. The mechanically regenerated source graph,
loader digest-program pin, and manifest fixed point are commits `013a0e8` and
`f958692`. The final development head is
`f9586926635abbb019eae6b63dab959ae3da4e5a` and is clean.

All 18 normalization tests, 28 manifest tests, 10 stratum-planner tests, the
focused native/Candle GLPK oracle, the base-prefix runtime gate, and the full
297-root/400-source manifest fixed-point check pass. No Flyspeck source or
normalization output changed. The existing 47-output overlay remains at
contract SHA-256
`396e82a76d740b6ac86945870e472103e80ad6cdd7d92e67920695d68d044213`.
The regenerated 297-action plan is
`/project/flyspeck-candle-runs/v126-stratum-plan-list-flatten-f958692-dev-001`,
SHA-256
`e7fc0860cadc69f0019c39d01d77e82914b3599b1ced70e99d32d0531205ddbf`.

## Confirmation state

The clean post-action-152 DMTCP predecessor is complete at
`/project/flyspeck-candle-runs/v125-post-action152-cumulative-checkpoint-afb59ae-cc36ecadd-dev-001`.
It contains exact actions 000--152 and was captured before action 153 was
loaded. The immutable 2,975,051,857-byte image has SHA-256
`ddfdd24a49b8c00464555a50edad193e31ede964b396bf5a5bc9942e4c29fd33`.

Because that state predates the one-line central alias, the focused candidate
probe re-exports the already-loaded `List` module and adds exactly the
`flatten` binding before evaluating action 153. A fresh snapshot copy completed
exact action 153 with its expected nonce-bound marker and no diagnostic at
`/project/flyspeck-candle-runs/v128-focused-action153-list-flatten-post152-probe-001`.
Its input SHA-256 is
`1bffffedeef519420bad23469a4c867bb9bb50552675560ec9f96e49c969d6ff`
and its log SHA-256 is
`0c1bf8e81a54d3db1e58e41005ffa7e098ff77ed82f35f9dc0cd61e3b76b950f`.

A separate fresh snapshot copy began collecting the complete available failure
set across actions 153--184 at
`/project/flyspeck-candle-runs/v129-focused-suffix153-through184-list-flatten-post152-probe-001`.
It passed action 153 exactly and emitted three of action 154's forty archive
thunks. The measured rate made the original fixed 7,200-second harness bound
inadequate, so that attempt was terminated and marked incomplete before the
bound could discard more work. The retained action-153 marker remains valid;
no action-154 credit is claimed.

The focused runner now accepts a validated positive timeout while retaining
the same fail-closed snapshot and fresh-copy checks. Its 28,800-second run at
`/project/flyspeck-candle-runs/v130-focused-suffix153-through184-list-flatten-post152-probe-002`
passed exact actions 153 and 154 from a fresh copy of the clean predecessor.
The action-154 marker identifies original source SHA-256
`703ea865a124aa69f0ee12d94df7065bbbf5701a3085776e24632d14493474db`
and ledger delta SHA-256
`7dd8fd545efc5f0e4493ae2831ff938c54183e4fe064ba50c715266e8e383264`.
It then selected the superseded overlay's `lpproc.ml` for action 155 and
reported the expected fixed-format parser failure: `need should be followed by
a "string literal" and then a double semicolon [;;]`. The final suffix
postcondition consequently failed. The shell-level exit-status file says zero
because the restored REPL handled both exceptions; absence of the suffix
success marker and the two explicit exceptions are the semantic result. The
input SHA-256 is
`7315636a2426ccdd0770d7735497d34909d85e7371126ab59297ad6adde17a18`,
and the 8.5 MiB log SHA-256 is
`719a3931dcdedb801624b58b0d3bd18c64420a7c9766422d878a77b28be9cbf4`.

This is the first actual post-action-153 failure and independently confirms
the static classification behind the `ca3e129` action-155 normalization. A
new candidate-overlay run is active from another fresh copy of the immutable
post-action-152 state at
`/project/flyspeck-candle-runs/v134-focused-suffix153-through184-lp-fixed-ca3e129-post152-probe-001`.
Its exact suffix is extracted from the hash-pinned cumulative `ca3e129`
program; all 50 candidate outputs are reverified before evaluation. This
focused probe necessarily disables the predecessor's one-shot source observer,
which is pinned to the superseded overlay's absolute paths, but retains the
nonce-bound action-ledger checks. It is therefore DEVELOPMENT / NON-RELEASE
functional evidence only and cannot replace the active exact action-zero
`ca3e129` replay for source-trace or cumulative confirmation.

The exact final-head fresh action-zero boundary run is active at
`/project/flyspeck-candle-runs/v127-dev-direct-boundary05-f958692-cc36ecadd-heap16-attempt-001`.
Only its emitted exact action ledger will count as cumulative confirmation of
the central fix.

That run has since passed raw `packing/marchal3.hl` at action 91 from action
zero, completing the required fresh cumulative confirmation of the Marchal
candidate. It then passed raw `packing/GRUTOTI.hl` at action 92 and
`packing/KIZHLTL.hl` at action 93. The GRUTOTI interval contains exactly 2,364
MESON progress records and ends in its exact nonce-bound marker. GRUTOTI has no
normalization-contract entry or overlay output, so this is direct cumulative
confirmation of the repaired module-name-resolution path rather than an
individual proof workaround.

The same fresh run has now passed raw
`jHOLLight/Examples/ssrnat-compiled.hl` at action 130. It established the
unchanged terminal theorem `Ssrnat.sqrn_distn` and emitted the exact
nonce-bound action marker before entering action 131. The retired
file-specific action-130 normalization has no entry or output in the current
contract, so this is fresh action-zero confirmation of the central nested
module-item separator repair beyond the former 130-action cumulative ceiling.

Raw `text_formalization/fan/hypermap_iso-compiled.hl` then established the
unchanged terminal theorem `Hypermap_iso.fan_hypermaps_iso2` and passed action
131 with its exact marker. It likewise has no current file-specific
normalization. Actions 132--140 subsequently emitted all nine ordered markers,
bringing this fresh cumulative replay to **141/141** through action 140 before
it entered `jHOLLight/Examples/seq-compiled.hl` at action 141.
Raw action 141 then established the unchanged terminal theorem
`Seq.allpairs_catr` and emitted its exact marker. It also has no current
file-specific normalization, completing fresh action-zero confirmation of the
central module-item separator across the former action-130, action-131, and
action-141 normalization sites.

Raw action 142 then established the unchanged terminal theorem
`Seq2.prev_el_hd`; raw action 143 established `Sort.min_k0_le_EL`. Both emitted
their ordered nonce-bound markers, and neither source has a current
file-specific normalization. The `f958692` replay is therefore **144/144**
through action 143 and has entered action 144,
`tame/ssreflect/FNJLBXS-compiled.hl`.

Normalized action 144 then established the unchanged terminal theorem
`Fnjlbxs.FCDJDOT` and emitted its exact marker. Its selected overlay object is
identical in the 47-output and 50-output contracts: normalized SHA-256
`4c80875e3e56baf6fef69cc3a5ac3a43ca868f290b1bd8a1c957acc9654557e1`.
The `f958692` replay is now **145/145** through action 144 and is evaluating
raw action 145, `formal_lp/hypermap/ssreflect/add_triangle-compiled.hl`.

## LP fixed-format candidate batch

A bounded static preflight of actions 155--184 found a shared source-contract
class before the long archive action completed. Action 155,
`formal_lp/glpk/lpproc.ml`, contained a live fixed `Printf.fprintf outs "%s"`
call. Actions 170, 173, 176, 182, 183, and 184 contained fixed progress-only
`sprintf` calls. Candle deliberately keeps the general, dynamically formatted
GLPK process route fail-closed; these sites do not justify enabling it.

The candidate normalizations replace only the fixed string/integer progress
formats with concatenation, `string_of_int`, or `output_string`. The one
action-184 elapsed-time `%f` notice is replaced by a deterministic statement
that the outer runner records total verification time; the timed proof
computation and its result are unchanged. A native equivalence oracle covers
exact progress bytes, including minimum and maximum OCaml integers; a Candle
fixture checks the corresponding normalized constructions. The focused gate,
all 18 normalization tests, all 28 manifest tests, all 10 stratum-planner
tests, and the 297-action/400-source/43-generated-input manifest fixed point
pass.

This batch is Candle commit `ca3e1298c4192863b80ea949e0a3233d093ccd09`
on branch `codex/v51-lp-fixed-format-progress`. The immutable 50-output overlay
is
`/project/flyspeck-candle-runs/v131-normalizations-lp-fixed-format-ca3e129-dev-001`,
with contract SHA-256
`e8a2a41fe294c0476fec6fbb203d190771b4839ea1266ce1207ff58708505cec`
and receipt SHA-256
`00a5eed26d2035adaf8b40e5b77bd9dfc1b2aaee2f04a0cb947960fba7c2ce4c`.
The independently reconstructed plan is
`/project/flyspeck-candle-runs/v132-stratum-plan-lp-fixed-format-ca3e129-dev-001`,
SHA-256
`86adf073fdbc553b7637b1758e73b1f5166287d6bd0c40effd9b019884cd7b2f`.

A fresh action-zero replay of this exact candidate is active at
`/project/flyspeck-candle-runs/v133-dev-direct-boundary05-ca3e129-cc36ecadd-heap16-prepared-001`.
It has emitted exact ordered markers for actions 000--130, independently
clearing the complete base, arithmetic, nonlinear-support, and Marchal
boundaries on `ca3e129`. Raw GRUTOTI passed at action 92. Its 2,364 extracted
MESON progress records have SHA-256
`72cc25686891885bada232cb66ec8f9ff0c1a88952257807dea0c273c43d9448`,
identical to the independent `f958692` replay's extracted record stream.
Raw action 130 then established the unchanged terminal theorem
`Ssrnat.sqrn_distn` and emitted its exact marker. It has no entry or output in
the candidate normalization contract, independently confirming the central
separator repair on the exact `ca3e129` source set. Raw action 131 subsequently
established `Hypermap_iso.fan_hypermaps_iso2`; actions 132--140 emitted all
nine following ordered markers. Action 131 likewise has no candidate
normalization entry or output. The replay reached **141/141** through action
140 and then established the unchanged terminal theorem `Seq.allpairs_catr`
in raw action 141. That action has no candidate normalization entry or output and
emitted its exact marker. The replay is **142/142** through action 141 and is
then established `Seq2.prev_el_hd` in raw action 142 and `Sort.min_k0_le_EL` in
raw action 143. Both emitted their exact ordered markers and neither has a
candidate normalization entry or output. The replay is **144/144** through
action 143. Normalized action 144 then established the unchanged terminal
theorem `Fnjlbxs.FCDJDOT` and emitted its exact marker. The selected object is
byte-identical to the independently confirmed 47-output overlay object at
SHA-256
`4c80875e3e56baf6fef69cc3a5ac3a43ca868f290b1bd8a1c957acc9654557e1`.
The candidate replay is **145/145** through action 144 and is evaluating raw
action 145 with no diagnostic.
The older-overlay focused suffix has now reached action 155 and confirmed its
fixed-format parser failure after exact actions 153 and 154. Static preflight
findings at later actions remain predictions, receive no action credit, and
will not be promoted to observed failures until an evaluator reaches them.

## GRUTOTI correction retained

The earlier comparator-ordering hypothesis is retired for GRUTOTI. The
established cause was name resolution: transitive module opens exposed the
custom `Trigonometry2.SET_TAC`/`SET_RULE` instead of native HOL Light's intended
bindings. Restoring those bindings made all 2,364 MESON progress records match
native output byte-for-byte. Comparator or pointer-equality work will not be
reopened for that incident without independent new evidence.

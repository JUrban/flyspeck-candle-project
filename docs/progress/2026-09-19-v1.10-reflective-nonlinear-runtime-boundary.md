# Direct Flyspeck and reflective nonlinear runtime milestone

Date: 2026-09-19 UTC  
Status: DEVELOPMENT / NON-RELEASE

## Direct Flyspeck headline

- Formal cumulative credit remains **185 actions reached / 184 passed** at
  `05-lp_support-through-184`.
- The uninterrupted cumulative run remains healthy inside the real
  action-184 `hard_1.dat` proof. At the latest sample for this report it had
  emitted terminal **1,923/2,899**; action 184 is not yet passed.
- The isolated focused action-296 process remains CPU-active but still has a
  zero-byte buffered log. It receives no functional, cumulative, S2, S3,
  qualification, promotion, or release credit.
- The PFT oracle remains active and untouched. No qualified Great100 runtime,
  source, report, transcript, or semantic-evidence byte was changed.

## Staged reflective milestones and measured benefit

The earlier decisions remain supported by measurement:

1. The corrected `Kernel.compute` wrapper gate is green at Candle `4a72ddc`.
2. The proof-producing `compute_all` adapter is green at `9c1a38e`, but its
   measured call is 7.373 ms versus 0.667 ms specialized, **11.06x slower**.
   It remains machinery evidence, not a performance target.
3. The generic LP linear-combination core and actual Flyspeck wrapper are green
   at `365f0b3`. Selected real `hard_2` slices are **9.87x, 4.15x, and 3.66x
   faster** locally. Those are proof-producing local gains, not inclusive
   terminal or end-to-end claims.

The plan therefore remains coarse-grained reflection for expensive checker
phases. Small already-specialized conversions are not candidates merely
because they can be reflected.

## Nonlinear proof-producing path

The authenticated nonlinear closure remains exactly 90 source nodes and 193
literal source edges: 34 identities already in the direct manifest and 56
exact extension identities. The parser gate remains 56/56 green.

Fresh one-process first-leaf runs have turned the former parser-only result
into a concrete runtime compatibility sequence:

- v3 reached the closed verifier sources and found the first fixed-format
  dependency after 156.162s;
- v4 reached the diagnostic formatter boundary after 289.197s;
- v5 exposed the first top-level hash-table value restriction after 264.556s;
- v6 reached the missing legacy `Big_int` surface after 291.288s;
- v7 reached the Num/Big_int conversion boundary after 285.932s;
- v8 reached the missing `compare_num` boundary after 292.715s;
- v9 cleared the mechanically complete Num member inventory and exposed one
  native polymorphic `Num.num` ordering after 288.268s;
- v10 completed all of `arith/arith_float.hl`, including its generated HOL
  interval theorems and proof-producing conversions, then stopped at the
  missing native `infinity` binding after 296.827s;
- v11 passed the exact float constants and equality bridge, then exposed the
  adjacent unqualified float-order typing after 299.528s;
- v12 passed the complete float conversion helper and stopped at the missing
  top-level `sign_num` export after 287.806s; and
- v13 loaded all of `arith/more_float.hl`, including its generated HOL
  theorems and interval conversions, then stopped at the first unsupported
  native `assert` in `arith/float_pow.hl` after 271.855s.

The coherent v14 batch covers the complete assertion inventory. It passed the
prior five-minute failure window and was still producing later HOL proof-search
records after eight minutes at this report cutoff. It had not yet emitted the
closure-loaded marker, so this is live progress rather than a pass claim.

Each failure was retained as DEVELOPMENT / NON-RELEASE evidence. None reached
the closure-loaded, smoke-theorem, reconstruction, or formal-verification
markers, so no nonlinear proof or S3 claim is made yet.

## Central compatibility now established

The isolated branch `codex/cv-nonlinear-verifier-v1` now has:

- the mechanically observed complete selected `Big_int` and `Num` surfaces;
- exact arbitrary-integer Num conversions and formatting, including 510
  generated `approx_num_exp` observations matching OCaml 4.14.1 byte-for-byte;
- the one private raw-float Num exponent ordering made explicit as `le_num`;
- exact binary64 definitions for OCaml infinity, negative infinity, and its
  canonical NaN;
- a typed IEEE equality bridge preserving native signed-zero and NaN behavior,
  plus typed IEEE less-than/greater-or-equal bridges preserving native NaN
  branches;
- the central top-level `sign_num` export expected from the native HOL Light
  environment; and
- one condition-preserving `candle_assert` helper plus all 14 active assertion
  sites, retaining the distinct `Assert_failure` constructor on false.

The first-leaf controller authenticates those source-authoritative overlays
before parsing any verifier extension. They are deliberately non-release use
of the older frozen development executable; a fresh linked runtime is required
before release evidence. The current closure contract has 22 normalized
sources and 126 exact operation records (covering all 14 assertion
replacements), and all 36 focused nonlinear tests pass.

## Material plan finding

The nonlinear lane has not yet reached the phase measurements needed to select
the first reflective predicate. The current evidence changes the immediate
plan in one way: finish this finite runtime/frontend surface first, because
profiling before the closure-loaded marker would measure source loading rather
than certificate reconstruction or verification. Once the closure and small
proof gate pass, retain separate reconstruction and formal-verification times
and choose the first coarse computation only if it dominates enough work to
justify translation and proof overhead.

## Next gates

1. Preserve and classify the active v14 run with the complete assertion batch.
2. Continue exact, batched compatibility fixes until the closure-loaded and
   small proof-producing markers pass.
3. Measure the first real leaf's reconstruction and formal-check phases, then
   select the largest semantically clean predicate for reflection.
4. Keep the cumulative action-184, focused action-296, and PFT processes
   isolated and moving; reserve another action-zero replay for a coherent
   direct-Flyspeck compatibility batch.
